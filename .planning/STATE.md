---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: — Real-Code Coroutine Observability
status: executing
stopped_at: Completed 08.5-03-PLAN.md (connect/onboarding → Surface-003 — client LIVE/DEMO derivation + badged sidebar-as-home + 3-step ConnectWizard). Both Wave-1 plans (01 + 03) of Phase 08.5 done; 08.5-02 (Wave 2) remains.
last_updated: "2026-06-27T17:07:00.000Z"
last_activity: 2026-06-27 -- Completed 08.5-03 (connect/onboarding — badged sidebar-as-home + ConnectWizard)
progress:
  total_phases: 8
  completed_phases: 7
  total_plans: 21
  completed_plans: 20
  percent: 95
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-11)

**Core value:** A developer can SEE and UNDERSTAND coroutine/Flow/structured-concurrency execution that is otherwise invisible — reducing time-to-understand.
**Current focus:** Phase 08.5 — align-the-frontend-to-the-validated-sketch-winners-fe-alignm

## Current Position

Phase: 08.5 (align-the-frontend-to-the-validated-sketch-winners-fe-alignm) — EXECUTING
Plan: 08.5-02 remaining (Surface 002 inline source panel — Wave 2)
  08.5-03 ✅ Wave 1 — connect/onboarding → Surface-003 (FE-ALIGN, ONB-01). Greenfield badged sessions-sidebar-as-home + 3-step connect wizard. NEW lib/session-kind.ts: pure deriveSessionKind(session)→'live'|'demo' (demo iff sessionId carries the scenario- prefix the scenarios route mints, else live; unknown→live so a live app never looks like a demo, PD-10 — NO backend field). NEW components/sessions/SessionRow.tsx: one badged row — reused LivePill (LIVE) or neutral DEMO Chip, label + mono {N} active from coroutineCount (sketch's {host} not wire-present → degrades gracefully), selected bg-primary/5 ring-2 ring-primary / hover bg-default-100, literal-class branches (IN-12). NEW components/sessions/SessionsSidebar.tsx (320px Card): header "Sessions" + primary + Connect; useSessions() list partitioned by deriveSessionKind into "Live apps" then "Demo scenarios" groups (text-xs uppercase tracking-wide text-default-400 headers, omitted when empty); empty list folds the inline "No app connected" empty state (Connect your app → onConnect, Run a demo scenario instead → /scenarios) INTO the list, not a standalone screen (UI-SPEC 145). NEW components/connect/ConnectWizard.tsx: HeroUI Modal "Connect your app" — 3 numbered Step rows (1 dep Snippet implementation("com.jh:vizcore-client:0.1"), 2 start() Snippet VizcoreClient.start(appName="order-service") + sub-note, 3 Spinner + mono "Waiting for events from order-service…"); footer Copy-setup-snippet + Cancel + a Skip-to-live fallback. PD-11 first-events signal: on open mints ONE session via useCreateSession, captures the new id, and POLLS it via a LOCAL useQuery({queryKey:['sessions',newId], enabled:!!newId, refetchInterval:300}) — chose the local new-id-scoped query over extending the shipped refetchInterval-less useSession so every existing useSession call site stays byte-equivalent; AUTO-navigates to /sessions/$sessionId the moment the polled coroutineCount transitions >0 (one-shot resolvedRef, scoped to the new id so it never fires off a stale cache, Pitfall 6). routes/sessions/index.tsx repointed to render <SessionsSidebar onConnect={openWizard}/> under Layout + mount <ConnectWizard/>; the old flat-grid confirm()-based create UI dropped (PD-12 — create affordance is now + Connect). appName is a static UI-SPEC constant rendered as React text (auto-escaped), snippets are static public copy — no secret embedded (PD-14, T-085-06/07). Two Rule-3 blocking gate fixes: (1) widened deriveSessionKind param Pick<SessionInfo,'sessionId'>→SessionInfo (excess-property typecheck on the test's full-object literals); (2) removed two eslint-disable react-hooks/exhaustive-deps directives — this project's flat ESLint config does NOT register react-hooks, so they were "rule not found" ERRORS → replaced with plain explanatory comments. TDD both tasks: SessionsSidebar.test.tsx (+7: derivation, LIVE-pill-vs-DEMO-chip, both group headers, + Connect fires, inline empty state + both CTAs) and ConnectWizard.test.tsx (+5: 3 steps + copyable Snippets + step-3 Spinner/waiting copy, AUTO-resolve on polled 0→N with NO click, new-id-scoped poll + explicit refetchInterval asserted via captured useQuery options + no fire at count 0, Skip fallback, Cancel). Literal Tailwind only (IN-12); no backend change, no new dep (package.json/pnpm-lock.yaml untouched). FE gates green: vitest full 61 files/504 tests (was 492; +12), tsc --noEmit 0 errors (project has no pnpm typecheck script), lint 0 errors (5 pre-existing baseline warnings, ≤6 budget), build (tsc+vite) clean. Commits 12aa5ca (feat sidebar + derivation, TDD), e6dd4ae (feat ConnectWizard + route repoint, TDD). Surface-003 closes the third sketch-vs-shipped divergence. Wave 2 (08.5-02) remains: mount the Surface-002 inline source panel into the LiveDockPanel right slot.
  08.5-01 ✅ Wave 1 — live real-app view → Surface-001 IDE-dock (FE-ALIGN). NEW LiveDockPanel.tsx: a presentational IDE-dock shell (rounded-xl bg-content1 border-t-2 border-primary min-h-[200px]) with a tiles-only metric-strip header (LivePill + reused SessionMetrics showLeaks={false}) and a grid-cols-[1fr_320px] body — the live "what's running now" list + a SINGLE inline amber LeakList in the left column, a sourcePanel?:ReactNode slot (muted "Select a coroutine…" placeholder) on the right; LeakList fed by useSessionMetrics(sessionId, streamEnabled, !readOnly) sharing the ['session-metrics',sessionId] React Query key (no extra fetch). ONE additive edit to SessionMetrics.tsx: optional showLeaks?:boolean (default true) guards the internal Potential-leaks Card — every existing call site byte-equivalent (back-compat), dock passes false so the strip is tiles-only (no double leak mount, PD-04a). SessionDetails.tsx: added isLiveView=!replayActive&&!readOnly; the Coroutines tab hosts the live list in the dock (live) or standalone (replay/shared); REMOVED the partial L1 docked-metrics strip + the duplicate SessionMetrics placement; source-selection (onSelect/selectedNodeId) gated to the live view so replay/shared nodes stay presentational (Pitfall 1). Numeral kept text-lg font-semibold (PD-04, no text-2xl); leaks warning/amber never danger. Literal Tailwind only (IN-12). No backend change, no new dep (package.json/pnpm-lock.yaml untouched), use-event-stream.ts untouched. Gates green: vitest full 59 files/492 tests (the initial full parallel run's 2 worker-startup timeouts were the known environmental flakiness — re-ran --no-file-parallelism fully green), tsc --noEmit 0 errors, lint 0 errors (6 pre-existing baseline warnings), build clean. Commits 70e3d8c (feat dock + showLeaks, TDD), bf011c3 (feat mount live-only + reachability). Plan 02 mounts the Surface-002 source panel into the dock's right slot.
  08.4-01 ✅ CR-01 hardening — structurally eliminated the same-FQN duplicate-DTO classpath-shadowing hazard. Task 1 (50f805b, prior session): `git rm` of all 8 :backend duplicates (7 models/*.kt + sync/DeadlockDetector.kt) — the two live SessionInfo consumers (auth/Tenancy.kt, persistence/ExposedSessionStore.kt) bind transitively to the core copy unchanged; comm -12 intersection of the two src/main/kotlin roots is now empty. Task 2 (f6c6107, prior session): verifyNoDuplicateSourceFqns Gradle task added to backend/build.gradle.kts (group=verification, whole-tree relative-path intersection of :backend + :coroutine-viz-core, throws GradleException naming offenders) wired via tasks.named("check"){dependsOn(...)}; no new dependency. Task 3 (a51bed1, TDD): NEW backend/src/test/.../routes/TimelineRouteAssembledTest.kt — boots the REAL :backend module() over the assembled classpath (Ktor Test Host), drives Created→Started→Suspended with a real SuspensionPoint(SpringVizcoreDemoApplication.kt:75), and asserts GET /api/sessions/{id}/coroutines/{cid}/timeline serves a coroutine.suspended event with suspensionPoint.fileName + non-null lineNumber>0 — load-bearing proof the CORE DTO is loaded (the only test shape that can catch the shadowing; module-isolated tests cannot). Three test-only deviations auto-fixed (Rule 1: poll tripped per-IP rate limit 429 → poll in-process projection then single GET; Rule 1: EventBus replay=0 startup race dropped events → await subscriptionCount.first{it>=1} before emitting; Rule 3: detekt LongMethod → extracted driveSuspendedCoroutine helper). Guard NEGATIVE-CHECK confirmed: reintroduced sync/DeadlockDetector.kt → :verifyNoDuplicateSourceFqns BUILD FAILED naming the file → removed → BUILD SUCCESSFUL. Full gate ./gradlew clean check GREEN under JDK 21 (ktlintCheck + detekt + :coroutine-viz-core:test + :test + verifyNoDuplicateSourceFqns). No production source changed beyond the 8 deletions; no FE / product-behavior change; no new dependency. CR-01 closed structurally with a permanent regression guard.
  08.3-02 ✅ Wave 2 contract reconciliation (RCO-06, D-01 end-to-end). Closed the FE/backend divergence that hid the bug. Task 1 (adc32ed): documentation.yaml gained a SuspensionPoint component (field-for-field on the Kotlin type, nullable, required [function,reason]) + suspensionPoint $ref on TimelineEventSummary + kebab-case kind description (coroutine.suspended/started, D-03); openapi.json surgically synced (Phase-2 P01 idiom, 34/-1 valid JSON); generated.ts regenerated via pnpm generate (openapi-typescript 7.13.0) carrying suspensionPoint + SuspensionPoint — proven build output by a two-run idempotency check (byte-identical, T-083-04). Task 2 (d508301): api.ts TimelineEvent/CoroutineTimeline/SuspensionPoint redeclared field-for-field to the generated shape (tsNanos not timestamp; activeDuration/suspendedDuration not activeTime/suspendedTime; dropped duration/dispatcherId/threadId + suspensionPoint.timestamp); all four use-timeline.ts consumers reconciled — useSuspensionPoints emits tsNanos+{function,fileName,lineNumber,reason}+eventSeq (drawer UNCHANGED), useTimelineStats sources activeTime/suspendedTime view-keys from activeDuration/suspendedDuration ?? 0 and avgSuspensionDuration=0 (per-event duration is a deferred source-only stub, D-02 — not fabricated), useDispatcherSwitches→seq/tsNanos/dispatcherName/threadName, useTimelineVisualizationData reads tsNanos + duration:undefined; test fixtures + CoroutineTimelineView reconciled. Two Rule-3 blocking files beyond the plan list — mock-data.ts (MSW generators) + SessionDetails.reachability.test.tsx (08.2 fixture) both built the divergent shape and broke the typecheck → reconciled. FE gates green: tsc --noEmit 0 errors, pnpm test 58 files/478 tests, lint 0 errors (6 pre-existing warnings); no new dep (package.json/pnpm-lock.yaml untouched, T-083-SC). Note: project has no `pnpm typecheck` script — used `npx tsc --noEmit`. Wave 3 (08.3-03) is the live Chrome UAT re-run of 08.2 Test 2 (D-05b).
  08.3-01 ✅ Wave 1 backend source-frame data path (RCO-06, D-01 backend half/D-02/D-03/D-04/D-05a): added nested `suspensionPoint: SuspensionPoint? = null` to TimelineEventSummary (reuse events.SuspensionPoint verbatim, no flatten/new type); implemented the ProjectionService.getCoroutineTimeline stub (was events=emptyList()) — pulls the coroutine's ordered raw events via session.getCoroutineTimeline(newestFirst=false) and maps via a private toSummary(event) helper: CoroutineSuspended→kind="coroutine.suspended" carrying suspensionPoint UNCHANGED, CoroutineStarted→kind="coroutine.started", all else→null (mapNotNull). Null-on-unknown-coroutine guard + SessionRoutes.kt:286 route call site UNCHANGED; thread/dispatcher/duration left null (deferred). Non-mocked CoroutineTimelineSourceFramesTest drives a REAL VizSession over Created→Started→Suspended (Rule 1: projection only registers a coroutine on CoroutineCreated, so the guard needs it — first run failed "unknown coroutine: request-2"), live send + poll-to-converge, load-bearing D-05a assertion fileName=="SpringVizcoreDemoApplication.kt" + real lineNumber>0. Backend gates green (ktlintCheck + detekt + :coroutine-viz-core:test, 0 violations, JDK 21) — commits 5dd9834 (feat DTO), be6bb16 (feat projection), 0f4fcc1 (test). Wave 2 (08.3-02) extends OpenAPI + regen api-types + reconcile FE; Wave 3 (08.3-03) live UAT re-run.
  08.2-02 ✅ Wave 2 wire source drawer into the live view (RCO-06, D-02/D-05/D-06, IN-12): added optional/additive onSelect?(id)/selectedNodeId? to CoroutineTree (TreeNode) + CoroutineTreeGraph (TreeNodeComponent), threaded recursively; tree node Card switched to HeroUI isPressable+onPress (Rule 3 — HeroUI Card destructures onClick and gates it behind isDisabled=!isPressable so plain onClick is swallowed; isPressable gives native role=button firing on click+Enter/Space, aria-label "Open source for {label}" still passes through), graph node (plain div) keeps the manual role/tabIndex/aria-label/onClick/onKeyDown spread; literal selected highlight ring-2 ring-primary bg-primary/5 (no cn()); nodes presentational when onSelect omitted (replay/shared back-compat). SessionDetails lifts selectedCoroutineId, threads onSelect/selectedNodeId ONLY into the live "What's running now" tree/graph (D-06 — 2 occurrences, graph+list), mounts CoroutineSourceDrawer once inside the live block (absent in shared/replay), label from renderedCoroutines.find. NEW SessionDetails.reachability.test.tsx (Rule 3 — the stub-mocked SessionDetails.test.tsx can't host a real-component proof; module-scoped vi.mock) renders the REAL tree + REAL drawer/stack mocking only apiClient.getCoroutineTimeline + toast: click live node → drawer title "Coroutine source — LiveWorker" + real UserService.kt:42 file:line jump target render, selected node carries the ring, no eager fetch before selection (single suspension frame backs both Created at + Suspended at so jump target asserted via findAllByRole>=1; node captured by ref before open since the Drawer focus-trap marks bg inert). No backend change, no new dep; use-event-stream.ts/package.json/pnpm-lock.yaml untouched (D-10 git-diff guards empty); FE gates green: pnpm test 58 files/478 tests, lint 0 errors (6 pre-existing out-of-scope warnings), build (tsc+vite) clean — commits 22b7429 (feat tree+graph onSelect), 405cab1 (feat drawer mount + reachability). Live-app open/close + clipboard jump-to-code human-verify deferred to /gsd-verify-work.
  08.2-01 ✅ Wave 1 source-attribution drawer surface (RCO-06, D-01/D-03/D-04/D-07/D-08/D-09): NEW CoroutineSourceStack (headerless Created at / Suspended at file:line body — Created-at derived from the first/started timeline event's suspensionPoint; suspension rows from useSuspensionPoints; jump-to-code copied VERBATIM from CoroutineTimelineView — user frames font-semibold text-primary role=button "Jump to code: {file}:{line}" copy {file}:{line} + toastSuccess "Copied {file}:{line}", library frames inert; loading "Loading timeline...", empty EmptyState "No source attribution yet", in-body error "Couldn't load coroutine source." — NOT a toast; lazy enabled-guarded fetch, disabled when coroutineId null; no StatCard/time-bar/events-list per D-03) + NEW CoroutineSourceDrawer (HeroUI Drawer placement=right size=lg, title "Coroutine source — {label||id}", close Button aria-label="Close source drawer", mounts the body; absent when isOpen false; Drawer ships with installed @heroui/react@2.7.11 — NO new dep). Literal Tailwind only (IN-12); no backend change. FE gates green: pnpm test 57 files/472 tests, lint 0 errors (6 pre-existing out-of-scope warnings), build (tsc+vite) clean; package.json/pnpm-lock.yaml unchanged — commits 48819fb (test/RED), d7ee0d3 (feat/GREEN), b359d1a (feat drawer). Wave 2 (08.2-02) mounts the drawer in SessionDetails + threads onSelect into CoroutineTree/CoroutineTreeGraph.
  08.1-01 ✅ Wave 1 FE primitives + jump-to-code (RCO-06/RCO-07 FE): NEW src/lib/source-frames.ts (isLibraryFrame/isUserFrame literal-prefix classifier — kotlinx.coroutines/kotlin.coroutines/kotlin./java./jdk./sun. + null/empty = library); NEW src/components/LivePill.tsx (LIVE pill bg-success/10 + "~150ms poll" / DEMO bg-default-100, driven by streamEnabled); Delta S1 jump-to-code on the two existing inert file:line sites — SuspensionSourceRef in CoroutineNodeEnhanced (user frames → role=button + aria-label "Jump to code: {file}:{line}" + copy {file}:{line} to clipboard + toastSuccess "Copied {file}:{line}"; library frames stay text-default-400 inert) and TimelineSourceRef in CoroutineTimelineView (same on suspension-card + event-card sites, wrapped in "Copy file:line" Tooltip; library frames keep text-default-500). Delta L2 (live-card source chip / SourceAttribution panel) NOT built — DEFERRED. Literal Tailwind only (IN-12); no new deps; backend + use-event-stream.ts untouched. FE gates green: pnpm test (3 files/12 tests), lint 0 errors, build (tsc+vite) clean — commits 4b3032c, 55b61bc, ca382f0, c2dbf8c.
  08-01 ✅ DebugProbes hierarchy reconstruction (RCO-06, D-01/D-02/D-03): pure HierarchyReconstructor (nearest-observed-ancestor over Job), batch CoroutineInfoAdapter.toSnapshots (parent+child keys from one jobKeys cache), CoroutineSnapshot.parentKey, synthesizer now emits non-null dp- parentCoroutineId + dispatcher-derived scopeId → existing ProjectionService/CoroutineTree populate hierarchically with zero FE change — commits 8565f5d, fdad6cc, 4541eb1, 4ef207f, 1445a9a, f3dd825
  08-02 ✅ MetricsProjection + /metrics route (RCO-07, D-04/D-05/D-06/D-07): per-session projection mirroring ProjectionService (init-subscribe + rebuildFrom + snapshot) computes active/peak/observed-throughput/dispatcher-util (from scopeId, NOT ThreadAssigned)/age-threshold leaks; VizSession.metricsProjection rebuilt on rehydrate (Pitfall 5); new tenant-scoped GET /api/sessions/{id}/metrics (resolveScopedSession → 404 cross-tenant, leakThresholdMs coerceIn-clamped) → MetricsResponse/LeakDto — commits f5e41d8, c11b5d3, d0c407a, 13b048d
  08-03 ✅ (code) FE live view + metrics panel (RCO-06/RCO-07 FE, D-06/D-07/D-08): MetricsResponse/LeakDto TS types + apiClient.getMetrics + useSessionMetrics (poll-while-live clone of useThreadActivity, enabled=!readOnly T-08-08); SessionMetrics Card grid (Active primary/Peak/Throughput "—"-or-"/s"/Dispatcher util/Potential leaks) + LeakList (warning amber "N potential leak{s}" + "{name} · alive {dur}" + threshold tooltip, never danger); SessionDetails active-only "What's running now" default (TERMINAL_STATES filter, NODE_CAP=200 "N more coroutines", "Show completed (N)" toggle, EmptyState) + SessionMetrics mount; use-event-stream.ts untouched; literal Tailwind only (IN-12). Gates green: pnpm test 446/52, lint 0 errors, build (tsc+vite) clean — commits 9265c64, 3a348f2, 04653fc, 226edf5, 3f86208, 62658d9. Task 4 (live demo SC#1/SC#2 human-verify) DEFERRED to /gsd-verify-work — NOT visually verified.
  08-04 ✅ (gap closure) CR-01 leak-age persistence fix (RCO-07/RCO-06): replaced cross-JVM System.nanoTime() leak-age subtraction with a wall-clock createdAtEpochMs basis (System.currentTimeMillis at construction; defaulted 0L last param, JSON-payload round-tripped, NO schema migration); MetricsProjection.snapshot(nowEpochMs, leakThresholdMs, nowNanos=System.nanoTime()) computes aliveMs in millis and skips non-positive ages; /metrics route reads System.currentTimeMillis(). Proven by MetricsLeakAgePersistenceTest crossing a real two-store H2-file restart boundary with the route's real clock (RED→GREEN). Folded WR-01 (single-lock rebuildFrom via applyLocked — clear+replay atomic) + WR-04 (throughput evicted against the nowNanos read clock → idle session decays to ~0/s); two-clock split (leak age=epoch, throughput=nanoTime) documented in the class doc. Backend :coroutine-viz-core:test + :test + ktlintCheck + detekt green (JDK 21) — commits 4fe218e, 04521eb, 95a1595. Truth #7 live-demo human UAT still deferred to /gsd-verify-work.
Phase 6: ✅ SHIPPED — PR #72 MERGED to main (commit 7481612).

Phase 7: all 4 sequential plans executed:
  07-01 ✅ move vizEventSerializersModule/appJson into coroutine-viz-core (enabler, RCO-04) — commits c62b111, 4834901
  07-02 ✅ install Ktor WebSockets + auth-scoped /api/sessions/{id}/ingest WS route → VizSession.send (RCO-05) — commits 3d1437a, ec7f5bf
  07-03 ✅ new coroutine-viz-client module: VizcoreClient (JWT session + DebugProbesSource + client WS, reconnect) (RCO-04) — commits dd134ff, 8ffed9b, 0c5a9ce
  07-04 ✅ CR-01 gap closure: lifetime-scoped OutboundBuffer bridges startup + reconnect windows (zero loss); surfaced dropped counter; delay(200) removed; EventBus.subscriptionCount accessor (RCO-04) — commits 0a687c2, 35b263b
  Decisions locked: WebSocket transport, client-creates-session-with-JWT. CONTEXT+RESEARCH on disk. GH #67 (RCO-04), #68 (RCO-05).
Status: Executing Phase 08.5 (Plans 01 + 03 of 3 complete; 08.5-02 remaining)
Last activity: 2026-06-27 -- Completed 08.5-03 (connect/onboarding — badged sidebar-as-home + ConnectWizard)

Progress: [████░░░░░░] 38% (8-phase view; product itself ~92% built, v1.1 adds real-code observability)

## Performance Metrics

**Velocity:**

- Total plans completed: 54
- Average duration: ~17 min
- Total execution time: ~34 min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 foundation | 2/5 | ~34 min | ~17 min |
| 01 | 15 | - | - |
| 02 | 8 | - | - |
| 03 | 7 | - | - |
| 07 | 4 | - | - |
| 08 | 4 | - | - |
| 08.1 | 2 | - | - |
| 08.3 | 3 | - | - |
| 08.4 | 1 | - | - |

**Recent Trend:**

- Last 5 plans: 01-01 (~20 min), 01-02 (~15 min)
- Trend: stable

*Updated after each plan completion*
| Phase 01 P03 | 6 | 3 tasks | 15 files |
| Phase 01 P04 | ~10min | 3 tasks | 4 files |
| Phase 01 P05 | 15min | 2 tasks | 8 files |
| Phase 01 P10 | 17 | 2 tasks | 2 files |
| Phase 01 P11 | 4min | 2 tasks | 1 files |
| Phase 01 P12 | 22 | 2 tasks | 7 files |
| Phase 01 P13 | ~7 min | 2 tasks | 5 files |
| Phase 01 P14 | ~10 min | 3 tasks | 10 files |
| Phase 01 P15 | ~14min | 3 tasks | 5 files |
| Phase 02 P01 | ~18min | 2 tasks | 8 files |
| Phase 02 P02 | ~12 min | 2 tasks | 4 files |
| Phase 02 P03 | ~17 min | 2 tasks | 6 files |
| Phase 02 P04 | ~25 min | 3 tasks | 5 files |
| Phase 02 P05 | ~12 min | 2 tasks | 10 files |
| Phase 02 P06 | ~14 min | 2 tasks | 7 files |
| Phase 02 P07 | ~16 min | 3 tasks | 7 files |
| Phase 02 P08 | ~20 min | 2 tasks | 7 files |
| Phase 03 P01 | ~40min | 2 tasks | 15 files |
| Phase 03 P02 | ~13min | 2 tasks | 12 files |
| Phase 03 P03 | ~14min | 2 tasks | 8 files |
| Phase 03 P04 | 30min | 2 tasks | 11 files |
| Phase 03 P05 | ~8min | 2 tasks | 10 files |
| Phase 03 P06 | ~12min | 2 tasks | 10 files |
| Phase 03 P07 | ~25min | 3 tasks | 4 files |
| Phase 07 P02 | ~25 min | 2 tasks | 7 files |
| Phase 07 P03 | ~30 min | 2 tasks | 7 files |
| Phase 07 P04 | ~50 min | 2 tasks | 6 files |
| Phase 08 P01 | ~22 min | 3 tasks | 7 files |
| Phase 08 P02 | ~18 min | 2 tasks | 6 files |
| Phase 08 P04 | ~14 min | 3 tasks | 6 files |
| Phase 08.2 P01 | ~13 min | 2 tasks | 4 files |
| Phase 08.2 P02 | ~22 min | 2 tasks | 5 files |
| Phase 08.3 P01 | ~6 min | 3 tasks | 3 files |
| Phase 08.3 P02 | ~7 min | 2 tasks | 9 files |
| Phase 08.5 P01 | ~21 min | 2 tasks | 6 files |
| Phase 08.5 P03 | ~7 min | 2 tasks | 7 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table (26 Accepted ADRs locked; ADR-010 Proposed/advisory).
Recent decisions affecting current work:

- Treat the duplicate `session/` fork removal as the first work item — it is the root cause of the unbounded-store (gap 2) and unwired-perf (gap 3) findings.
- Persistence (ADR-015) and route-level auth (ADR-016) are designed-but-unimplemented; current deployment is in-memory/ephemeral (ADR-009).
- Business-model and KPI variants are unresolved — V2 is the working default; not blocking engineering.
- [Phase 07, Plan 02]: WebSocket ingest is the server half of real-app transport (RCO-05) — installed once via configureWebSockets() (1 MiB frame cap), registered inside authenticatedApi { rateLimit(api) { } }; publishes into the server-resolved session (never the frame's sessionId, T-07-03) so all downstream (EventStore→snapshot→EventBus→SSE→FE) is pure reuse. AUTH-04 cross-tenant refusal + no-Bearer rejection test-proven over an H2 ExposedSessionStore.
- [Phase 07, Plan 03]: VizcoreClient is the client half of real-app transport (RCO-04) in a NEW coroutine-viz-client gradle module (JVM 17, Ktor client CIO) kept OUT of core (Ktor-client deps would break core's no-web-framework rule). start(appName, backendUrl, token) authenticates via JWT, POSTs /api/sessions for a server sessionId, builds a LOCAL VizSession with the SERVER id (Pitfall 1/T-07-03), drives a DebugProbesSource, and forwards bus.stream() over a client WS using the SHARED core appJson (Pitfall 3 — wire == ingest == SSE/FE). Reconnect loop on a private SupervisorJob (never GlobalScope) with capped exp backoff+jitter (500ms→30s, T-07-08); CancellationException rethrown; stop() idempotently stops source + closes client + cancels scope. JWT always in the Authorization: Bearer header, never the URL (T-07-02). Notes: kotlinx-serialization-json declared directly in the client (core exposes it as implementation); tests use @org.junit.jupiter.api.Test for platform discovery; round-trip proven in-process with a synthetic event landing in the backend session carrying the server id.
- [Phase 07, Plan 04]: CR-01 closed (RCO-04). The 07-03 client lost events emitted before the first WS connect and during every reconnect-backoff window because IngestTransport subscribed the replay=0/DROP_OLDEST EventBus INSIDE the open socket. Fix: a lifetime-scoped OutboundBuffer (bounded Channel<VizEvent>, cap 10k) written by ONE source-side collector (feed) that subscribes the bus exactly once and survives reconnects, drained per-socket (drain); events emitted while disconnected are retained and delivered on the next connect. Startup race closed deterministically — feed() awaits its OWN bus subscription (subscriptionCount STRICTLY above the pre-launch baseline, NOT >=1, because VizSession eagerly subscribes its ProjectionService collector at construction) before returning, and VizcoreClient.start() runs feed-then-source. Overflow is drop-newest-when-full with a surfaced AtomicLong `dropped` counter (observable, never silent) replacing the EventBus silent DROP_OLDEST at the bridge (T-07-08 instrumented). delay(200) removed from VizcoreClientTest, replaced by a zero-loss completeness assertion gated on subscriptionCount>=2 (forced first-attempt drop proves the reconnect window). One additive Ktor-free core change: EventBus.subscriptionCount: StateFlow<Int>. No new dependency. Full gate (ktlintCheck detekt + both module tests + build) green under JDK 21.
- [Phase 1]: Folded runtime-audit fixes FIX-01..04 into Phase 1 (edited goal/requirements/success criteria); FIX wave executes first, before FND-01 de-fork. Evidence: VERIFICATION.md runtime addendum + SCENARIO-AUDIT.md (2026-06-11).
- [Phase 1, Plan 02]: ValidationResult renamed to ValidationResponse (no alias) — all consumers must use new name; ValidationWarningCard left in file with local type (unused, backend has no Warning variant); api-client.ts updated as part of type rename (Rule 3).
- [Phase ?]: D-01 big-bang delete: 10 fork files removed in one commit; no import changes needed (same package name)
- [Phase ?]: APP_VERSION constant '0.0.1' hardcoded from build.gradle.kts version string (simplest non-empty version for Phase 1)
- [Phase ?]: OpenAPI security: [] global declaration satisfies security-defined lint rule without real auth; ADR-016 auth planned for Phase 3
- [Phase ?]: PROD-02: logstash dep added; prod logback profile wired via JVM flag in Dockerfile ENTRYPOINT
- [Phase ?]: PROD-05: /metrics endpoint; all 7 ADR-020 metrics wired; coroutine-viz-core kept Micrometer-free via callbacks
- [Phase 1, Plan 09]: VizScope terminal ordering fix: emit JobStateChanged BEFORE coroutineFailed/coroutineCancelled so terminal event has highest seq; vizAsync left unchanged (no JobStateChanged emitted there)
- [Phase ?]: TimingAnalyzer ns->ms: divide by NANOS_PER_MILLI=1_000_000L; frontend BackendTimingReport unchanged (already ms contract)
- [Phase ?]: Normalization already correct; added discriminator regression test as the missing proof artifact
- [Phase ?]: [Phase 1, Plan 13]: Max-wait caps added to both 01-12 debounces (SSE invalidation 1000ms, session refetch 1500ms); every SSE flush invalidates both sessions and thread-activity keys with a 5s live fallback poll (CR-01/CR-02 closed)
- [Phase ?]: [Phase 1, Plan 13]: SessionDetails max-wait window ref reset moved to a streamEnabled-keyed teardown effect — per-event cleanup would restart the max-wait clock (plan instruction corrected via Rule 1)
- [Phase 1, Plan 14]: Frontend aligned to the real GET /threads wire shape (Map<threadId, ThreadEvent[]>) via pure buildThreadLanes adapter; fictional {threads, dispatcherInfo} mock generators deleted so MSW serves the backend's exact shape (CR-02 regression trap closed)
- [Phase 1, Plan 14]: useThreadLanesByDispatcher groups null-dispatcher lanes via (dispatcherId ?? 'Unknown') — strict id equality would silently drop them (Rule 1 fix to plan instruction)
- [Phase 1, Plan 14]: SessionDetails.test.tsx mocks apiClient (not the hook) and renders the real ThreadTimeline so the UAT-gap-1 integration test exercises the genuine wire-shape pipeline
- [Phase 1]: [Phase 1, Plan 15]: SSE first-connect fix is two-sided: backend ': connected' comment frame flushes headers on 0-event sessions; frontend retries fatal (readyState=2) EventSource errors with bounded backoff (1s,2s,4s,8s,8s; 5 retries, budget reset on open) plus maxSeqRef replay dedup so WR-01 full-history replay never duplicates the live view
- [Phase 1]: [Phase 1, Plan 15]: Ktor testApplication live-stream reads must run under withContext(Dispatchers.Default) — the virtual-time dispatcher fires withTimeout before the wall-clock server writes bytes (MetricsWiringTest convention)
- [Phase 2, Plan 01]: Compare endpoint renamed to GET /api/sessions/compare?a=&b= (D-09); SessionComparison gained distinctThreadsDiff (CMPR-01 thread metric) derived from distinct ThreadAssigned.threadId via store.all(); strict 404 on unknown id retained (D-12, no getOrCreate)
- [Phase 2, Plan 01]: Static path /api/sessions/compare coexists safely with /api/sessions/{id} — Ktor prioritizes constant segments over parameterized ones (confirmed by passing integration test)
- [Phase 2, Plan 01]: shared/api-types/openapi.json synced surgically (compare path + SessionComparison/CoroutineComparison/ErrorResponse) not full YAML->JSON regen — JSON copy is stale (22 vs 55 paths); full regen remains a pending todo
- [Phase ?]: [Phase 2, Plan 02]: events/ + checksystem/ forks deleted (54 .kt); core is keeper (flow events keep @SerialName the forks lacked). ns->ms fix (NANOS_PER_MILLI) landed in core TimingAnalyzer with proving test; core's own TimingAnalyzerTest rescaled to ms.
- [Phase ?]: [Phase 2, Plan 02]: D-12 strict-404 audit complete — all 22 getOrCreateSession callers are scenario/pattern/flow create POSTs; every read/SSE/compare route uses getSession->404 (WR-07 closed backend-wide).
- [Phase ?]: [Phase 2, Plan 02]: parallel Gradle test-worker flakiness is pre-existing/environmental — full suite green with --max-workers=1; deferred, not a de-fork regression.
- [Phase ?]: [Phase 2, Plan 03]: projectCoroutines emits nodes in creation order (matches backend RuntimeSnapshot LinkedHashMap) so deep-equal vs server snapshot holds; terminal state by event kind only (FIX-03). useReplay clamp aligned to ADR-017 (base 50-2000ms then /speed); two 10ms timing tests realigned to 50ms floor (Rule 1).
- [Phase ?]: [Phase 2, Plan 04]: @heroui/react pinned to ~2.7.11 (tilde, not ^2.7 caret) to keep the React Aria bump isolated to the 2.7 line — lockfile had drifted to 2.8.5 and an open caret would bypass the full-suite + human-smoke gate; ToastProvider mounted above the router (issue #5086 ordering).
- [Phase ?]: [Phase 2, Plan 05]: Replaced pre-existing foreignObject exportToSvg(HTMLElement) with D-21 findSvgRoot + exportToSvg(SVGSVGElement) whitelist (10 props, Pattern 4/T-02-08); no production importer existed (Rule 1).
- [Phase ?]: [Phase 2, Plan 05]: ExportMenu Record item enabled-but-no-op (onRecord placeholder) when supported, disabled+D-25 tooltip when MediaRecorder/codec unsupported; 02-08 wires recording.
- [Phase 2, Plan 07]: RPLY-01/02/03 complete — replay mounted in SessionDetails as a frozen-snapshot time-travel view (useReplay over an entry snapshot; panelEvents/panelCoroutines switch to projected visibleEvents). SSE invalidation gated via a replayActive ref so the EventSource stays open and events buffer for the clickable "N new events" badge, with exactly one flush on exit (D-04); both useEventStream and the local debounced refetch gated. Phase-1 dedup/backoff/max-wait untouched. Recording-state slot is props-driven for 02-08. Closeout (SUMMARY+tracking) done by a continuation agent after an executor API socket drop — no work lost, no new feat commits.
- [Phase 2, Plan 08]: EXPT-02 WebM tier complete — pure record-replay.ts pipeline (pickMimeType vp9→vp8→webm cascade/D-25, estimateDurationMs 50–2000ms clamp÷speed/D-26, createReplayRecorder 2x mirror-canvas + captureStream(0)+requestFrame/D-27) + useRecordReplay scripted glue (enter replay→seek0→record→auto-stop at last event→download .webm, >120s confirm modal/D-26, visibilitychange→hidden abort/D-24, controller Stop discard/D-23) + RecordConfirmModal + SessionDetails wiring (ExportMenu onRecord→startRecording, ReplayController recording props). record-replay.ts stop() now resolves the .webm filename so the success toast shows the real name. Real-codec validation deferred to Phase 5 Playwright per VALIDATION.md. Continuation closeout: Task 1 (cd8fc20) committed by prior executor before an API socket drop; this agent finished Task 2 (236c255) + wrote SUMMARY/tracking — no Task 1 re-commit.
- [Phase 2, Plan 06]: CMPR-02 complete — /compare?a=&b= route (validateSearch normalizes/drops blank ids, T-02-10) drives controlled ComparisonView selection (shareable URL, D-10); SyncedTreePair renders its own clickable tree nodes (not CoroutineTree) for selection rings + delta badges, counterpart match by label then coroutineId (D-19/D-20); session-not-found EmptyState on 404 (D-12). Route component uses useSearch({strict:false}) so it mounts under a standalone test router (file-route re-parent duplicates __root__).
- [Phase ?]: [Phase 3, Plan 01]: Persistence behind storage.type=database via SessionManager.useStore() facade + VizSession injectable eventStoreFactory (default in-memory); coroutine-viz-core stays DB-free. Payload as portable CLOB (de)serialized with the SSE PolymorphicSerializer; JSONB/GIN deferred. Exposed 1.3.0 needs top-level v1.core.{eq,greater,and}; H2 needs CASE_INSENSITIVE_IDENTIFIERS=TRUE to match Exposed's quoted lowercase columns.
- [Phase ?]: [Phase 3, Plan 02]: Dual-provider fail-open auth — authDisabled computed once (no keys AND no usable jwt) makes authenticatedApi() a pure pass-through (D-04a); else authenticate(api-key, jwt) so EITHER credential satisfies (D-08). SSE auth uses ?token= query param (param name 'token', SSE_TOKEN_QUERY_PARAM) via jwt authHeader{} fallback — LOCKED contract Plan 05 binds to. currentPrincipal() resolves ApiKeyPrincipal|UserPrincipal for Plan 03 tenancy. Refresh tokens deferred. configListOrEmpty guards MapApplicationConfig missing-list crash.
- [Phase 3, Plan 05]: Frontend auth loop (AUTH-03) at the single api-client choke point — fetchJson attaches Authorization: Bearer only when getToken() is non-null (no header when auth off, D-07/D-08); a 401 calls clearToken()+navigateToLogin() then rethrows (D-05); createEventSource appends ?token= (locked SSE contract) when a token exists. auth-store = localStorage `vizcore.jwt` + in-memory source of truth rehydrated at module init (D-06). Router-navigate indirection (lib/navigation.ts registerNavigator/navigateToLogin, wired to router.navigate in main.tsx) keeps the api-client testable + avoids a circular router import. login() is a dedicated method (NOT fetchJson) so the token-endpoint 401 = wrong-creds (typed LoginAuthError) never fires the global interception. /login route is OUTSIDE Layout with exact UI-SPEC copy; redirect-back via ?redirect= search param (default /). Share methods (createShare/listShares/revokeShare/getSharedSession) + types/share.ts DTOs added now so Plan 06 needn't re-open the client; getSharedSession returns a typed SharedSessionResult (ok|expired|not-found|rate-limited) for the 200/410/404/429 matrix and attaches no Bearer (the share token is the credential).
- [Phase 3, Plan 03]: Tenancy (AUTH-04/D-03) via a backend-only TenantScopedSessionStore interface (the core SessionStoreInterface is untouched, SDK stays DB/auth-free). TenantContext sealed: Scoped(tenantId)|Admin|Unscoped; resolve(principal) → UserPrincipal.userId / ApiKeyPrincipal.name / ADMIN-bypass / null→Unscoped (auth-off global, D-04b). Filter lives in the store as tenantPredicate(): Op<Boolean> (tenant_id eq for Scoped, Op.TRUE for Admin/Unscoped) on every read/delete; Scoped creation stamps tenant_id (null for Unscoped/global → not visible to scoped tenants). Routes narrow SessionManager.backingStore() and fall back to unscoped SessionManager in memory mode. DbRetentionPolicy (PERS-03, sibling to in-memory RetentionPolicy, ADR-015 30d/100000/60min): max-age DELETE guarded by NOT EXISTS active-share subquery (expires_at IS NULL = never-expires = always active; Pitfall 6/ADR-019) + per-session event-trim by seq watermark; wired in Application.module only when storage.type=database, app-scope launch (no global scope), stop on ApplicationStopping.
- [Phase ?]: Sharing UI reuses SessionDetails behind a readOnly prop via React Query cache-seeding (no fork, D-10); /shared/$token is a standalone shell with no Layout chrome
- [Phase 08.2, Plan 01]: Source-attribution drawer surface (RCO-06, D-01/D-03/D-04/D-07/D-08/D-09). Gave Phase 08.1's already-built-but-unmounted jump-to-code a reachable home. Chose to EXTRACT a NEW headerless CoroutineSourceStack (NOT an isEmbedded chrome-suppression flag on CoroutineTimelineView) so the timeline view stays untouched and the drawer body is source-focused (D-03: no StatCard/time-bar/timeline-events list). Created-at frame derives from the first/started timeline event's suspensionPoint (fallback: first event's suspensionPoint); the Created at section is omitted when no creation frame exists, so a suspension-only coroutine still renders. copyFileLine + TimelineSourceRef copied VERBATIM from CoroutineTimelineView (LOCKED v1 — user frames font-semibold text-primary role=button, library frames inert). A small SourceFrameRow helper holds the literal suspension-row markup so Created at and Suspended at share one visual idiom. CoroutineSourceDrawer is a 1:1 translation of the existing SessionDetails Modal pattern to the HeroUI Drawer family (placement=right size=lg), verified exported at the installed @heroui/react@2.7.11 — NO new dependency (package.json/pnpm-lock.yaml unchanged). Literal Tailwind only (IN-12); no backend change. Tested in isolation: the stack mocks the use-timeline hooks (states + jump-to-code + inert-library + disabled-when-null), the drawer mocks the body (title + close→onClose + absent-when-closed). FE gates green (472 tests, lint 0 errors, build clean). Wave 2 (08.2-02) imports CoroutineSourceDrawer and wires onSelect from the live tree/graph.
- [Phase 08.3, Plan 02]: Contract reconciliation (RCO-06, D-01 end-to-end). Closed the FE/backend divergence (FE api.ts declared suspensionPoint; the generated/backend DTO did not — the exact gap that hid the bug). documentation.yaml gained a SuspensionPoint component (field-for-field on the Kotlin SuspensionPoint, nullable, required [function,reason]) + a suspensionPoint $ref on TimelineEventSummary + a kebab-case kind description (coroutine.suspended/started, D-03). openapi.json was surgically synced (the Phase-2 P01 idiom — the stale JSON intermediate is patched per-schema, NOT regenerated from yaml). generated.ts was regenerated via pnpm generate (openapi-typescript 7.13.0); proven to be build output (not a hand-edit, T-083-04) by a two-run idempotency check producing a byte-identical file. FE api.ts redeclared TimelineEvent/CoroutineTimeline/SuspensionPoint field-for-field to match the generated shape (chose redeclaration over re-exporting components["schemas"][...] so kind stays string for the hooks' string-literal filters and the whole stale generated surface need not be enabled via index.ts) — dropped timestamp/duration/dispatcherId/threadId (event), activeTime/suspendedTime (timeline), and the extra suspensionPoint.timestamp nobody read. All four use-timeline.ts consumers reconciled to the generated field names; the source-only DTO (D-02) means per-event duration + dispatcherId/threadId are a deferred stub — useTimelineStats reports avgSuspensionDuration=0 and useDispatcherSwitches omits ids rather than fabricating data; useSuspensionPoints still emits {function,fileName,lineNumber,reason}+eventSeq so the 08.2 drawer renders UNCHANGED. Two Rule-3 blocking files beyond the plan's <files> list (mock-data.ts MSW generators + SessionDetails.reachability.test.tsx 08.2 fixture) also built the divergent shape and broke the typecheck — reconciled. FE gates green (tsc --noEmit 0 errors; pnpm test 58 files/478 tests; lint 0 errors, 6 pre-existing warnings); no new dependency (T-083-SC, lockfiles untouched). Note: the project has no `pnpm typecheck` script (the plan's verify command) — used `npx tsc --noEmit`. Commits adc32ed (OpenAPI+codegen), d508301 (FE reconciliation).
- [Phase 08.2, Plan 02]: Wire the source-attribution drawer into the live view (RCO-06 — now USER-REACHABLE). Added optional/additive onSelect?(id)/selectedNodeId? to CoroutineTree + CoroutineTreeGraph (threaded recursively), wired ONLY at the live "What's running now" mount in SessionDetails (D-06 — replay/shared node renders stay presentational). Two Rule-3 deviations from the plan's read_first assumptions: (1) HeroUI Card swallows a plain onClick — it destructures onClick and routes it through useAriaButton with isDisabled=!isPressable, so the tree node uses isPressable+onPress (native role=button, click+Enter/Space) instead of the planned manual spread; the graph node is a plain div so it keeps the manual role/tabIndex/aria-label/onClick/onKeyDown spread. (2) The reachability proof lives in a NEW SessionDetails.reachability.test.tsx, not the planned SessionDetails.test.tsx, because that file's module-scoped vi.mock() stubs of CoroutineTree/Graph cannot coexist with a real-component proof; the new file renders the REAL tree + REAL drawer/stack and mocks only apiClient.getCoroutineTimeline + toast, asserting click→drawer title + a real UserService.kt:42 file:line jump target + the selected-node ring + no eager fetch. Literal Tailwind only (no cn(), IN-12); literal ring-2 ring-primary bg-primary/5 selected highlight. No backend change, no new dep, use-event-stream.ts/package.json/pnpm-lock.yaml untouched (D-10 verified). FE gates green (478 tests, lint 0 errors, build clean). Live-app open/close + clipboard jump-to-code human-UAT deferred to /gsd-verify-work. Commits 22b7429, 405cab1.
- [Phase 8, Plan 01]: DebugProbes hierarchy reconstruction (RCO-06, D-01/D-02/D-03). Root cause of the flat ~1,800-card real-app wall: the DebugProbes path emitted parentless, "debugprobes"-bucketed coroutines because the direct parent of an observed leaf is usually an UNOBSERVED intermediate scope/async job. Fix is pure + additive: new HierarchyReconstructor (inverts Job.children over the whole reachable set, walked-set guards re-entry T-08-03, climbs to nearest OBSERVED ancestor — spike-proven D-02, operates only on kotlinx Job so it is fakeable) folded INTO a new batch CoroutineInfoAdapter.toSnapshots(infos) (Open Q4) so parent and child keys come from the SAME jobKeys cache (child.parentKey == parent's own key — edges connect, T-08-02); CoroutineSnapshot gained an all-defaulted parentKey: CoroKey?. Synthesizer contextFor now sets parentCoroutineId = parentKey?.let { "dp-${it.token}" } (identical dp- form as coroutineId, so ProjectionService's CoroutineCreated branch wires the tree edge with ZERO downstream change) and scopeId = dispatcherName ?: sourceId (D-03 dispatcher grouping, replacing the hardcoded "debugprobes"). DebugProbesSource.dump switched to the batch path; pollTick unchanged. Full backend gate (ktlintCheck detekt + core tests) green under JDK 21; ProjectionService test set green proves the edge-wiring. Note: lint tasks live at backend root not the submodule; the :spike main is a blocking long-runner so regression was verified by compiling the demo (algorithm file untouched).
- [Phase 8, Plan 02]: MetricsProjection + /metrics route (RCO-07, D-04/D-05/D-06/D-07). New per-session MetricsProjection in coroutine-viz-core mirrors ProjectionService EXACTLY (init eventBus.stream() subscribe + rebuildFrom(events) clear-then-replay + read-only snapshot(nowNanos, leakThresholdMs)) so it is DB-rehydrate/replay consistent. Metrics: active = non-terminal active-set size; peak = monotonic high-water mark; throughputPerSec = observed events/sec over a 10s sliding ArrayDeque window of event tsNanos (0 when <2 events — honest about poll limits, Pitfall 2); dispatcherUtilization = Map<dispatcherName,activeCount> grouped on event.scopeId (the 08-01 dispatcher-derived field, NOT ThreadAssigned/getThreadActivity which is empty for DebugProbes — Pitfall 1); leaks = active coroutines older than the threshold (now - createdAtNanos), carrying id/label/aliveMs (D-07). Active-set-only tracking (drop on terminal) bounds memory (T-08-06). MetricsSnapshot/LeakInfo are PLAIN core types (no @Serializable, like ProjectionService's HierarchyNode); the route maps them to @Serializable MetricsResponse/LeakDto. VizSession gained `val metricsProjection` + a metricsProjection.rebuildFrom(events) call in rehydrateFromStore (Pitfall 5 — proven: a DB-rehydrated scoped session returns non-empty metrics over the route). New tenant-scoped GET /api/sessions/{id}/metrics copies the /hierarchy shape: resolveScopedSession → 404 (not 403, no existence leak) cross-tenant (T-08-04/Pitfall 3); leakThresholdMs validated via toLongOrNull()?.coerceIn(MIN_LEAK_MS=1s, MAX_LEAK_MS=1h) ?: DEFAULT_LEAK_MS=30s (T-08-05/V5), resolved threshold echoed for the FE tooltip. Full backend gate (ktlintCheck detekt + both module test suites) green under JDK 21. Note: backend Gradle root project IS named `backend` so the verify task is `:test` not `:backend:test`; MetricsRouteTest needed @Suppress(TooManyFunctions) (routes/ is not in detekt excludes).
- [Phase 8, Plan 04]: CR-01 gap closure — persistence-safe leak age (RCO-07 SC#2). The /metrics route serves the DB-rehydrated path (ExposedSessionStore.buildSession → rehydrateFromStore → metricsProjection.rebuildFrom over events persisted by a PRIOR process), where the old leak-age subtraction of two System.nanoTime() values from different JVM lifetimes was undefined → garbage aliveMs, missed/spurious leaks (the same-process tests passed only because they injected a synthetic nowNanos matched to tsNanos and never crossed a JVM boundary). Fix: a WALL-CLOCK basis — CoroutineCreated.createdAtEpochMs (= System.currentTimeMillis() at construction via EventContext.epochMillis()), added as the LAST defaulted (0L) constructor param so all positional/named call sites + persisted rows stay compatible with NO schema migration (it round-trips through the JSON payload column; legacy rows decode to 0L). MetricsProjection.snapshot(nowEpochMs, leakThresholdMs, nowNanos=System.nanoTime()) computes aliveMs = nowEpochMs - createdAtEpochMs in millis (no NANOS division) and never reports a non-positive age (guards skew + the 0L default); the route passes System.currentTimeMillis(). Proven by MetricsLeakAgePersistenceTest crossing a real two-store H2-file restart boundary, reading with the route's real clock (NOT a synthetic tsNanos-matched clock) — RED against nanoTime, GREEN after. Two warnings folded in as a separate REFACTOR task: WR-01 (rebuildFrom now clears+replays under ONE lock via a lock-held applyLocked helper — a live event can no longer interleave) and WR-04 (throughput evicted against the nowNanos read clock and divided by the elapsed span, so an idle session decays to ~0/s instead of a stale non-zero rate). Two-clock split (leak age=wall-clock epoch; throughput=per-process nanoTime) documented in the MetricsProjection class doc so it is not collapsed back together. snapshot gained a defaulted nowNanos param so throughput is testable; production route call unchanged. Detekt LongMethod on the regression test fixed by extracting Boot1/Boot2 helpers. WR-02/WR-03/WR-05 left OUT of scope. Backend gate green under JDK 21. NOTE: backend Gradle root project is named `backend`, so the module test task is `:test` not `:backend:test` (plan's verify paths corrected, Rule 3). Truth #7 live-demo human UAT (SC#1 named hierarchical tree + SC#2 populated metrics/amber leak list; + post-restart leak-age repro now expected to show sane positive ages) remains deferred to /gsd-verify-work.
- [Phase 08.5, Plan 01]: Live real-app view → Surface-001 IDE-dock (FE-ALIGN, sketch winner C + B's tiles). Closed the first sketch-vs-shipped divergence — un-buried Active/Peak/Throughput/Dispatcher-util metrics from the Threads tab into a dock header strip. NEW presentational LiveDockPanel (props sessionId/streamEnabled/readOnly/liveList:ReactNode/sourcePanel?:ReactNode) = rounded-xl bg-content1 border-t-2 border-primary min-h-[200px]; header flex row hosts LivePill + reused <SessionMetrics showLeaks={false}> (tiles-only); body grid-cols-[1fr_320px]: left column = liveList + a SINGLE inline LeakList ({metrics && metrics.leaks.length>0 && <LeakList leaks leakThresholdMs/>}, LeakList.tsx real signature, NOT sessionId/isLive), right column = sourcePanel ?? muted "Select a coroutine to view its source". Leak data from useSessionMetrics(sessionId, streamEnabled, !readOnly) sharing the ['session-metrics',sessionId] RQ key (React Query dedupes → one fetch shared with the strip's own internal call; SessionMetrics mounted ONCE, LeakList ONCE). PD-04a: the ONLY SessionMetrics.tsx edit is an additive optional showLeaks?:boolean (default true) wrapping the existing Potential-leaks Card in a showLeaks&&(...) guard — every existing mount omits the prop and is byte-equivalent. PD-01: dock is LIVE-ONLY via isLiveView=!replayActive&&!readOnly in SessionDetails; the Coroutines tab renders <LiveDockPanel liveList={the existing tree/graph + Show-completed/N-more controls}/> in live, or the standalone list in replay/shared (existing tabbed layout, no dock); REMOVED the partial L1 docked-metrics strip (~790–803) + the duplicate SessionMetrics; source-selection (onSelect/selectedNodeId) gated to isLiveView so replay/shared tree/graph nodes stay presentational (Pitfall 1 back-compat). PD-04: numeral kept text-lg font-semibold (no text-2xl); leaks warning/amber never danger. Literal Tailwind only (IN-12). No backend change, no new dep (lockfiles untouched), use-event-stream.ts untouched. Reachability test extended: (a) live render shows the dock metric strip + live list + a single inline "2 potential leaks" amber badge; (b) read-only shared render shows the tabs with NO dock metric strip/LivePill, no "Open source for …" affordance, and no protected getMetrics fetch. Gates: vitest full 59 files/492 tests (--no-file-parallelism; the initial parallel run's 2 worker-startup timeouts were the known environmental flakiness, not a regression), tsc --noEmit 0 errors, lint 0 errors (6 baseline warnings), build clean. Commits 70e3d8c, bf011c3. Plan 02 mounts the Surface-002 source panel into the dock's right slot.
- [Phase 08.5, Plan 03]: Connect/onboarding → Surface-003 (FE-ALIGN, ONB-01). Closed the third sketch-vs-shipped divergence — greenfield badged sessions-sidebar-as-home + 3-step connect wizard. PD-10 (LIVE/DEMO is CLIENT-derived, no backend field): NEW lib/session-kind.ts deriveSessionKind(session)→'live'|'demo' = demo iff sessionId carries the scenario- prefix the scenarios route mints (createSession.mutateAsync(`scenario-${name}`)), else live; unknown→live so a live app never masquerades as a demo (T-085-08 accept — best-effort, a clean backend discriminator is a deferred phase). SessionRow reuses LivePill (LIVE) / neutral DEMO Chip + mono {N} active (the sketch's {host} is not wire-present in SessionInfo → degrades gracefully, Pitfall 3). SessionsSidebar (320px Card) partitions useSessions() into "Live apps"/"Demo scenarios" groups + primary + Connect; empty list folds the inline "No app connected" empty state (Connect-your-app / Run-a-demo CTAs) INTO the list, not a standalone screen (UI-SPEC 145). PD-11 (first-events signal): ConnectWizard mints ONE session via useCreateSession on open, captures the new id, and POLLS it via a LOCAL useQuery({queryKey:['sessions',newId], enabled:!!newId, refetchInterval:300}) — chose the local new-id-scoped query OVER extending the shipped refetchInterval-less useSession so every existing useSession call site stays byte-equivalent (zero regression surface); AUTO-navigates to /sessions/$sessionId the moment the polled coroutineCount transitions >0 (one-shot resolvedRef, scoped to the new id so it never resolves off a stale cache, Pitfall 6), with a Skip-to-live fallback so it is never a dead-end. 3 Snippet/Spinner steps are static public copy — appName="order-service" is a UI-SPEC constant rendered as React text (auto-escaped), no secret embedded (PD-14, T-085-06/07). PD-12: routes/sessions/index.tsx repointed to the SessionsSidebar-as-home + mounted wizard under Layout; the old flat-grid confirm()-based create UI dropped (create affordance is now + Connect). Two Rule-3 blocking gate fixes: (1) widened deriveSessionKind param Pick<SessionInfo,'sessionId'>→SessionInfo (excess-property typecheck on the test's full-object literals); (2) removed two eslint-disable react-hooks/exhaustive-deps directives — this project's flat ESLint config does NOT register react-hooks so they were "rule not found" ERRORS → replaced with plain comments. TDD both tasks (RED→GREEN): SessionsSidebar.test.tsx (+7) + ConnectWizard.test.tsx (+5, incl. the auto-resolve-on-0→N + new-id-scoping + refetchInterval assertions via captured useQuery options). Literal Tailwind only (IN-12); no backend change, no new dep (package.json/pnpm-lock.yaml untouched, T-085-SC). FE gates green: vitest full 61 files/504 tests (+12), tsc --noEmit 0 errors (no pnpm typecheck script — used npx tsc), lint 0 errors (5 baseline warnings, ≤6 budget), build clean. Commits 12aa5ca, e6dd4ae. Wave 2 (08.5-02) remains: mount the Surface-002 inline source panel into the LiveDockPanel right slot.
- [Phase 3, Plan 07]: Tenant-isolation gap-closure (CR-01/CR-02/AUTH-05). All five session sub-resource/SSE handlers now route through a shared `ApplicationCall.resolveScopedSession` helper (store!=null → getSession(id, resolveTenant()) → cross-tenant 404; else SessionManager.getSession D-04b fallback); SSE resolves PRE-stream (before connected frame/gauge/bus subscribe/replay) so a cross-tenant caller never opens a stream nor replays. Share routes enforce ownership: mint verifies session ownership via the scoped store (404 for non-owner), list/revoke use new `created_by`-scoped ShareService overloads (existing unscoped signatures kept for the public read path + ShareRoutesTest). New TenantIsolationE2ETest guards the invariant over the REAL routes + JWT auth (jwt provider mirrors Auth.kt incl. ?token= SSE fallback). Known limitation logged to deferred-items: a DB-rebuilt VizSession does NOT replay stored events into projectionService, so /timeline 404s even for the owner (hierarchy/threads return empty 200) — out of scope; isolation still proven via /events + SSE replay.

### Pending Todos

11 pending in `.planning/todos/pending/` — review with `/gsd-capture --list`. Includes 4 captured from the 2026-06-21 runtime audit: WR-02 admin cross-tenant share mint, share-UI silent no-op in memory mode, cors quoted-default config fragility, and DB-mode empty coroutine projection.

### Blockers/Concerns

Verified gaps from the 2026-06-11 codebase audit (Phase 1 addresses 1–3; auth in Phase 3; plugin in Phase 5):

1. **Duplicate `com.jh.proj.coroutineviz.session.*` fork** in `backend/src/main/` shadows the stronger `coroutine-viz-core` versions at runtime — ROOT CAUSE of 2 and 3. Fix: delete the fork, resolve against core. (Phase 1 / FND-01)
2. **Persistence (ADR-015) unimplemented** — runtime EventStore is an unbounded `CopyOnWriteArrayList`; no DB. Sessions lost on restart. (Bounded store: Phase 1 / FND-02; DB: Phase 3 / PERS-*)
3. **Perf-scaling (ADR-020) not wired** — bounded EventStore/EventSampler/RetentionPolicy exist in core but are unused by the backend; only 2 of 7 Micrometer gauges present. (Phase 1 / PROD-05, FND-02; sampling/batching: Phase 4)
4. **Auth (ADR-016) effectively off** — `Auth.kt` + `authenticatedApi()` exist but `Routing.kt` registers all routes without auth; every endpoint open. Write enforcement tests BEFORE wrapping routes. (Phase 3 / AUTH-*)
5. **IntelliJ plugin (ADR-010)** mostly built but `RunWithVisualizerAction.actionPerformed` is a TODO stub (javaagent launch missing); zero plugin tests. (Phase 5 / IDE-*)

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260621-rgi | Fix quoted DB_URL default in application.yaml so STORAGE_TYPE=database boots | 2026-06-21 | a23496c | [260621-rgi-fix-quoted-db-url-default-in-application](./quick/260621-rgi-fix-quoted-db-url-default-in-application/) |

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Business | Pricing/license model (competing PRD variants) | Pending resolution (V2 default) | 2026-06-11 |
| Business | Success-metric/KPI set (competing PRD variants) | Pending resolution (V2 default) | 2026-06-11 |
| Growth | Marketing site, theme/layout customization, onboarding tutorial | v2 | 2026-06-11 |
| SDK | Maven Central publication | v2 (after GitHub Packages) | 2026-06-11 |

## Session Continuity

Last session: 2026-06-27T17:07:00Z
Stopped at: Completed 08.5-03-PLAN.md (connect/onboarding → Surface-003; lib/session-kind.ts client LIVE/DEMO derivation + badged SessionsSidebar-as-home + SessionRow + 3-step ConnectWizard with new-id-scoped polled auto-resolve; routes/sessions/index.tsx repointed). Both Wave-1 plans (01 + 03) of Phase 08.5 done.
Resume file: .planning/phases/08.5-align-the-frontend-to-the-validated-sketch-winners-fe-alignm/08.5-03-SUMMARY.md — next: execute 08.5-02 (Wave 2 — mount the Surface-002 inline source panel into the LiveDockPanel right sourcePanel slot)
