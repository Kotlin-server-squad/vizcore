---
phase: 08.5-align-the-frontend-to-the-validated-sketch-winners-fe-alignm
plan: 02
subsystem: ui
tags: [react, heroui, tailwind, vitest, source-attribution, ide-dock, jump-to-code]

# Dependency graph
requires:
  - phase: 08.5-01
    provides: "LiveDockPanel — Surface-001 IDE-dock shell with a right-column sourcePanel?: ReactNode slot; live-only dock mount in SessionDetails"
  - phase: 08.1
    provides: "LOCKED copyFileLine jump-to-code + isUserFrame classifier + CoroutineSourceStack/Drawer source surface"
provides:
  - "CoroutineSourceStack — Surface-002 inline source panel: compact 'Created at'/'Suspended at' file:line srcchips by default, expand-to-full-stack (creation + suspension sections), text-sm headers, LOCKED copyFileLine preserved"
  - "SessionDetails live view hosts the source inline in the dock right-column slot; the live-view CoroutineSourceDrawer mount is retired (one selection, one mounted source body, one timeline fetch)"
affects: [08.5-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Surface-002 compact-chips → expand-to-full-stack: collapsed srcchip (font-mono text-xs text-primary bg-primary/10 px-2 py-0.5 rounded-md) default + useState expand toggle revealing the full frame-row sections"
    - "Inline-in-dock source hosting: pass a selection-gated ReactNode into LiveDockPanel's sourcePanel slot instead of mounting a separate right-side Drawer (no double-mount / double-fetch)"

key-files:
  created: []
  modified:
    - frontend/src/components/CoroutineSourceStack.tsx
    - frontend/src/components/CoroutineSourceStack.test.tsx
    - frontend/src/components/SessionDetails.tsx
    - frontend/src/components/SessionDetails.reachability.test.tsx

key-decisions:
  - "PD-05 (inline-in-dock, retire live Drawer): live-view source mounts in the LiveDockPanel sourcePanel slot; the CoroutineSourceDrawer mount is removed from SessionDetails so the timeline is fetched/mounted once. CoroutineSourceDrawer.tsx + its colocated test are left on disk as a thin reusable wrapper (discretion), no longer in the live path"
  - "PD-06 (jump-to-code LOCKED): copyFileLine + TimelineSourceRef copied verbatim, not re-authored; user frames font-semibold text-primary jump targets, library frames inert/dimmed"
  - "PD-07 (heading size fix): expanded section headers moved from text-lg font-semibold to text-sm font-semibold per the four-size contract; text-lg stays the metric numeral's sole owner"
  - "PD-08 (compact-chip token): collapsed chips use the literal srcchip token; expand is local useState, default collapsed"
  - "PD-09 (data path unchanged): useCoroutineTimeline/useSuspensionPoints stay enabled-guarded (disabled when coroutineId null); the started/created-event creation-frame derivation is unchanged"

patterns-established:
  - "Collapsed-default triage surface: compact file:line chips with a Jump affordance + a single 'Show full stack' expander, the same LOCKED jump-to-code firing from both the chip and the full frame rows"

requirements-completed: [FE-ALIGN]

# Metrics
duration: 8min
completed: 2026-06-27
---

# Phase 08.5 Plan 02: Source attribution → inline compact-chips → expand-to-full-stack Summary

**Reconciled Surface-002 source attribution from the right-side `Drawer` to the winning inline compact-chips → expand-to-full-stack model: `CoroutineSourceStack` now renders collapsed "Created at"/"Suspended at" `file:line` srcchips (each with a Jump affordance) by default and expands on click to the full creation + suspension stacks with `text-sm` headers, hosted inline in the dock's right-column `sourcePanel` slot while the live-view `CoroutineSourceDrawer` mount is retired — the LOCKED `copyFileLine` jump-to-code preserved verbatim.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-06-27T17:33:42Z
- **Completed:** 2026-06-27T17:42:00Z
- **Tasks:** 2
- **Files modified:** 4 (0 created, 4 modified)

## Accomplishments
- `CoroutineSourceStack` refactored to a collapsed compact-chips default + expand-to-full-stack toggle (`useState`, default collapsed):
  - **Collapsed (PD-08):** a "Created at" chip and a "Suspended at" chip, each a literal `srcchip` (`font-mono text-xs text-primary bg-primary/10 px-2 py-0.5 rounded-md`) wrapping `TimelineSourceRef` so the LOCKED copy+toast fires from the compact chip; a "Show full stack" expander button toggles `expanded`.
  - **Expanded (PD-07):** the existing two frame-row sections, renamed to the copy contract — "Created at — creation stack" / "Suspended at — last observed" — with both header classNames changed from `text-lg font-semibold` to `text-sm font-semibold`.
  - `SourceFrameRow`/`TimelineSourceRef`/`copyFileLine` and the `useCoroutineTimeline`/`useSuspensionPoints`/created-at derivation kept EXACTLY as shipped (PD-06/PD-09); loading/empty/error states preserved verbatim.
- `SessionDetails` (live branch) now passes the source panel into the `LiveDockPanel` `sourcePanel` slot — `selectedCoroutineId ? <CoroutineSourceStack …/> : <muted placeholder/>` — and the live-view `<CoroutineSourceDrawer …/>` mount is removed, so a live selection drives exactly one mounted source body and one enabled-guarded timeline fetch (Pitfall 5). The 08.2 live `onSelect`/`selectedNodeId` wiring is unchanged; replay/shared renders stay presentational (no dock, no source interactivity).
- Tests extended: `CoroutineSourceStack.test.tsx` (9 cases — collapsed chips by default, expand reveals the full stack, user-frame jump copies+toasts verbatim, library frame inert, headers `text-sm` not `text-lg`, query disabled when `coroutineId` null); `SessionDetails.reachability.test.tsx` updated to assert the inline-in-dock source (`UserService.kt:42` jump target, no `Coroutine source —` Drawer title, placeholder before selection) while preserving the replay/shared back-compat assertion.

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor CoroutineSourceStack to compact-chips → expand-to-full-stack + fix heading size** (TDD RED→GREEN) — `4e7e07a` (feat)
2. **Task 2: Mount the source panel inline in the dock right column + retire the live-view Drawer** — `68f1a54` (feat)

**Plan metadata:** (this commit) (docs: complete plan)

## Files Created/Modified
- `frontend/src/components/CoroutineSourceStack.tsx` — Collapsed compact-chips default (srcchip token + Jump) + "Show full stack" expander; expanded sections renamed and headers fixed to `text-sm font-semibold`; `copyFileLine`/`TimelineSourceRef`/data path verbatim.
- `frontend/src/components/CoroutineSourceStack.test.tsx` — Reworked to 9 cases covering collapsed chips, expand, verbatim jump-to-code, inert library frame, `text-sm` (not `text-lg`) headers, and the disabled-query guard.
- `frontend/src/components/SessionDetails.tsx` — Imports `CoroutineSourceStack` (dropped the `CoroutineSourceDrawer` import); live branch feeds the dock `sourcePanel` slot with a selection-gated source panel + muted placeholder; removed the live-view `CoroutineSourceDrawer` mount.
- `frontend/src/components/SessionDetails.reachability.test.tsx` — Asserts the inline-in-dock source (file:line jump target, no Drawer title, placeholder pre-selection); back-compat replay/shared assertion kept.

## Decisions Made
- **PD-05 (inline-in-dock, retire live Drawer):** source mounts in the dock slot; the live `CoroutineSourceDrawer` mount is gone — one selection, one mounted body, one fetch. `CoroutineSourceDrawer.tsx` and its colocated test remain on disk (thin reusable wrapper, no longer in the live path).
- **PD-06 (jump-to-code LOCKED):** `copyFileLine` + `TimelineSourceRef` preserved verbatim; user frames `font-semibold text-primary` jump targets, library frames inert/dimmed.
- **PD-07 (heading size):** expanded headers `text-sm font-semibold`; `text-lg` stays the metric numeral's sole owner.
- **PD-08 (compact-chip token):** literal `srcchip` token; expand is local `useState`, default collapsed.
- **PD-09 (data path):** `useCoroutineTimeline`/`useSuspensionPoints` enabled-guarded; created-at derivation (first started/created event's `suspensionPoint`) unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Narrowed `firstSuspension` under `noUncheckedIndexedAccess`**
- **Found during:** Task 2 (the `tsc --noEmit` gate)
- **Issue:** `const firstSuspension = suspensionPoints[0]` is typed `T | undefined` under the project's `noUncheckedIndexedAccess`; the `hasSuspensionFrames && firstSuspension.fileName` guard did not narrow the separately-bound variable, producing 4 TS18048 errors.
- **Fix:** Derived `firstSuspension = hasSuspensionFrames ? suspensionPoints[0] : undefined` and guarded the compact chip with `firstSuspension?.fileName`.
- **Files modified:** `frontend/src/components/CoroutineSourceStack.tsx`
- **Commit:** `68f1a54` (folded into Task 2, which is where the tsc gate ran)

## Issues Encountered
- The project has no `pnpm typecheck` script — used `npx tsc --noEmit` (0 errors), consistent with prior phases.
- Full `vitest run` was executed with `--no-file-parallelism` per the prior-wave note on environmental fork-worker startup flakiness; fully green serially.

## Gate Results
- `vitest run` (full, `--no-file-parallelism`): **61 files / 507 tests passed**
- `vitest run src/components/CoroutineSourceStack.test.tsx`: **9 passed**
- `vitest run SessionDetails.reachability + SessionDetails`: **32 passed**
- `npx tsc --noEmit`: **0 errors**
- `pnpm lint`: **0 errors, 5 warnings** (the accepted pre-existing baseline — none in touched files)
- `pnpm build` (tsc + vite): **clean** (pre-existing chunk-size advisory only)
- `git diff --exit-code frontend/package.json frontend/pnpm-lock.yaml`: **no dependency change**

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Surface-002 is now inline in the dock; Surface-001 (Plan 01) and Surface-003 (Plan 03, already landed) are complete.
- `CoroutineSourceDrawer.tsx` remains on disk as an unmounted reusable wrapper; a future cleanup may delete it if no consumer adopts it.
- No blockers.

## Self-Check: PASSED

- FOUND: frontend/src/components/CoroutineSourceStack.tsx
- FOUND: frontend/src/components/CoroutineSourceStack.test.tsx
- FOUND: frontend/src/components/SessionDetails.tsx
- FOUND: frontend/src/components/SessionDetails.reachability.test.tsx
- FOUND: commit 4e7e07a (Task 1)
- FOUND: commit 68f1a54 (Task 2)

---
*Phase: 08.5-align-the-frontend-to-the-validated-sketch-winners-fe-alignm*
*Completed: 2026-06-27*
