---
phase: 02-user-value-visualization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/projections/project-coroutines.ts
  - frontend/src/lib/projections/project-coroutines.test.ts
  - frontend/src/lib/projections/project-thread-activity.ts
  - frontend/src/lib/projections/project-thread-activity.test.ts
  - frontend/src/hooks/use-replay.ts
  - frontend/src/hooks/use-replay.test.ts
autonomous: true
requirements: [RPLY-01, RPLY-02]
must_haves:
  truths:
    - "projectCoroutines(events) produces the same CoroutineNode[] the server snapshot returns for the same session"
    - "projectThreadActivity(events) produces the Map<threadId, ThreadEvent[]> shape buildThreadLanes consumes"
    - "useReplay clamps inter-event delay to the ADR-017 50–2000ms base before dividing by speed"
    - "useReplay play/pause/step/seek/speed transitions update currentIndex and visibleEvents correctly"
  artifacts:
    - path: "frontend/src/lib/projections/project-coroutines.ts"
      provides: "VizEvent[] → CoroutineNode[] pure reducer (OQ-1)"
      exports: ["projectCoroutines"]
      min_lines: 40
    - path: "frontend/src/lib/projections/project-thread-activity.ts"
      provides: "VizEvent[] → ThreadActivity pure reducer (OQ-1)"
      exports: ["projectThreadActivity"]
    - path: "frontend/src/hooks/use-replay.test.ts"
      provides: "Playback/seek/speed/clamp coverage (Wave-0 test for use-replay)"
  key_links:
    - from: "frontend/src/lib/projections/project-coroutines.ts"
      to: "CoroutineNode shape consumed by buildCoroutineTree"
      via: "output type"
      pattern: "CoroutineNode"
    - from: "frontend/src/lib/projections/project-thread-activity.ts"
      to: "buildThreadLanes input"
      via: "ThreadActivity Map shape"
      pattern: "ThreadActivity|threadId"
---

<objective>
Build the client-side event→snapshot projection layer (OQ-1 — the single largest new work item in the phase) so replay-driven panels can derive their view-models from `visibleEvents` instead of server snapshots, and reconcile `useReplay`'s delay clamp to the ADR-017 50–2000ms base. Add the Wave-0 `use-replay` test that the rest of Wave 3 depends on.

Purpose: D-17 requires tree/graph/thread-lanes/timeline to replay from `visibleEvents`, but today only `EventsList` consumes events — the Coroutines graph reads `session.coroutines` (server snapshot) and the Threads tab reads a server projection. Without client-side reducers, those panels cannot reflect the replay cursor. This is the correctness-critical foundation for the replay mount (plan 02-07) and the recording pipeline (plan 02-08).
Output: Pure, heavily-tested `projectCoroutines` + `projectThreadActivity` reducers (validated against the server snapshot as oracle), and a delay-clamp fix + Wave-0 test for `useReplay`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/phases/02-user-value-visualization/02-VALIDATION.md
@docs/adr/017-replay-engine-design.md
@.claude/skills/typescript-advanced-types/SKILL.md
@frontend/src/lib/thread-lanes.ts
@frontend/src/types/api.ts
</context>

<artifacts_produced>
- `projectCoroutines(events: VizEvent[]): CoroutineNode[]` — folds coroutine lifecycle events into the CoroutineNode shape `buildCoroutineTree` consumes
- `projectThreadActivity(events: VizEvent[]): ThreadActivity` — folds ThreadAssigned events into `Map<threadId, ThreadEvent[]>`
- `useReplay` delay clamp reconciled to ADR-017 (base clamp 50–2000ms, then ÷ speed)
- `use-replay.test.ts` — Wave-0 playback/seek/speed/clamp coverage
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: projectCoroutines + projectThreadActivity reducers, tested against the server snapshot as oracle</name>
  <read_first>
    - frontend/src/lib/thread-lanes.ts (buildThreadLanes :37 — the pure events→viewmodel reducer pattern + ThreadActivity = Map<threadId, ThreadEvent[]>)
    - frontend/src/lib/thread-lanes.test.ts (the wire-shape fixture + deep-equal test convention)
    - frontend/src/types/api.ts:166 (CoroutineNode interface — the exact output shape, esp. id/parentId/state/label)
    - frontend/src/lib/utils.ts:82 (buildCoroutineTree — what CoroutineNode[] feeds into)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt and RuntimeSnapshot.kt (authoritative state machine: which events transition which coroutine state, parent/scope assignment, terminal classification — reimplement in TS, do not port literally)
    - frontend/src/types/api.ts (VizEvent kinds: CoroutineCreated/Started/Suspended/Resumed/Completed/Cancelled/Failed, ThreadAssigned)
  </read_first>
  <behavior>
    - Test: projectCoroutines(eventsFixture) deep-equals the CoroutineNode[] from the matching GET /api/sessions/{id} snapshot fixture (server snapshot as oracle) — same ids, parentId, terminal/non-terminal state, label.
    - Test: a coroutine with CoroutineFailed projects FAILED; a cancelled victim projects CANCELLED; a clean child projects COMPLETED (mirrors the Phase-1 FIX-03 terminal classification).
    - Test: projecting a prefix of events (replay cursor mid-session) yields the in-progress state (e.g. SUSPENDED) for a coroutine not yet completed.
    - Test: projectThreadActivity(eventsFixture) deep-equals the Map<threadId, ThreadEvent[]> that buildThreadLanes accepts for the same session (oracle from the threads-endpoint fixture).
  </behavior>
  <files>frontend/src/lib/projections/project-coroutines.ts, frontend/src/lib/projections/project-coroutines.test.ts, frontend/src/lib/projections/project-thread-activity.ts, frontend/src/lib/projections/project-thread-activity.test.ts</files>
  <action>
    Create `project-coroutines.ts` exporting a pure `projectCoroutines(events: VizEvent[]): CoroutineNode[]` (useMemo-friendly, no side effects) that folds coroutine lifecycle events into the exact `CoroutineNode` shape from `types/api.ts:166`. Reimplement the backend `RuntimeSnapshot`/`ProjectionService` state machine in TS: handle CoroutineCreated (new node, parentId/scope), Started/Suspended/Resumed (state transitions), and the terminal trio Completed/Cancelled/Failed (terminal state by event kind, matching Phase-1 FIX-03 — classify by cause/event type, never by message text). Apply ThreadAssigned to the node's current-thread field if CoroutineNode carries one. Sort/structure output identically to what the server returns so deep-equal holds.

    Create `project-thread-activity.ts` exporting `projectThreadActivity(events: VizEvent[]): ThreadActivity` that folds ThreadAssigned events into the `Map<threadId, ThreadEvent[]>` shape `buildThreadLanes` consumes (mirror the wire shape documented in thread-lanes.ts:3).

    Build the test fixtures from a real captured session: use a stored events array fixture as input and the matching server snapshot (coroutines list) / threads-map as the expected oracle. Mirror `thread-lanes.test.ts`'s fixture + deep-equal style. Use `@testing-library` / plain Vitest `expect(...).toEqual(...)`. No `any` (strict mode; per CLAUDE.md). These are the riskiest correctness tests in the phase — cover the terminal-state and mid-cursor cases explicitly.
  </action>
  <verify>
    <automated>cd frontend && pnpm test projections --run</automated>
  </verify>
  <acceptance_criteria>
    - `projectCoroutines` and `projectThreadActivity` are exported pure functions (source assertion).
    - Oracle deep-equal tests pass (test command green): projection output matches the server snapshot for the same session.
    - Terminal-state (FAILED/CANCELLED/COMPLETED) and mid-cursor in-progress cases covered and green.
    - `pnpm tsc --noEmit` clean (no `any`).
  </acceptance_criteria>
  <done>Both reducers exist, are pure, and deep-equal the server snapshot oracle for a real session including terminal and mid-cursor states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile useReplay delay clamp to ADR-017 and add the Wave-0 use-replay test</name>
  <read_first>
    - frontend/src/hooks/use-replay.ts (full — public API ReplayState/ReplayControls :11/:28, play :132, pause :141, stepForward :145, stepBack :150, seekTo :160, setSpeed :166, visibleEvents :177; the delay clamp at :97-98)
    - docs/adr/017-replay-engine-design.md (the 50–2000ms delay clamp + keyboard/speed contract)
    - .planning/phases/02-user-value-visualization/02-RESEARCH.md (Code Examples — the exact one-line clamp change)
    - existing frontend/src/hooks/*.test.ts (renderHook/act convention)
  </read_first>
  <behavior>
    - Test: with speed=1, an inter-event gap below 50ms is clamped UP to 50ms; a gap above 2000ms is clamped DOWN to 2000ms; an in-range gap is unchanged — then divided by speed.
    - Test: setSpeed(2) halves the effective delay vs speed=1 for the same gap.
    - Test: play advances currentIndex over time; pause stops it; stepForward/stepBack move exactly one index and clamp at bounds; seekTo(n) sets currentIndex=n and visibleEvents has length n+1.
    - Test: visibleEvents is the events[0..currentIndex] slice.
  </behavior>
  <files>frontend/src/hooks/use-replay.ts, frontend/src/hooks/use-replay.test.ts</files>
  <action>
    In `use-replay.ts`, replace the delay computation at :97-98 with the ADR-017-aligned clamp: clamp the BASE gap `(next.tsNanos - current.tsNanos) / 1_000_000` to `[50, 2000]` ms, THEN divide by `speedRef.current` (per the RESEARCH Code Example). Do NOT refactor the hook to a useReducer state machine — the existing useState impl satisfies every RPLY requirement (RESEARCH recommendation). Keep the entire public API (ReplayState/ReplayControls/UseReplayReturn) unchanged.

    Create `use-replay.test.ts` (Wave-0 dependency for plans 02-07/02-08): use `renderHook` + `act` + fake timers (`vi.useFakeTimers`) to cover the behaviors above. Mirror the existing hook-test convention. This is the test the VALIDATION.md Wave-0 list requires before the replay engine + controller work.
  </action>
  <verify>
    <automated>cd frontend && pnpm test use-replay --run</automated>
  </verify>
  <acceptance_criteria>
    - use-replay.ts clamps to [50,2000] before dividing by speed (source assertion: the clamp expression present at the delay site).
    - use-replay.test.ts covers clamp + speed + play/pause/step/seek/visibleEvents and is green (test command).
    - Public hook API unchanged (no signature edits to ReplayControls).
  </acceptance_criteria>
  <done>Delay clamp matches ADR-017; Wave-0 use-replay test exists and is green; hook API preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| events array → projected view-model | Untrusted-shaped event stream folded into UI state; pure functions, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering (correctness) | client projection diverging from server snapshot | mitigate | Oracle deep-equal tests against the server snapshot for the same session; terminal-state + mid-cursor cases covered |
| T-02-SC | Tampering | npm installs | accept | No new packages (pure TS reducers + existing Vitest); legitimacy audit: none flagged |
</threat_model>

<verification>
- `cd frontend && pnpm test projections use-replay --run` green.
- `cd frontend && pnpm tsc --noEmit` clean (strict, no `any`).
- projection output deep-equals the server snapshot oracle for a real captured session.
</verification>

<success_criteria>
- OQ-1 foundation delivered: event-derived panels can now reflect the replay cursor via client projections (consumed in plan 02-07).
- RPLY-02 delay clamp aligned to ADR-017; useReplay Wave-0 test green (unblocks Wave 3/4 replay work).
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-03-SUMMARY.md` when done
</output>
