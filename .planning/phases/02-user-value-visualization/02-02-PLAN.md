---
phase: 02-user-value-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/events/
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzer.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzerCoreTest.kt
autonomous: true
requirements: [CMPR-01]
must_haves:
  truths:
    - "Zero .kt files remain under backend/src/main/.../events/ and backend/src/main/.../checksystem/ (forks reconciled into core)"
    - "The TimingAnalyzer ns→ms conversion lives in coroutine-viz-core and a test proves it there"
    - "The backend compiles and the full backend test suite is green against core-only event/checksystem classes"
    - "No read/SSE/compare route calls getOrCreateSession (strict-404 on reads, D-12)"
  artifacts:
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt"
      provides: "Static guard extended to events/ and checksystem/"
      contains: "checksystem"
    - path: "backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzer.kt"
      provides: "ns→ms conversion in core (NANOS_PER_MILLI)"
      contains: "NANOS_PER_MILLI"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzerCoreTest.kt"
      provides: "Magnitude-sanity test asserting ms output from core"
  key_links:
    - from: "backend/src/test/.../ForkDeletionTest.kt"
      to: "backend/src/main/.../events and checksystem dirs"
      via: "directory .kt count assertion"
      pattern: "events|checksystem"
---

<objective>
Reconcile the `events/` and `checksystem/` FQCN forks under `backend/src/main/` into `coroutine-viz-core` (same hazard class FND-01 eliminated for `session/` and `wrappers/`), ensure the `TimingAnalyzer` ns→ms fix lives authoritatively in core, extend `ForkDeletionTest` to guard both packages, and audit that no read/SSE/compare route silently creates sessions (D-12 strict-404).

Purpose: Adverse fat-jar classloader ordering could silently undo the backend-only ns→ms fix (the conversion currently exists ONLY in `backend/src/main/.../checksystem/TimingAnalyzer.kt`, not in core). Forks on one classpath are a verified runtime hazard. This is a folded todo, coordinated with the Wave-1 compare/type-regen pass.
Output: Deleted forks resolving against core, the ns→ms fix landed in core with a proving test, an extended `ForkDeletionTest`, and a strict-404 audit confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/todos/pending/backend-events-package-fork.md
@.planning/todos/pending/wr-07-getorcreatesession-silent-substitution.md
@.planning/codebase/TESTING.md
@.claude/skills/kotlin-specialist/SKILL.md
</context>

<artifacts_produced>
- core `checksystem/TimingAnalyzer.kt`: gains `NANOS_PER_MILLI` ns→ms conversion (lifted from the backend copy)
- `ForkDeletionTest.kt`: two new guard tests — zero .kt under `events/` and zero under `checksystem/` in `backend/src/main`
- `TimingAnalyzerCoreTest.kt`: magnitude-sanity test (a multi-second span reports realistic ms, not 10^9-scale ns)
- Deleted: all .kt under `backend/src/main/.../events/**` and `backend/src/main/.../checksystem/**`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the ns→ms fix in core, then big-bang delete the events/ + checksystem/ forks</name>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzer.kt (the backend copy that HAS the fix — NANOS_PER_MILLI at :9, used at :64/:81/:96)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzer.kt (the core copy — verify whether the fix is present)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/events/ (fork inventory: channel/coroutine/deferred/dispatcher/flow/job subdirs + VizEvent.kt, DeadlockEvents.kt, MutexEvents.kt, SemaphoreEvents.kt, SuspensionPoint.kt, WaitingForChildren.kt)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/events/ (confirm core already has these packages)
    - .planning/phases/01-foundation-production-readiness/01-03-SUMMARY.md (the FND-01 big-bang delete pattern: same package name → no import changes)
  </read_first>
  <behavior>
    - Test: core TimingAnalyzer converts a known ns span to the expected ms value (e.g. 2_000_000_000 ns → 2000 ms via NANOS_PER_MILLI = 1_000_000L), asserted in coroutine-viz-core's test module (or backend test exercising the core class).
  </behavior>
  <files>backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzer.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/checksystem/TimingAnalyzerCoreTest.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/events/, backend/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/</files>
  <action>
    First, diff the backend `checksystem/TimingAnalyzer.kt` against the core copy. Port the ns→ms conversion (the `NANOS_PER_MILLI = 1_000_000L` constant and every `/ NANOS_PER_MILLI` division at the duration/suspension/total sites) into the CORE `TimingAnalyzer.kt` so the conversion is authoritative there. If any other backend-fork checksystem class diverged from its core twin, port the divergence into core the same way (compare file-by-file; the fix-bearing class is TimingAnalyzer per the SECURITY UF-01 note). Add `TimingAnalyzerCoreTest.kt` asserting the magnitude-sanity behavior above.

    Then delete the forks: remove ALL `.kt` files under `backend/src/main/kotlin/com/jh/proj/coroutineviz/events/**` and `backend/src/main/kotlin/com/jh/proj/coroutineviz/checksystem/**`. Because core uses the identical package names (`com.jh.proj.coroutineviz.events` / `.checksystem`), no import statements change — this mirrors the Phase-1 FND-01 big-bang delete. Compile; resolve any reference that pinned a backend-only symbol against the core equivalent. If a fork class has NO core twin (a genuinely backend-only class), do NOT delete it — flag it in the SUMMARY instead and leave it. Verify the core `events/` and `checksystem/` packages provide every symbol the backend references before deleting.

    Do NOT touch detekt/ktlint config; keep the same code style.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin test --tests "*TimingAnalyzer*" -x detekt -x ktlintMain</automated>
  </verify>
  <acceptance_criteria>
    - Core `TimingAnalyzer.kt` contains `NANOS_PER_MILLI` and uses it for conversion (source assertion: grep core file).
    - `backend/src/main/.../events/` and `.../checksystem/` contain zero `.kt` files (behavior assertion: `find ... -name '*.kt' | wc -l` == 0).
    - `TimingAnalyzerCoreTest` passes (test command green).
    - `compileKotlin` succeeds (backend resolves against core).
  </acceptance_criteria>
  <done>ns→ms fix authoritative in core with a proving test; both forks deleted; backend compiles against core-only classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ForkDeletionTest to guard events/ + checksystem/, and audit strict-404 read paths</name>
  <read_first>
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt (full — existing session/ + wrappers/ guard pattern, working-directory sanity anchor, DELETED_FORK_CLASS_FILES style)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt (read endpoints already 404 via getSession at :60,109,126,145,169,191)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/FlowScenarioRoutes.kt:86 (getOrCreateSession — confirm it is only reached by scenario-run/pattern explicit-create routes)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ (grep all callers of getOrCreateSession)
  </read_first>
  <behavior>
    - Test: zero .kt files exist under backend/src/main/.../events/ (and the directory is absent or empty), failing loudly with a file list if a fork class reappears — mirroring the existing session/wrappers guards.
    - Test: zero .kt files exist under backend/src/main/.../checksystem/, same guard shape.
  </behavior>
  <files>backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt</files>
  <action>
    Extend `ForkDeletionTest.kt`: add two new `@Test` methods (and the corresponding `EVENTS_FORK_DIR` / `CHECKSYSTEM_FORK_DIR` `File(...)` companion fields) that assert zero `.kt` files under `backend/src/main/.../events/` and `.../checksystem/`, using the same recursive/listing logic, the same loud-failure message style, and the existing working-directory sanity anchor. Because `events/` has subdirectories (channel/coroutine/...), walk recursively (`File.walkTopDown().filter { it.extension == "kt" }`) rather than a flat `listFiles`, and assert the count is 0. Keep the existing session/ and wrappers/ guards intact.

    Strict-404 audit (D-12): grep every caller of `getOrCreateSession` across `backend/src/main/.../routes/`. Confirm each caller is a scenario-run / pattern / explicit-create route (NOT a GET read, SSE stream, or compare lookup). If ANY read/SSE/compare route is found calling `getOrCreateSession`, switch it to `SessionManager.getSession(...)` + 404 (matching `SessionRoutes.kt:60`). Document the audit result (the list of getOrCreateSession callers and that each is create-path) in the SUMMARY. This resolves WR-07 across the backend, complementing the compare-route 404 in plan 02-01.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*ForkDeletionTest*" -x detekt -x ktlintMain && grep -rn "getOrCreateSession" backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ || true</automated>
  </verify>
  <acceptance_criteria>
    - `ForkDeletionTest` has guard tests naming `events` and `checksystem`, both green (source assertion + test command).
    - The grep output shows every `getOrCreateSession` caller is a create/scenario/pattern route (behavior assertion documented in SUMMARY); no read/SSE/compare route calls it.
  </acceptance_criteria>
  <done>ForkDeletionTest guards events/ + checksystem/ and is green; strict-404 read-path audit complete and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classpath (backend ↔ core) | Duplicate FQCN classes on one classpath; classloader ordering decides which wins |
| client → read/SSE/compare routes | Unknown session ids must not silently create state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | fork classloader ordering silently undoing the ns→ms fix | mitigate | Land the fix in core + delete the backend fork; ForkDeletionTest static guard fails CI on reintroduction |
| T-02-04 | Tampering/Integrity | getOrCreateSession silent-create on reads (WR-07) | mitigate | D-12 strict-404 audit; reads use getSession→404 |
| T-02-SC | Tampering | npm/gradle installs | accept | No new packages introduced (deletion + test only) |
</threat_model>

<verification>
- `cd backend && ./gradlew test` green (full suite) — no regression from the de-fork.
- `find backend/src/main/kotlin/com/jh/proj/coroutineviz/events backend/src/main/kotlin/com/jh/proj/coroutineviz/checksystem -name '*.kt' | wc -l` returns 0.
- core `TimingAnalyzer.kt` contains `NANOS_PER_MILLI`.
</verification>

<success_criteria>
- Folded fork-reconciliation todo closed: events/ + checksystem/ forks gone, ns→ms fix authoritative in core, ForkDeletionTest guards both.
- D-12 strict-404 audit complete across all read/SSE/compare routes.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-02-SUMMARY.md` when done
</output>
