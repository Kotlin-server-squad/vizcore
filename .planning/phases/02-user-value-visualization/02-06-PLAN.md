---
phase: 02-user-value-visualization
plan: 06
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/routes/compare/index.tsx
  - frontend/src/routes/compare/index.test.tsx
  - frontend/src/components/comparison/ComparisonView.tsx
  - frontend/src/components/comparison/SyncedTreePair.tsx
  - frontend/src/components/comparison/SyncedTreePair.test.tsx
  - frontend/src/components/Layout.tsx
autonomous: true
requirements: [CMPR-02]
must_haves:
  truths:
    - "A /compare route renders ComparisonView with A/B pickers seeded from ?a=&b= search params (shareable URL)"
    - "Changing a session picker updates the URL search params"
    - "A SyncedTreePair renders two coroutine trees with A-only (warning) / B-only (secondary) delta badges"
    - "Clicking a coroutine in one tree highlights its counterpart in the other (selection sync, matched by label then coroutineId)"
    - "A Compare nav item links to /compare from the Layout navbar"
    - "An unknown session id in ?a=/?b= shows a 'Session not found' state, not a phantom view (D-12)"
  artifacts:
    - path: "frontend/src/routes/compare/index.tsx"
      provides: "/compare TanStack route with ?a=&b= validateSearch"
      contains: "validateSearch"
    - path: "frontend/src/components/comparison/SyncedTreePair.tsx"
      provides: "Two synced trees + delta badges + selection sync (D-19/D-20)"
      exports: ["SyncedTreePair"]
    - path: "frontend/src/components/Layout.tsx"
      provides: "Compare nav item"
      contains: "/compare"
  key_links:
    - from: "frontend/src/routes/compare/index.tsx"
      to: "ComparisonView (?a=&b=)"
      via: "search-param seeded mount"
      pattern: "ComparisonView"
    - from: "frontend/src/components/comparison/SyncedTreePair.tsx"
      to: "selectedCoroutineId counterpart highlight"
      via: "lifted selection state matched by label/id"
      pattern: "selectedCoroutineId"
---

<objective>
Mount the existing `ComparisonView` on a dedicated `/compare` TanStack route with `?a=&b=` search params (D-10, shareable URLs), add a Compare nav link, and build the synchronized side-by-side coroutine-tree pair with delta badges and selection sync (D-11/D-19/D-20) below the existing diff dashboard — completing CMPR-02.

Purpose: CMPR-02 requires viewing two sessions side-by-side with delta highlights. `ComparisonView` (347 lines: pickers, summary delta cards, unique/common tables) exists but is unmounted and manages A/B selection with internal `useState`. This plan routes it, makes selection shareable, and adds the D-19 tree pair the success criterion's "side-by-side visualization" calls for.
Output: `routes/compare/index.tsx`, `SyncedTreePair.tsx`, the Compare nav item, and a search-param-seeded ComparisonView — with a session-not-found state for unknown ids (D-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/phases/02-user-value-visualization/02-UI-SPEC.md
@frontend/src/components/comparison/ComparisonView.tsx
@frontend/src/routes/__root.tsx
@frontend/src/components/Layout.tsx
@.claude/skills/heroui-react/SKILL.md
</context>

<artifacts_produced>
- `routes/compare/index.tsx`: `/compare` route, `validateSearch` for `a`/`b`, mounts `<ComparisonView>` seeded from search params; SessionNotFound state on 404
- `ComparisonView.tsx`: A/B selection lifted to accept controlled `a`/`b` + onChange that writes route search params (shareable); renders `<SyncedTreePair>` below the tables when comparison data is present
- `SyncedTreePair.tsx`: `grid grid-cols-2` two CoroutineTree cards, A-only `warning` / B-only `secondary` chips + ring outlines, lifted `selectedCoroutineId` counterpart highlight (match by label then coroutineId)
- `Layout.tsx`: "Compare" NavbarItem after Gallery (existing active-state Button pattern)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /compare route with ?a=&b= search params, ComparisonView controlled selection, Compare nav</name>
  <read_first>
    - frontend/src/routes/__root.tsx (createRootRouteWithContext, RouterContext, route conventions)
    - frontend/src/routes/gallery/ and scenarios/ (directory-based route file structure to mirror)
    - frontend/src/components/comparison/ComparisonView.tsx (full — internal useState sessionAId/sessionBId :31-32, Select onSelectionChange, useComparison hook, Spinner/empty/error states)
    - frontend/src/components/Layout.tsx:38-56 (NavbarItem + Link + isActive Button pattern for Gallery — copy for Compare)
    - frontend/src/components/EmptyState.tsx + ErrorAlert.tsx (session-not-found state surfaces)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §6 /compare route (page order, copy: "Compare Sessions" h1, session-not-found copy)
  </read_first>
  <behavior>
    - Test: navigating to /compare?a=A&b=B seeds both pickers with A and B (render with a router memory history + mocked useSessions/useComparison; assert selected keys).
    - Test: changing the Session A Select writes the new id into the route's ?a= search param (assert navigate/search update).
    - Test: an unknown id (useComparison returns a 404 error) renders the "Session not found" copy, not the results tables (D-12).
    - Test: the Layout renders a Compare nav item linking to /compare.
  </behavior>
  <files>frontend/src/routes/compare/index.tsx, frontend/src/routes/compare/index.test.tsx, frontend/src/components/comparison/ComparisonView.tsx, frontend/src/components/Layout.tsx</files>
  <action>
    Create `routes/compare/index.tsx` following the directory-based route pattern of `gallery/`/`scenarios/`: define the route with `validateSearch` parsing `a?: string` and `b?: string` from search params. Page layout per UI-SPEC §6: `container-custom py-8 space-y-6`, h1 "Compare Sessions" (`text-2xl font-bold`), then `<ComparisonView>`. Pass the search-param `a`/`b` down and an onChange that calls `navigate({ search: ... })` so the URL stays the source of truth (shareable).

    Refactor `ComparisonView.tsx` to accept controlled `a`/`b` props + `onChange` callbacks INSTEAD of its internal `useState` (lift selection to the route). Keep every existing Card/Select/Table/Spinner/empty/error surface and `data-testid`s verbatim — only the selection source changes. When `useComparison` returns a 404 (unknown id, D-12), render the UI-SPEC "Session not found" surface (ErrorAlert/EmptyState with the locked copy) in place of the results.

    Add the "Compare" NavbarItem to `Layout.tsx` after the Gallery item, copying the exact `Link to="/compare"` + `isActive` Button variant/color pattern; label "Compare".
  </action>
  <verify>
    <automated>cd frontend && pnpm test compare Layout --run && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /compare route exists with `validateSearch` for a/b (source assertion).
    - ?a=&b= seeds the pickers and Select changes update the URL (tests green).
    - Unknown id → "Session not found" surface, not results (test green, D-12).
    - Compare nav item links to /compare (test green).
    - `pnpm tsc --noEmit` clean.
  </acceptance_criteria>
  <done>/compare route mounts ComparisonView with shareable ?a=&b= selection, a session-not-found state, and a Compare nav link.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncedTreePair — two trees, delta badges, selection sync; render it below the diff tables</name>
  <read_first>
    - frontend/src/components/comparison/ComparisonView.tsx:234,248 (warning = A-only / secondary = B-only Chip colors to reuse; CommonCoroutines/UniqueCoroutines tables it renders above the pair)
    - frontend/src/components/CoroutineTree.tsx (the tree component each side renders; how it takes coroutines + how a node can receive a selected/ring style)
    - frontend/src/hooks/use-sessions.ts / use-session (fetch each session's snapshot — the compare response gives only id lists + per-coroutine state, NOT full trees, so each side needs GET /api/sessions/{a|b})
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §New SyncedTreePair (grid grid-cols-2 gap-4, CardHeader "Session A — {id}", ring-2 ring-primary selection, A only/B only chips + ring outlines, no scroll/zoom coupling)
  </read_first>
  <behavior>
    - Test: SyncedTreePair given two coroutine sets + a SessionComparison renders two trees; coroutines in coroutinesOnlyInA get an "A only" warning chip + ring-warning outline; coroutinesOnlyInB get "B only" secondary chip + ring-secondary; common nodes have no ring.
    - Test: clicking a node present in both trees applies ring-2 ring-primary to it AND its counterpart in the other tree (counterpart matched by label, falling back to coroutineId).
    - Test: clicking an A-only node (no counterpart in B) highlights only the A node without error.
  </behavior>
  <files>frontend/src/components/comparison/SyncedTreePair.tsx, frontend/src/components/comparison/SyncedTreePair.test.tsx</files>
  <action>
    Create `SyncedTreePair.tsx`: `grid grid-cols-2 gap-4`, two Cards each `CardHeader text-lg font-semibold` "Session A — {id}" / "Session B — {id}" (id in `font-mono text-sm text-default-500`), each rendering the existing `CoroutineTree` from that session's coroutines. Props: the two sessions' coroutine snapshots (fetched by the parent via useSession for `a`/`b`) and the `SessionComparison` result. Delta badges: A-only nodes (`comparison.coroutinesOnlyInA`) get `Chip size="sm" variant="flat" color="warning"` "A only" + `ring-1 ring-warning`; B-only (`coroutinesOnlyInB`) get `color="secondary"` "B only" + `ring-1 ring-secondary`; common nodes neutral. Selection sync: lift a `selectedCoroutineId` state in this component; clicking a node sets it and applies `ring-2 ring-primary rounded-md` to that node AND its counterpart in the other tree — match the counterpart by `label` then by `coroutineId` (Claude's-discretion rule from RESEARCH/CONTEXT). NO scroll/zoom coupling (D-20). All interactive nodes carry aria-label/data-testid.

    Wire it into `ComparisonView.tsx`: below the existing CommonCoroutines table, when comparison data is present, fetch each session's snapshot (useSession for `a`/`b`) and render `<SyncedTreePair>`. Reuse the existing Spinner/empty patterns while the snapshots load.
  </action>
  <verify>
    <automated>cd frontend && pnpm test SyncedTreePair compare --run</automated>
  </verify>
  <acceptance_criteria>
    - Two trees render with A-only warning / B-only secondary badges + ring outlines; common nodes neutral (test green).
    - Clicking a common node highlights both counterparts via ring-primary (test green, matched by label/id).
    - Clicking an A-only node highlights only the A side without error (test green).
    - SyncedTreePair rendered below the diff tables when comparison data present (source assertion in ComparisonView).
  </acceptance_criteria>
  <done>Synchronized tree pair with delta badges + selection sync renders below the diff dashboard, completing the CMPR-02 side-by-side view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search params → /compare | Untrusted `a`/`b` ids from a shared URL drive session fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | unknown/blank `a`/`b` in a shared compare URL | mitigate | `validateSearch` rejects malformed params; 404 from the backend (D-12) renders "Session not found", never a phantom view |
| T-02-11 | Information Disclosure | a===b self-compare | accept | Harmless (compares a session to itself); existing empty-state copy guides to pick two different sessions |
| T-02-SC | Tampering | npm installs | accept | No new packages (existing HeroUI + TanStack Router); legitimacy audit: none flagged |
</threat_model>

<verification>
- `cd frontend && pnpm test compare SyncedTreePair Layout --run` green.
- `cd frontend && pnpm tsc --noEmit` clean.
- Manual: `/compare?a=<id>&b=<id>` reproduces a comparison (shareable URL).
</verification>

<success_criteria>
- CMPR-02: two sessions viewable side-by-side with delta highlights — diff dashboard + synced tree pair on a shareable `/compare?a=&b=` route.
- D-10/D-11/D-19/D-20 satisfied; D-12 session-not-found state present.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-06-SUMMARY.md` when done
</output>
