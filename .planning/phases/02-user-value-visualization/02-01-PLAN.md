---
phase: 02-user-value-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutes.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ComparisonService.kt
  - backend/src/main/resources/openapi/documentation.yaml
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutesTest.kt
  - shared/api-types/openapi.json
  - shared/api-types/generated.ts
  - frontend/src/lib/api-client.ts
  - frontend/src/types/api.ts
autonomous: true
requirements: [CMPR-01]
must_haves:
  truths:
    - "GET /api/sessions/compare?a=&b= returns 200 with event-count, duration, and thread-utilization deltas"
    - "GET /api/sessions/compare with an unknown session id returns 404 (not a silent create)"
    - "The old GET /api/compare?sessionA=&sessionB= path no longer exists"
    - "frontend compareSessions() calls the new path and the regenerated SessionComparison type carries the thread metric"
  artifacts:
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutes.kt"
      provides: "Renamed compare route at /api/sessions/compare with a/b params"
      contains: "/api/sessions/compare"
    - path: "backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ComparisonService.kt"
      provides: "SessionComparison with thread-utilization delta field"
      contains: "distinctThreadsDiff"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutesTest.kt"
      provides: "Integration test for new path + thread delta + 404"
      contains: "/api/sessions/compare"
    - path: "shared/api-types/generated.ts"
      provides: "Regenerated TS types from the updated OpenAPI spec"
  key_links:
    - from: "frontend/src/lib/api-client.ts"
      to: "/api/sessions/compare"
      via: "fetchJson"
      pattern: "sessions/compare\\?a="
    - from: "backend/.../routes/ComparisonRoutes.kt"
      to: "ComparisonService.compare"
      via: "service call"
      pattern: "ComparisonService\\.compare"
---

<objective>
Rename the comparison endpoint to the roadmap-mandated `GET /api/sessions/compare?a=&b=` (D-09), add the missing thread-utilization delta to `SessionComparison` (CMPR-01 / OQ-3), document both in the OpenAPI spec, and regenerate `shared/api-types` so the frontend compiles against the new contract in one coordinated pass.

Purpose: CMPR-01 requires count, duration, AND thread-utilization deltas via the renamed path; today the route is at the old path and the response has no thread metric. This is real backend work, not just a rename.
Output: Renamed route, extended `SessionComparison`, updated OpenAPI YAML, synced `openapi.json`, regenerated `generated.ts`, updated `api-client.ts` + TS type, and a Ktor integration test covering the new path + thread delta + 404.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/phases/02-user-value-visualization/02-VALIDATION.md
@.planning/codebase/TESTING.md
@.claude/skills/kotlin-specialist/SKILL.md
</context>

<artifacts_produced>
- `ComparisonRoutes.kt`: route path `/api/sessions/compare`, query params `a` / `b`
- `SessionComparison`: new field `distinctThreadsDiff: Int` (thread-utilization delta)
- `ComparisonService.compare`: computes distinct-thread count per session from `ThreadAssigned` events / snapshot thread data
- `ComparisonRoutesTest.kt`: tests `compare returns 200 with thread delta`, `compare unknown id returns 404`, `compare missing param returns 400`
- `api-client.ts`: `compareSessions(a, b)` → `/sessions/compare?a=&b=`
- `api.ts`: `SessionComparison` TS type gains `distinctThreadsDiff`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thread-utilization delta to ComparisonService and rename the route</name>
  <read_first>
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ComparisonService.kt (full — SessionComparison data class :21, compute helpers, computeDurationNanos :119)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutes.kt (full — old path, BadRequest/NotFound handling)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/*Test.kt (one example for the Ktor testApplication convention)
    - .planning/codebase/TESTING.md (live-stream reads under withContext(Dispatchers.Default); JUnit 5 + Ktor Test Host)
  </read_first>
  <behavior>
    - Test: compare(sessionA, sessionB) returns a SessionComparison whose distinctThreadsDiff equals (distinct threads in B) − (distinct threads in A), where distinct threads are derived from each session's ThreadAssigned events (or snapshot thread data).
    - Test: two sessions using the same single thread yield distinctThreadsDiff == 0.
    - Test: existing fields (coroutineCountDiff, eventCountDiff, totalDurationDiffNanos, only-in-A/B, commonCoroutines) are unchanged.
  </behavior>
  <files>backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ComparisonService.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutes.kt</files>
  <action>
    In `ComparisonService.kt`: add a `distinctThreadsDiff: Int` field to the `SessionComparison` `@Serializable data class` (place it after `totalDurationDiffNanos`, with a KDoc `@property` line matching the existing doc style). Compute it inside `compare()` as `distinctThreads(sessionB) - distinctThreads(sessionA)`, where a new private helper `distinctThreads(session: VizSession): Int` returns the count of unique thread identifiers observed for that session. Derive distinct threads from the session's `ThreadAssigned` events via `session.store.all()` (mirror how `computeDurationNanos` consumes `store.all().map { it.tsNanos }`); if a thread-keyed snapshot is more authoritative, prefer `session.snapshot` thread data — pick whichever is already populated and unit-test the result. Match the existing `private fun` placement and naming. Do NOT alter the existing diff fields. This is the CMPR-01 / OQ-3 thread metric.

    In `ComparisonRoutes.kt`: change `get("/api/compare")` to `get("/api/sessions/compare")` and read query params `a` / `b` instead of `sessionA` / `sessionB` (per D-09). Keep the existing `BadRequest` on missing params and `NotFound` on `SessionManager.getSession(...) == null` (the strict-404 path satisfies D-12 for this route — do not introduce getOrCreate here). Update the error messages to reference `a`/`b`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*ComparisonService*" -x detekt -x ktlintMain</automated>
  </verify>
  <acceptance_criteria>
    - `SessionComparison` declares `distinctThreadsDiff: Int` (source assertion: grep `distinctThreadsDiff` in ComparisonService.kt).
    - `ComparisonRoutes.kt` registers `/api/sessions/compare` and reads `a`/`b` (source assertion: grep `/api/sessions/compare` and `queryParameters["a"]`).
    - ComparisonService unit test for the thread delta passes (test command above green).
  </acceptance_criteria>
  <done>Thread-utilization delta computed and serialized; route lives at the new path with a/b params; old path string is gone; ComparisonService tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ktor integration test, OpenAPI doc, api-types regen, frontend client + type update</name>
  <read_first>
    - backend/src/main/resources/openapi/documentation.yaml (existing compare path entry — hand-maintained YAML, Phase-1 convention)
    - shared/api-types/package.json (the `generate` script: openapi-typescript openapi.json -o generated.ts)
    - shared/api-types/openapi.json (local copy that feeds generation)
    - frontend/src/lib/api-client.ts:134 (compareSessions URL via fetchJson)
    - frontend/src/types/api.ts (SessionComparison TS type, near CoroutineNode :166)
    - .planning/todos/pending/shared-api-types-regeneration.md (the sync + regen procedure)
  </read_first>
  <behavior>
    - Test (Ktor testApplication): GET /api/sessions/compare?a={A}&b={B} returns 200 and the JSON body contains distinctThreadsDiff plus the existing diff fields.
    - Test: GET /api/sessions/compare?a={A}&b=does-not-exist returns 404.
    - Test: GET /api/sessions/compare?a={A} (missing b) returns 400.
  </behavior>
  <files>backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ComparisonRoutesTest.kt, backend/src/main/resources/openapi/documentation.yaml, shared/api-types/openapi.json, shared/api-types/generated.ts, frontend/src/lib/api-client.ts, frontend/src/types/api.ts</files>
  <action>
    Create `ComparisonRoutesTest.kt` (mirror an existing `routes/*Test.kt` Ktor `testApplication` test): seed two sessions via the explicit creation/scenario endpoints, then assert the three behaviors above. If a live SSE read is involved anywhere, run it under `withContext(Dispatchers.Default)` per the MetricsWiringTest convention noted in TESTING.md. Name tests descriptively (backtick style).

    Update `documentation.yaml`: rename the compare path to `/api/sessions/compare`, change the query params to `a`/`b` with descriptions, and add the `distinctThreadsDiff` property to the `SessionComparison` schema with a description. Keep the hand-maintained YAML style.

    Sync `shared/api-types/openapi.json` with the updated spec (copy the relevant compare path + SessionComparison schema changes), then run the generation script (`pnpm --filter @<scope>/api-types generate` or the exact script name in `shared/api-types/package.json`) to rewrite `generated.ts`. Resolve the scope/script name from package.json — do not guess.

    Update `frontend/src/lib/api-client.ts` `compareSessions(a, b)` to call `/sessions/compare?a=${encodeURIComponent(a)}&b=${encodeURIComponent(b)}` (keep the `fetchJson<SessionComparison>` idiom). Update the `SessionComparison` TS type in `frontend/src/types/api.ts` to include `distinctThreadsDiff: number`. Update any callers that referenced the old param names (`sessionA`/`sessionB`) — grep for `compareSessions(` to find them.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*Comparison*" -x detekt -x ktlintMain && cd ../frontend && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Ktor test asserts 200+thread-delta, 404 on unknown id, 400 on missing param (test command green).
    - `documentation.yaml` contains `/api/sessions/compare` and `distinctThreadsDiff` (source assertion).
    - `generated.ts` regenerated and references the new path/field; `api-client.ts` calls `sessions/compare?a=` (source assertion).
    - `pnpm tsc --noEmit` passes (no stale-contract type errors).
  </acceptance_criteria>
  <done>New path + thread delta covered by a passing Ktor integration test; OpenAPI updated; api-types regenerated; frontend client/type aligned and type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/sessions/compare | Untrusted `a`/`b` query params cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | compare read against unknown session id | mitigate | `SessionManager.getSession(...) == null` → 404 (D-12 strict-404); never getOrCreate on a read |
| T-02-02 | Information Disclosure | missing/blank `a`/`b` params | mitigate | BadRequest 400 on null params; no partial compare leaked |
| T-02-SC | Tampering | npm/gradle installs | accept | No new packages introduced this plan (regen uses existing openapi-typescript dep; legitimacy audit: none flagged) |
</threat_model>

<verification>
- `cd backend && ./gradlew test --tests "*Comparison*"` green (rename + thread delta + 404 + 400).
- `cd frontend && pnpm tsc --noEmit` clean against the regenerated contract.
- grep confirms the old `/api/compare` path string is absent from `ComparisonRoutes.kt`.
</verification>

<success_criteria>
- CMPR-01 satisfied: `GET /api/sessions/compare?a=&b=` returns event-count, duration, and thread-utilization deltas.
- D-09 satisfied: route renamed, OpenAPI documented, api-types regenerated, frontend aligned.
- D-12 satisfied for the compare route: unknown id → 404.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-01-SUMMARY.md` when done
</output>
