---
phase: 02-user-value-visualization
plan: 07
type: execute
wave: 3
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - frontend/src/components/replay/ReplayController.tsx
  - frontend/src/components/replay/ReplayController.test.tsx
  - frontend/src/components/SessionDetails.tsx
  - frontend/src/components/SessionDetails.test.tsx
  - frontend/src/hooks/use-event-stream.ts
  - frontend/src/hooks/use-event-stream.test.ts
  - frontend/src/components/replay/LiveDataNotice.tsx
autonomous: true
requirements: [RPLY-01, RPLY-02, RPLY-03]
must_haves:
  truths:
    - "A developer can play/pause/stop/step-forward/step-back, scrub, and choose 0.5x–5x speed from a sticky ReplayController, with every event-derived panel reflecting the current event index"
    - "ADR-017 keyboard shortcuts (Space/←/→/Home/End/1–4) drive the controller while replay is active and are suppressed in form fields"
    - "Entering replay starts at the end paused; SSE stays connected and live events buffer (no panel re-renders) with a '● N new events' badge; exiting jumps to live and applies buffered events (D-01..04)"
    - "Event-derived panels (tree/graph/thread-lanes/timeline/EventsList) render from visibleEvents via the client projections; projection-backed tabs show the 'live data — not replayed' notice (D-17)"
    - "Scrub snaps instantly; play/step animates per ADR-011/027; future events are dimmed (D-16/D-18)"
    - "use-event-stream's query invalidation is gated while replay is active without tearing down the EventSource or breaking Phase-1 dedup/backoff/max-wait machinery"
  artifacts:
    - path: "frontend/src/components/replay/ReplayController.tsx"
      provides: "Stop + FastForward + keyboard shortcuts + recording-state slot, sticky-bar ready"
      contains: "FiSquare"
    - path: "frontend/src/components/replay/LiveDataNotice.tsx"
      provides: "D-17 'live data — not replayed' banner"
      exports: ["LiveDataNotice"]
    - path: "frontend/src/components/SessionDetails.tsx"
      provides: "Replay toggle, sticky controller, REPLAY chip + new-events badge, ExportMenu, replay-vs-live panel data source"
      contains: "ReplayController"
  key_links:
    - from: "frontend/src/components/SessionDetails.tsx"
      to: "useReplay + projectCoroutines/projectThreadActivity"
      via: "replay panel data source"
      pattern: "useReplay|projectCoroutines"
    - from: "frontend/src/hooks/use-event-stream.ts"
      to: "gated invalidateQueries"
      via: "replayActive flag"
      pattern: "replayActive|paused"
---

<objective>
Mount and complete the replay experience: extend `ReplayController` (Stop, FastForward, keyboard shortcuts, a recording-state slot), mount it as a sticky bar in `SessionDetails` with the replay toggle / REPLAY chip / new-events badge / ExportMenu, switch event-derived panels to `visibleEvents` (using the plan-02-03 client projections for the graph/threads), gate SSE query invalidation while replay is active (D-02) without breaking Phase-1 SSE hardening, and implement snap-on-scrub / animate-on-play / dim-future (D-16/D-18). This delivers RPLY-01/02/03.

Purpose: All the replay pieces exist but are unmounted (`ReplayController`, `useReplay`, `EventHighlight`, `use-replay-motion`); the projections (02-03) and toast (02-04) and ExportMenu (02-05) are now ready. This is the integration that turns them into the user-visible time-travel feature.
Output: A completed, mounted ReplayController; SessionDetails wired for replay-vs-live with the D-15 signals and ExportMenu; a gated `use-event-stream`; and the LiveDataNotice for projection-backed tabs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/phases/02-user-value-visualization/02-UI-SPEC.md
@.planning/phases/02-user-value-visualization/02-VALIDATION.md
@docs/adr/017-replay-engine-design.md
@docs/adr/011-animation-system-design.md
@docs/adr/027-animation-performance-replay.md
@frontend/src/hooks/use-replay.ts
@frontend/src/components/replay/ReplayController.tsx
@frontend/src/components/SessionDetails.tsx
@frontend/src/hooks/use-event-stream.ts
@.claude/skills/heroui-react/SKILL.md
</context>

<artifacts_produced>
- `ReplayController.tsx`: Stop (FiSquare, seek-0+paused), FastForward (seek-last), keyboard shortcuts (Space/←/→/Home/End/1–4 with form-field suppression), a recording-state cluster slot (red dot + elapsed + Stop, props-driven; pipeline wired in 02-08), sticky-bar wrapper
- `LiveDataNotice.tsx`: D-17 banner shown on projection-backed tabs during replay
- `SessionDetails.tsx`: replay toggle (D-01), sticky controller (D-13), REPLAY chip + clickable new-events badge (D-15/D-04), ExportMenu in the toolbar, panel data source = visibleEvents + projectCoroutines/projectThreadActivity in replay (D-17), dim-future + scrub-snap (D-16/D-18)
- `use-event-stream.ts`: a `replayActive`/`paused` flag gating ONLY the invalidation/refetch effect
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete ReplayController (Stop, FastForward, keyboard, recording slot) + LiveDataNotice</name>
  <read_first>
    - frontend/src/components/replay/ReplayController.tsx (full — existing ButtonGroup :75-135, Slider :53, speed Dropdown :116-135, Fi* icons, aria-labels, data-testid)
    - frontend/src/hooks/use-replay.ts (the controls it drives: play/pause/stepForward/stepBack/seekTo/setSpeed, currentIndex, total)
    - docs/adr/017-replay-engine-design.md (locked keyboard set: Space play/pause, ←/→ step, Home rewind, End jump-to-end, 1–4 speed 0.5/1/2/5x)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §1 + §Interaction (Stop = seek-0+paused = FiSquare; recording cluster red dot + elapsed + Stop; controls disabled while recording; shortcut focus-guard; Space preventDefault; Slider focus wins)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §3 (LiveDataNotice classes + copy)
  </read_first>
  <behavior>
    - Test: Stop button (FiSquare) calls seekTo(0) and pause; FastForward calls seekTo(total-1).
    - Test: with replay active, pressing Space toggles play/pause; ArrowRight/Left step; Home seeks 0; End seeks last; keys 1–4 set speed 0.5/1/2/5x.
    - Test: keyboard handler is suppressed when focus is in input/textarea/select/[contenteditable]; Space calls preventDefault.
    - Test: when an `isRecording` prop is true, the recording cluster (red dot + elapsed + Stop) renders and the playback controls are disabled; when false, normal controls render.
    - Test: LiveDataNotice renders the locked copy with FiInfo and the default-100 banner classes.
  </behavior>
  <files>frontend/src/components/replay/ReplayController.tsx, frontend/src/components/replay/ReplayController.test.tsx, frontend/src/components/replay/LiveDataNotice.tsx</files>
  <action>
    Extend `ReplayController.tsx` keeping its existing Slider/ButtonGroup/Dropdown structure: add a Stop button (FiSquare, aria-label "Stop replay" → seekTo(0)+pause), a FastForward/jump-to-end button (FiSkipForward already present for step — add a distinct rewind-to-start FiRotateCcw if not present and an end-jump; reconcile against UI-SPEC §1's exact button list: Rewind FiRotateCcw, Step back FiSkipBack, Play/Pause primary, Step forward FiSkipForward, Stop FiSquare). Add keyboard shortcuts (ADR-017 set) bound via a `useEffect` keydown listener active ONLY while replay is active, suppressed when `document.activeElement` is input/textarea/select/[contenteditable]; Space `preventDefault`; when the Slider itself has focus let native Slider keys win. Add a props-driven recording-state cluster (`isRecording`, `elapsedMs`, `onStopRecording`): red pulsing dot `bg-danger animate-pulse`, elapsed `font-mono text-xs text-danger` m:ss, and a danger Stop button; while `isRecording`, set playback controls `isDisabled` (the 02-08 scripted pipeline drives them). The recording cluster lives in the controller chrome, never as a panel overlay (D-08/D-23). Keep every aria-label/data-testid.

    Create `LiveDataNotice.tsx` (D-17): `flex items-center gap-2 rounded-md bg-default-100 px-3 py-2 text-xs text-default-500` + FiInfo, copy: "Live data — this tab shows the current session state and is not affected by replay." Export it.
  </action>
  <verify>
    <automated>cd frontend && pnpm test ReplayController LiveDataNotice --run</automated>
  </verify>
  <acceptance_criteria>
    - Stop=seek0+pause, FastForward=seek last (test green).
    - Full ADR-017 keyboard set fires controls, suppressed in form fields, Space preventDefault (tests green).
    - isRecording prop renders the recording cluster + disables playback controls (test green).
    - LiveDataNotice renders locked copy + classes (test green).
  </acceptance_criteria>
  <done>ReplayController has Stop/FastForward/keyboard/recording-slot; LiveDataNotice exists — both unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate SSE invalidation during replay without breaking Phase-1 hardening</name>
  <read_first>
    - frontend/src/hooks/use-event-stream.ts (full — esp. the invalidation effect :225-260: seenSeqsRef dedup, bounded fatal-retry, INVALIDATION_DEBOUNCE_MS / INVALIDATION_MAX_WAIT_MS, the dual invalidate of ['sessions',id] and ['thread-activity',id])
    - frontend/src/hooks/use-event-stream.test.ts (existing dedup/backoff/zero-event/max-wait tests to preserve)
    - .planning/phases/02-user-value-visualization/02-RESEARCH.md (Pattern 3 + Pitfall 5/6: gate the invalidation effect ONLY, keep EventSource open, keep appending events for the badge, do NOT touch connect/retry/dedup)
  </read_first>
  <behavior>
    - Test: when the hook is called with replayActive/paused = true, events still append (the events array grows) but queryClient.invalidateQueries is NOT called for sessions/thread-activity.
    - Test: when paused flips back to false, a single invalidation flush occurs and the buffered events apply (D-04).
    - Test: the existing dedup (seenSeqsRef), bounded fatal-retry, and max-wait-capped debounce tests still pass unchanged (no regression).
  </behavior>
  <files>frontend/src/hooks/use-event-stream.ts, frontend/src/hooks/use-event-stream.test.ts</files>
  <action>
    Add a `replayActive` (or `paused`) boolean parameter/option to `useEventStream`. Gate ONLY the cache side effects: skip `queryClient.invalidateQueries({ queryKey: ['sessions', sessionId] })` and `['thread-activity', sessionId]` (and the SessionDetails refetch counterpart, handled in Task 3) while the flag is true. Keep the EventSource open, keep appending to `events` (the "● N new events" badge counts these), and keep `seenSeqsRef` dedup, the bounded fatal-retry, and the max-wait-capped debounce EXACTLY as-is (Pitfall 6 — do not refactor connect/retry/dedup). On the flag returning to false, perform one invalidation flush so buffered events apply (D-04). Extend the existing test file with the gate behaviors above; do NOT weaken the existing Phase-1 tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm test use-event-stream --run</automated>
  </verify>
  <acceptance_criteria>
    - replayActive=true → events append, no invalidation (test green).
    - paused→false → single flush applies buffered events (test green).
    - All pre-existing dedup/backoff/max-wait/zero-event tests still pass (test command green, no skips).
  </acceptance_criteria>
  <done>Invalidation gated behind the replay flag with EventSource kept open and Phase-1 SSE hardening intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount replay in SessionDetails — toggle, sticky controller, signals, ExportMenu, projected panels, dim/scrub</name>
  <read_first>
    - frontend/src/components/SessionDetails.tsx (full — allEvents data source :70, scenarioState/jobStates useMemos, the debounced refetch :101-138, the header chip cluster, the Tabs :389 incl. Coroutines graph/list :393+, projection-backed tab calls)
    - frontend/src/hooks/use-replay.ts (useReplay(events) → visibleEvents/currentIndex/controls)
    - frontend/src/lib/projections/project-coroutines.ts + project-thread-activity.ts (plan 02-03 — replay snapshot derivation)
    - frontend/src/components/replay/ReplayController.tsx + EventHighlight.tsx + use-replay-motion.ts (mount targets; EventHighlight for EventsList current-event; futureOpacity from useReplayMotion)
    - frontend/src/components/export/ExportMenu.tsx (plan 02-05 — toolbar mount; pass active panel ref + events + session)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §1/§2/§3 (sticky top-16 z-30 wrapper; REPLAY chip primary solid; new-events badge warning dot clickable; replay toggle button; LiveDataNotice on projection tabs; dim future opacity 0.3; timeline playhead; EventsList auto-scroll + manual-scroll-pauses-follow)
  </read_first>
  <behavior>
    - Test: clicking the Replay toggle enters replay, seeks to the end, and pauses (D-03); a REPLAY chip appears; the controller renders sticky above the tabs.
    - Test: in replay, the Coroutines panel renders from projectCoroutines(visibleEvents) (not session.coroutines) and EventsList renders visibleEvents; stepping forward advances which events/coroutines are shown.
    - Test: projection-backed tabs (e.g. Flow/Jobs/Validation) render LiveDataNotice while replay is active.
    - Test: live events arriving during replay increment the "● N new events" badge without re-rendering the frozen panels; clicking the badge exits replay, applies buffered events, and forgets the cursor (D-04).
    - Test: scrubbing the slider re-renders panels at the target index without animation (snap, D-16); play/step keeps animation.
    - Test: future events (index > cursor) are dimmed (opacity 0.3) and still rendered (D-18).
    - Test: the ExportMenu is present in the toolbar.
  </behavior>
  <files>frontend/src/components/SessionDetails.tsx, frontend/src/components/SessionDetails.test.tsx</files>
  <action>
    Wire `SessionDetails.tsx` per RESEARCH Pattern 1 and the UI-SPEC: add `replayActive` state and `useReplay(allEvents)` over a frozen snapshot taken at replay entry (D-01); on entering, `seekTo(total-1)` + ensure paused (D-03). Compute `panelEvents = replayActive ? replay.visibleEvents : allEvents` and `panelCoroutines = replayActive ? projectCoroutines(replay.visibleEvents) : session.coroutines`; feed the Coroutines tab (graph + list), EventsList, timeline, and thread-lanes from these (use projectThreadActivity for threads). Render `<ReplayController>` in a `sticky top-16 z-30` wrapper directly above `<Tabs>` (:389). Add the replay toggle button (D-01, always visible — "Replay"/"Exit Replay" per UI-SPEC §2), the REPLAY chip (`primary` solid) and the clickable "● N new events" badge (warning dot; count = live events appended since replay entry; click → exit replay + apply buffered events + forget cursor, D-04) in the header chip cluster. Pass `replayActive` into `useEventStream` (Task 2 gate) AND gate the local debounced `refetch` (:101-138) behind `!replayActive`. Mount `<ExportMenu>` in the toolbar, passing the active visualization panel ref, the session id/name, and the normalized events (for JSON). Apply dim-future via `futureOpacity` from `useReplayMotion` (opacity 0.3 past the cursor, D-18) and the timeline playhead line; scrub = snap (no animation), play/step = framer-motion variants at the active speed (D-16/RPLY-03). EventsList: auto-scroll + EventHighlight on the current event during play/step, manual scroll pauses follow until the next play/step (D-14). Show LiveDataNotice at the top of each projection-backed tab panel while replayActive (D-17). Preserve every existing scenarioState/jobStates derivation and tab structure. No `any`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test SessionDetails --run && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Replay toggle enters-at-end-paused + REPLAY chip + sticky controller (test green, D-03/D-13/D-15).
    - Coroutines/EventsList render from projected visibleEvents in replay; stepping changes output (test green, D-17).
    - LiveDataNotice on projection tabs during replay (test green).
    - New-events badge increments without re-rendering frozen panels; badge click exits+applies+forgets (test green, D-02/D-04).
    - Scrub snaps, future dimmed; ExportMenu present (tests green).
    - `pnpm tsc --noEmit` clean.
  </acceptance_criteria>
  <done>SessionDetails delivers the full replay UX (RPLY-01/02/03) with D-01..18 signals, projected panels, gated SSE buffering, and the ExportMenu mounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → frozen replay view | Live events must not mutate the frozen replay panels while buffering |
| keyboard input → page | Replay shortcuts must not hijack typing or page scroll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering (UI integrity) | live SSE invalidation jittering the frozen replay view (D-02) | mitigate | Gate the invalidation/refetch effect behind replayActive; keep EventSource open; buffered events apply only on exit (D-04) |
| T-02-13 | Denial (UX) | replay shortcuts hijacking form input / page scroll | mitigate | Suppress keydown when focus is in input/textarea/select/[contenteditable]; Space preventDefault; Slider focus wins |
| T-02-14 | Tampering | breaking Phase-1 SSE dedup/backoff while adding the gate | mitigate | Gate only the cache side effect; leave seenSeqsRef/retry/max-wait untouched; existing tests guard regressions |
| T-02-SC | Tampering | npm installs | accept | No new packages (existing useReplay/framer-motion/HeroUI); legitimacy audit: none flagged |
</threat_model>

<verification>
- `cd frontend && pnpm test ReplayController LiveDataNotice use-event-stream SessionDetails --run` green.
- `cd frontend && pnpm tsc --noEmit` clean.
- Phase-1 SSE tests (dedup/backoff/max-wait/zero-event) remain green (no regression).
</verification>

<success_criteria>
- RPLY-01: play/pause/stop/step + scrub from the controller; event-derived panels reflect the current index (via projections).
- RPLY-02: 0.5x–5x speed + scrub; ADR-017 clamp (from 02-03).
- RPLY-03: animations respect replay speed and the current event (snap-on-scrub / animate-on-play, dim-future).
- D-01..04, D-13..18 satisfied; ExportMenu mounted for EXPT-01/02/D-22.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-07-SUMMARY.md` when done
</output>
