---
phase: 02-user-value-visualization
plan: 05
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - frontend/src/lib/export-png.ts
  - frontend/src/lib/export-png.test.ts
  - frontend/src/lib/export-svg.ts
  - frontend/src/lib/export-svg.test.ts
  - frontend/src/lib/export-json.ts
  - frontend/src/lib/export-json.test.ts
  - frontend/src/lib/export-filename.ts
  - frontend/src/components/export/ExportMenu.tsx
  - frontend/src/components/export/ExportMenu.test.tsx
autonomous: true
requirements: [EXPT-01, EXPT-02]
must_haves:
  truths:
    - "PNG export composites the ADR-018 info header (session name, timestamp, event count) before capture"
    - "SVG export inlines computed styles from an <svg> root and downloads image/svg+xml; the SVG option is hidden when the active panel has no <svg> root (D-21)"
    - "JSON export downloads the normalized event array as .json (D-22)"
    - "ExportMenu renders PNG/SVG(conditional)/JSON items and a disabled-with-tooltip Record item when MediaRecorder is unavailable"
  artifacts:
    - path: "frontend/src/lib/export-svg.ts"
      provides: "Standalone style-inlined SVG export (EXPT-02)"
      exports: ["exportToSvg", "findSvgRoot"]
    - path: "frontend/src/lib/export-json.ts"
      provides: "Normalized event-array JSON export (D-22)"
      exports: ["exportEventsToJson"]
    - path: "frontend/src/components/export/ExportMenu.tsx"
      provides: "ADR-018 export dropdown"
      exports: ["ExportMenu"]
  key_links:
    - from: "frontend/src/components/export/ExportMenu.tsx"
      to: "exportToPng / exportToSvg / exportEventsToJson"
      via: "menu item handlers"
      pattern: "exportTo(Png|Svg)|exportEventsToJson"
    - from: "frontend/src/lib/export-svg.ts"
      to: "<svg viewBox> panel root"
      via: "findSvgRoot auto-detect"
      pattern: "svg\\[viewBox\\]"
---

<objective>
Deliver the ADR-018 export surface: add the info header to the existing PNG export (D-08), build standalone style-inlined SVG export that auto-detects `<svg>`-native panels (EXPT-02 / D-21), build JSON event export (D-22), and compose them into the `ExportMenu` dropdown with success/error toasts. The Record-video item is rendered here but disabled-by-default (its pipeline lands in plan 02-08).

Purpose: EXPT-01 (PNG) and EXPT-02 (SVG) plus D-22 (JSON) are the shareable-artifact value. The "graph view" is HTML/CSS not `<svg>` (OQ-2), so SVG export attaches only where a genuine `<svg viewBox>` root exists (DeadlockVisualization/FlowParticlePath/SemaphoreGauge/JobStatusDisplay) and the coroutine graph falls back to PNG — exactly as ADR-018 permits.
Output: `export-png.ts` (header), new `export-svg.ts` + `export-json.ts` + `export-filename.ts`, and the `ExportMenu` component, all unit-tested with mocked browser APIs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-PATTERNS.md
@.planning/phases/02-user-value-visualization/02-UI-SPEC.md
@docs/adr/018-export-system-design.md
@frontend/src/lib/export-png.ts
@frontend/src/components/export/ExportButton.tsx
@.claude/skills/heroui-react/SKILL.md
</context>

<artifacts_produced>
- `export-png.ts`: info-header compositing (session name / timestamp / event count) before html2canvas capture (D-08)
- `export-svg.ts`: `findSvgRoot(panelEl)` (D-21 auto-detect) + `exportToSvg(svgEl, filename)` (whitelist computed-style inlining, XMLSerializer, Blob download)
- `export-json.ts`: `exportEventsToJson(events, filename)` (JSON.stringify + Blob download)
- `export-filename.ts`: `{sessionId}-{panel}-{yyyyMMdd-HHmm}.{ext}` builder (UI-SPEC locked naming)
- `ExportMenu.tsx`: HeroUI Dropdown — PNG / SVG (conditional) / JSON / divider / Record (disabled when unsupported), with toast feedback
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PNG info header, SVG export, JSON export, filename builder (the lib layer)</name>
  <read_first>
    - frontend/src/lib/export-png.ts (full — html2canvas opts scale 2 / bg #18181b / useCORS; Blob + object-URL + temp-anchor + revokeObjectURL finally idiom)
    - frontend/src/lib/export-png.test.ts (the vi.mock('html2canvas') + mocked URL.createObjectURL / document.createElement('a') pattern)
    - .planning/phases/02-user-value-visualization/02-RESEARCH.md (Pattern 4 SVG computed-style whitelist + Pattern, Code Examples: findSvgRoot detect)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §4 (file naming {sessionId}-{panel}-{yyyyMMdd-HHmm}.{ext})
    - docs/adr/018-export-system-design.md (info-header content; SVG metadata comment)
  </read_first>
  <behavior>
    - Test (export-png, extend): exportToPng composites a header band with session name + timestamp + event count before capture (assert the header element is created/added prior to html2canvas, or that the composited node contains the three strings).
    - Test (export-svg): exportToSvg deep-clones the svg, inlines the whitelisted computed-style props (fill/stroke/stroke-width/stroke-dasharray/color/opacity/font-family/font-size/font-weight/transform) inline on elements, sets xmlns + width/height, and triggers a Blob download with type image/svg+xml.
    - Test (export-svg): findSvgRoot returns the panel el if it matches svg, else querySelector('svg[viewBox], svg[width]'), else null.
    - Test (export-json): exportEventsToJson stringifies the events array and downloads a .json blob (type application/json).
    - Test (export-filename): buildExportFilename('abc','tree','png', date) returns 'abc-tree-20260612-1430.png' for a fixed date.
  </behavior>
  <files>frontend/src/lib/export-png.ts, frontend/src/lib/export-png.test.ts, frontend/src/lib/export-svg.ts, frontend/src/lib/export-svg.test.ts, frontend/src/lib/export-json.ts, frontend/src/lib/export-json.test.ts, frontend/src/lib/export-filename.ts</files>
  <action>
    Extend `export-png.ts`: before the html2canvas call, composite an ADR-018 info header (session name, ISO timestamp, event count) onto the captured region — either by temporarily prepending a styled header node to the cloned/captured element or by drawing onto the resulting canvas. Keep the existing html2canvas opts (scale 2, bg #18181b, useCORS) and the object-URL download idiom. Accept the header data via new params (e.g. `{ sessionName, eventCount }`) — keep the existing call sites working by making header data optional. Extend `export-png.test.ts` to assert the header content is present.

    Create `export-svg.ts` exporting `findSvgRoot(el: HTMLElement): SVGSVGElement | null` (D-21 auto-detect: `el.matches('svg') ? el : el.querySelector('svg[viewBox], svg[width]')`) and `exportToSvg(svg: SVGSVGElement, filename?: string)` implementing Pattern 4: deep-clone, walk every element writing the WHITELIST of computed-style props inline (fill, stroke, stroke-width, stroke-dasharray, color, opacity, font-family, font-size, font-weight, transform — whitelist to avoid multi-MB bloat), set `xmlns="http://www.w3.org/2000/svg"` + explicit width/height from getBoundingClientRect, add an ADR-018 metadata comment, then `XMLSerializer().serializeToString` → `Blob([...], {type:'image/svg+xml'})` → object-URL download (reuse the export-png download idiom). Mirror `export-png.test.ts`'s mocking approach.

    Create `export-json.ts` exporting `exportEventsToJson(events, filename?)` — `JSON.stringify(events, null, 2)` → `Blob([...], {type:'application/json'})` → object-URL download (D-22 normalized event array).

    Create `export-filename.ts` exporting `buildExportFilename(sessionId, panel, ext, date = new Date())` returning `{sessionId}-{panel}-{yyyyMMdd-HHmm}.{ext}` (UI-SPEC locked naming; panel ∈ tree/graph/threads/timeline/events/replay). No `any`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test export-png export-svg export-json export-filename --run</automated>
  </verify>
  <acceptance_criteria>
    - export-png composites the info header (test green).
    - exportToSvg inlines whitelisted styles + downloads image/svg+xml; findSvgRoot returns null on non-svg panels (tests green).
    - exportEventsToJson downloads application/json (test green).
    - buildExportFilename matches the locked pattern (test green).
    - `pnpm tsc --noEmit` clean (no `any`).
  </acceptance_criteria>
  <done>PNG header, SVG export with auto-detect, JSON export, and the filename builder all exist and are unit-tested with mocked browser APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExportMenu dropdown wiring PNG/SVG(conditional)/JSON + disabled Record item, with toasts</name>
  <read_first>
    - frontend/src/components/export/ExportButton.tsx (size="sm" variant="flat", isLoading during export, FiCamera, aria-label, error try/finally)
    - frontend/src/components/replay/ReplayController.tsx:116-135 (HeroUI Dropdown/DropdownTrigger/DropdownMenu/DropdownItem idiom to mirror)
    - frontend/src/lib/toast.ts (toastSuccess/toastError from plan 02-04)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §New components ExportMenu (item order, icons FiImage/FiCode/FiFileText/FiVideo/FiDownload trigger; SVG item only when active panel root is <svg>; Video item disabled + Tooltip when MediaRecorder unavailable, D-25; success/error toast copy)
  </read_first>
  <behavior>
    - Test: ExportMenu renders items "Export view as PNG", "Export events as JSON", and "Record replay as video" always; "Export graph as SVG" ONLY when findSvgRoot(panel) is non-null (mock findSvgRoot).
    - Test: when MediaRecorder is undefined (deleted from globalThis in the test), the Record item is disabled and carries the unsupported tooltip; otherwise it is enabled.
    - Test: clicking PNG calls exportToPng then toastSuccess with the "Exported {filename}" copy; an export rejection calls toastError with the failure copy (mock the export libs + toast).
  </behavior>
  <files>frontend/src/components/export/ExportMenu.tsx, frontend/src/components/export/ExportMenu.test.tsx</files>
  <action>
    Create `ExportMenu.tsx`: HeroUI `Dropdown` (mirror ReplayController:116-135). Trigger `Button size="sm" variant="flat"` + FiDownload, label "Export". Props: a ref/getter for the active visualization panel element, the session id/name, the normalized events array, and the current view/panel key. Items top→bottom (UI-SPEC §New): "Export view as PNG" (FiImage), "Export graph as SVG" (FiCode — rendered ONLY when `findSvgRoot(panelEl)` is non-null, D-21/OQ-2), "Export events as JSON" (FiFileText), divider, "Record replay as video" (FiVideo). The Video item is DISABLED with the D-25 Tooltip "Video recording is not supported in this browser" when `typeof MediaRecorder === 'undefined'` OR no codec is supported; otherwise enabled but its onPress is a no-op placeholder this plan (the recording pipeline + handler land in plan 02-08 — leave a clearly-named `onRecord?` callback prop so 02-08 wires it without restructuring). Each export handler: set the trigger `isLoading`, call the matching lib fn with a `buildExportFilename(...)`, then `toastSuccess('Exported ' + filename)`; on rejection `toastError` with the locked copy. Disable items while an export runs. Every icon-only/menu control keeps `aria-label`/`data-testid` per convention. No `any`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test ExportMenu --run</automated>
  </verify>
  <acceptance_criteria>
    - PNG/JSON/Record items always render; SVG item conditional on findSvgRoot (test green).
    - Record item disabled+tooltip when MediaRecorder undefined (test green).
    - PNG click → exportToPng + toastSuccess; rejection → toastError (test green).
    - Exposes an `onRecord` (or equivalent) prop for plan 02-08 to wire (source assertion).
  </acceptance_criteria>
  <done>ExportMenu composes PNG/SVG/JSON exports with conditional SVG + disabled-Record + toasts, ready for SessionDetails (02-07) to mount and 02-08 to wire recording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel DOM → exported file | Cross-origin images in the panel can taint the canvas on PNG export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service / Error | tainted-canvas SecurityError on PNG export | mitigate | `useCORS:true` already set; catch the SecurityError and surface the ADR-018 error toast ("Export failed — {reason}…") |
| T-02-08 | Information Disclosure | SVG style-inlining bloating with all computed props | mitigate | Whitelist inlining (10 props) per Pattern 4 — no full computed-style dump |
| T-02-09 | Resource (client) | export blob never freed | mitigate | object-URL revoked in `finally` (export-png idiom reused) |
| T-02-SC | Tampering | npm installs | accept | No new packages (native APIs + existing html2canvas); legitimacy audit: none flagged |
</threat_model>

<verification>
- `cd frontend && pnpm test export-png export-svg export-json export-filename ExportMenu --run` green.
- `cd frontend && pnpm tsc --noEmit` clean.
- findSvgRoot returns null for the HTML/CSS coroutine graph (OQ-2 — no SVG export claimed on it).
</verification>

<success_criteria>
- EXPT-01: PNG export with info header, in-browser download.
- EXPT-02 (SVG tier): standalone style-inlined SVG export on genuinely-SVG panels; auto-detect hides it elsewhere.
- D-22: JSON event export. Record item present, correctly disabled when unsupported, ready for 02-08.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-05-SUMMARY.md` when done
</output>
