---
phase: 02-user-value-visualization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/lib/toast.ts
  - frontend/src/lib/toast.test.ts
autonomous: false
requirements: [EXPT-01, EXPT-02]
user_setup: []
must_haves:
  truths:
    - "@heroui/react is upgraded to ^2.7.x and the full existing frontend suite still passes"
    - "ToastProvider is mounted at the app root before any addToast call (issue #5086 ordering)"
    - "A toast helper exposes success/error toasts with the ADR-018 copy contract"
  artifacts:
    - path: "frontend/src/lib/toast.ts"
      provides: "Thin addToast wrapper (success/error) used by export + recording"
      exports: ["toastSuccess", "toastError"]
    - path: "frontend/src/main.tsx"
      provides: "ToastProvider mounted at root"
      contains: "ToastProvider"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "ToastProvider"
      via: "root-level mount above routes"
      pattern: "ToastProvider"
---

<objective>
Upgrade `@heroui/react` to ^2.7 to obtain the `Toast` system (`addToast` + `ToastProvider`) that ADR-018 mandates and that does not exist anywhere in the codebase, mount `ToastProvider` at the app root, and expose a thin toast helper with the locked copy contract — without regressing the existing HeroUI-heavy panels.

Purpose: PNG/SVG/JSON export (plan 02-05) and WebM recording (plan 02-08) all need toast feedback per ADR-018 and the UI-SPEC. The HeroUI 2.7 React Aria bump is the only non-trivial regression risk in the phase, so it is isolated here and gated behind the full suite plus a human smoke checkpoint before downstream plans build on it.
Output: HeroUI bumped to ^2.7, `ToastProvider` mounted at root (correct ordering per issue #5086), a `toast.ts` helper, and a green full suite + manual smoke of the HeroUI primitives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-value-visualization/02-RESEARCH.md
@.planning/phases/02-user-value-visualization/02-UI-SPEC.md
@.claude/skills/heroui-react/SKILL.md
@frontend/src/main.tsx
</context>

<artifacts_produced>
- `@heroui/react` bumped ^2.6.8 → ^2.7.x in package.json + lockfile
- `ToastProvider placement="bottom-right"` mounted in main.tsx above the router
- `toast.ts`: `toastSuccess(title)` / `toastError(title)` wrappers over `addToast({ title, color })`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump HeroUI to 2.7, mount ToastProvider, add the toast helper</name>
  <read_first>
    - frontend/package.json (current @heroui/react ^2.6.8, framer-motion ^11.14.4 — already satisfies 2.7 peer)
    - frontend/src/main.tsx (HeroUIProvider mount point + router; ToastProvider must mount here, above routes — issue #5086 ordering)
    - .planning/phases/02-user-value-visualization/02-UI-SPEC.md §Toast System (placement="bottom-right", addToast({title,color}), 4s auto-dismiss, max 3 visible; copy contract)
    - .claude/skills/heroui-react/SKILL.md (HeroUI provider/component conventions)
  </read_first>
  <behavior>
    - Test: toastSuccess(title) calls addToast with { title, color: 'success' }; toastError(title) calls addToast with { title, color: 'danger' } (mock @heroui/react addToast, assert the args).
  </behavior>
  <files>frontend/package.json, frontend/src/main.tsx, frontend/src/lib/toast.ts, frontend/src/lib/toast.test.ts</files>
  <action>
    Bump `@heroui/react` to the latest `^2.7.x` in `frontend/package.json` and update the lockfile via `pnpm install` (this is the APPROVED dependency from the legitimacy audit — same publisher as the pinned 2.6.8, official Toast component). Do not add any other package; `framer-motion ^11.14.4` already satisfies the 2.7 peer range.

    Mount `<ToastProvider placement="bottom-right" />` in `main.tsx` at the application root, ABOVE the router/routes (the issue #5086 ordering footgun: ToastProvider must be mounted before any `addToast` call fires). Keep the existing `HeroUIProvider` wrapper.

    Create `frontend/src/lib/toast.ts` exporting `toastSuccess(title: string)` and `toastError(title: string)` that call HeroUI's `addToast({ title, color })` with `success` / `danger` respectively. These are the single entry points export/recording will use, so the copy contract stays consistent. Add `toast.test.ts` mocking `@heroui/react`'s `addToast` and asserting the color mapping.
  </action>
  <verify>
    <automated>cd frontend && pnpm test toast --run && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json declares `@heroui/react: ^2.7` (source assertion).
    - main.tsx mounts `ToastProvider` above the router (source assertion).
    - toastSuccess/toastError map to success/danger (test command green).
    - `pnpm tsc --noEmit` clean after the bump.
  </acceptance_criteria>
  <done>HeroUI on 2.7; ToastProvider mounted with correct ordering; toast helper present and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full frontend suite as the React Aria regression guard</name>
  <read_first>
    - .planning/phases/02-user-value-visualization/02-RESEARCH.md (Pitfall 7 — bump in isolation, run full suite, smoke Tabs/Select/Slider/Dropdown/Table)
    - frontend/src/components/*.test.tsx (existing suite is the guard; do not add new component tests here)
  </read_first>
  <files>frontend/ (no source files modified; runs the existing suite — only forward-fixes to HeroUI-touching components if 2.7 API drift breaks a test)</files>
  <action>
    Run the FULL existing frontend test suite — it is the automated regression guard for the React Aria packages HeroUI 2.7 upgraded. If anything breaks, the failure is in the HeroUI surface (Tabs/Select/Slider/Dropdown/Table API drift). Triage: fix forward against the 2.7 API if the change is trivial and mechanical; if the React Aria bump destabilizes the suite non-trivially, the documented fallback is `sonner` (HeroUI-token-styled) per the UI-SPEC Toast System — but do NOT switch to sonner without surfacing it in the checkpoint below. Do not weaken or skip tests to make them pass.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run</automated>
  </verify>
  <acceptance_criteria>
    - Full existing suite green after the bump (test command).
    - No tests skipped/weakened to pass (source assertion: no new `.skip`/`.only`/removed assertions in the diff).
  </acceptance_criteria>
  <done>Full frontend suite green on HeroUI 2.7; any drift fixed forward without weakening tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke of HeroUI 2.7 primitives (React Aria regression gate)</name>
  <files>frontend/ (no files modified — human verification of the running app after the bump)</files>
  <action>Pause for the developer to manually smoke-test the HeroUI primitives in the running app after the 2.7 bump (the automated suite cannot catch all React Aria interaction-level regressions). Present the what-built/how-to-verify below and wait for the resume signal before any downstream plan builds on the toast system.</action>
  <what-built>HeroUI upgraded to 2.7 with ToastProvider mounted; full automated suite green. Downstream export/recording plans depend on this toast system and on the HeroUI primitives not regressing.</what-built>
  <how-to-verify>
    1. Run `cd frontend && pnpm dev` and open the app.
    2. Smoke the HeroUI-heavy surfaces that the React Aria bump could affect: open a session → confirm the Tabs switch, the Coroutines view-mode toggle works, the speed Dropdown opens, any Slider drags, and a Select (e.g. on the gallery/scenarios page) opens and selects.
    3. Confirm no console errors related to HeroUI / React Aria on load.
    4. (Optional) trigger a toast via the temporary dev hook if present, or confirm ToastProvider mounts without error.
  </how-to-verify>
  <resume-signal>Type "approved" if the HeroUI primitives behave and there are no React Aria console errors, or describe what regressed (e.g. "Slider drag broken") so it can be fixed before export work builds on it.</resume-signal>
  <verify><human-check>User confirms Tabs/Select/Slider/Dropdown/Table behave and no React Aria console errors appear on load.</human-check></verify>
  <done>User typed "approved"; HeroUI 2.7 primitives verified non-regressive in the running app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency upgrade | @heroui/react 2.7 transitively bumps React Aria — regression surface for existing components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering (supply chain) | @heroui/react ^2.7 bump | mitigate | APPROVED in legitimacy audit (same publisher as 2.6.8); bumped in isolation; full suite + human smoke gate before downstream builds on it |
| T-02-SC | Tampering | npm install (@heroui/react ^2.7) | mitigate | Package Legitimacy Audit verdict APPROVED (no new publisher trust surface); no [ASSUMED]/[SUS]/[SLOP] packages — no blocking-human legitimacy checkpoint required |
</threat_model>

<verification>
- `cd frontend && pnpm test --run` green (full suite — the React Aria regression guard).
- `cd frontend && pnpm tsc --noEmit` clean.
- Human smoke checkpoint passed (Tabs/Select/Slider/Dropdown/Table behave; no React Aria console errors).
</verification>

<success_criteria>
- Toast system available app-wide (ADR-018) with correct provider ordering.
- HeroUI 2.7 upgrade verified non-regressive — downstream export/recording plans can safely use `toastSuccess`/`toastError`.
</success_criteria>

<output>
Create `.planning/phases/02-user-value-visualization/02-04-SUMMARY.md` when done
</output>
