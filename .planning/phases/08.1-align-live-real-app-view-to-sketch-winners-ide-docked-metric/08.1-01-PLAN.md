---
phase: "08.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/source-frames.ts
  - frontend/src/lib/source-frames.test.ts
  - frontend/src/components/LivePill.tsx
  - frontend/src/components/LivePill.test.tsx
  - frontend/src/components/SourceAttribution.tsx
  - frontend/src/components/SourceAttribution.test.tsx
  - frontend/src/components/CoroutineNodeEnhanced.tsx
  - frontend/src/components/CoroutineTimelineView.tsx
autonomous: true
requirements: [RCO-06, RCO-07]

must_haves:
  truths:
    - "A LIVE pill with '~150ms poll' renders when streamEnabled is true; a DEMO pill renders otherwise (sketch 001-C)"
    - "Clicking a user-code file:line copies '{fileName}:{lineNumber}' to the clipboard and shows the toast 'Copied {fileName}:{lineNumber}' (S1 v1, LOCKED)"
    - "User-code frames render bold (font-semibold) + accent (text-primary) + cursor-pointer; library frames (kotlinx.coroutines / kotlin.coroutines / kotlin) render dimmed (text-default-400, weight 400) and are NOT clickable (sketch 002-B)"
    - "SourceAttribution renders a compact creation-frame chip by default and expands to 'Created at' / 'Suspended at' collapsible stacks on click (sketch 002-B; 002-A compact as collapsed default)"
  artifacts:
    - path: "frontend/src/lib/source-frames.ts"
      provides: "isLibraryFrame() / isUserFrame() classifier over a frame's function string (kotlinx.coroutines/kotlin.coroutines/kotlin prefixes = library)"
      exports: ["isLibraryFrame", "isUserFrame"]
    - path: "frontend/src/components/LivePill.tsx"
      provides: "LIVE/DEMO pill driven by a streamEnabled boolean"
      exports: ["LivePill"]
    - path: "frontend/src/components/SourceAttribution.tsx"
      provides: "Compact source chip -> expandable Created-at/Suspended-at stacks with per-user-frame jump-to-code"
      exports: ["SourceAttribution"]
  key_links:
    - from: "frontend/src/components/SourceAttribution.tsx"
      to: "frontend/src/lib/source-frames.ts"
      via: "isUserFrame import to decide bold/clickable vs dimmed"
      pattern: "isUserFrame|isLibraryFrame"
    - from: "frontend/src/components/CoroutineNodeEnhanced.tsx"
      to: "frontend/src/lib/toast.ts"
      via: "toastSuccess on clipboard copy"
      pattern: "toastSuccess"
---

<objective>
Build the Wave-1 primitives that the docked-panel reflow (Plan 02) depends on, and land the jump-to-code affordance (Delta S1) on the two existing inert `file:line` render sites.

Purpose: Deltas S1 and L2 of the UI-SPEC require reusable pieces — a frame classifier (user vs library), a LIVE/DEMO pill, and a SourceAttribution chip→stack — plus making the two existing `file:line` text sites clickable. These have no dependency on SessionDetails layout, so they ship first and in parallel-friendly isolation.

Output: `source-frames.ts` (+ test), `LivePill.tsx` (+ test), `SourceAttribution.tsx` (+ test), and jump-to-code edits to `CoroutineNodeEnhanced.tsx` + `CoroutineTimelineView.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md
@.claude/skills/sketch-findings-vizcore/references/source-attribution.md
@frontend/src/lib/toast.ts
@frontend/src/types/api.ts
</context>

<artifacts_this_phase_produces>
- NEW `frontend/src/lib/source-frames.ts` + `source-frames.test.ts` — frame classifier (user vs library).
- NEW `frontend/src/components/LivePill.tsx` + `LivePill.test.tsx` — LIVE/DEMO pill.
- NEW `frontend/src/components/SourceAttribution.tsx` + `SourceAttribution.test.tsx` — compact chip → expandable Created-at/Suspended-at stacks.
- EDIT `CoroutineNodeEnhanced.tsx` (:75-77 suspension `file:line`) and `CoroutineTimelineView.tsx` (:131-135 and :255-259) — jump-to-code on user frames.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frame classifier + LivePill</name>
  <files>frontend/src/lib/source-frames.ts, frontend/src/lib/source-frames.test.ts, frontend/src/components/LivePill.tsx, frontend/src/components/LivePill.test.tsx</files>
  <read_first>
    - frontend/src/types/api.ts (SuspensionPoint shape: function/fileName/lineNumber/reason — lines 484-490)
    - .planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md (Typography 2-weight rule line 59; Copywriting "LIVE"/"DEMO"/"~150ms poll" lines 93-94; Color accent reserved list lines 76-82)
    - .claude/skills/sketch-findings-vizcore/references/source-attribution.md (user-bold/library-dim, library = kotlinx.coroutines / kotlin.coroutines / stdlib)
  </read_first>
  <behavior>
    source-frames.ts:
    - isLibraryFrame("kotlinx.coroutines.delay") === true
    - isLibraryFrame("kotlin.coroutines.intrinsics.foo") === true
    - isLibraryFrame("kotlin.collections.map") === true
    - isLibraryFrame("com.acme.app.OrderService.process") === false
    - isUserFrame is the strict negation of isLibraryFrame
    - Null/empty/undefined function string => treated as library (not a jump target), so isUserFrame("") === false
    LivePill:
    - streamEnabled=true renders text "LIVE" and the sub-label "~150ms poll"
    - streamEnabled=false renders text "DEMO" and no poll sub-label
  </behavior>
  <action>
    Create `frontend/src/lib/source-frames.ts` exporting `isLibraryFrame(fn: string | null | undefined): boolean` and `isUserFrame(fn): boolean`. Classify as library when the function string starts with any of these literal prefixes: `kotlinx.coroutines`, `kotlin.coroutines`, `kotlin.`, `java.`, `jdk.`, `sun.`. Empty/null/undefined => library (so it is never a jump target). `isUserFrame = !isLibraryFrame`. No regex template construction; plain `startsWith` checks.

    Create `frontend/src/components/LivePill.tsx` exporting `LivePill({ streamEnabled }: { streamEnabled: boolean })`. When `streamEnabled`, render a HeroUI `Chip` (size="sm") with literal classes `bg-success/10 text-success border-success/20` containing a dot + "LIVE", and an adjacent `<span className="text-xs text-default-500">~150ms poll</span>`. When not, render a `Chip` with literal classes `bg-default-100 text-default-500` containing "DEMO" and no poll sub-label. Driven solely by the `streamEnabled` prop (the existing live flag in SessionDetails) per UI-SPEC line 126. Literal Tailwind only (IN-12).

    Colocate Vitest tests asserting the behavior cases above. For LivePill use Testing Library `render` + `screen.getByText`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/lib/source-frames.test.ts src/components/LivePill.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `isLibraryFrame` returns true for the kotlinx/kotlin/java prefixes and false for app packages; `isUserFrame("")` is false (asserted in test).
    - `<LivePill streamEnabled />` renders text "LIVE" and "~150ms poll"; `<LivePill streamEnabled={false} />` renders "DEMO" with no "~150ms poll".
    - LivePill markup contains only literal Tailwind class strings (no `bg-${...}` interpolation) — grep `grep -v '^//' frontend/src/components/LivePill.tsx | grep -c 'bg-\${'` returns 0.
    - `cd frontend && pnpm test --run src/lib/source-frames.test.ts src/components/LivePill.test.tsx` passes.
  </acceptance_criteria>
  <done>source-frames classifier and LivePill exist with passing colocated tests; both use literal Tailwind only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SourceAttribution chip -> expandable stacks</name>
  <files>frontend/src/components/SourceAttribution.tsx, frontend/src/components/SourceAttribution.test.tsx</files>
  <read_first>
    - frontend/src/types/api.ts (SuspensionPoint — lines 484-490; HierarchyNode.suspensionPoints — line 469)
    - frontend/src/lib/source-frames.ts (isUserFrame — created in Task 1)
    - frontend/src/lib/toast.ts (toastSuccess — line 13)
    - .claude/skills/sketch-findings-vizcore/references/source-attribution.md (winner B full stack, A compact collapsed default; user-bold/library-dim; per-user-frame jump-to-code)
    - .planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md (Delta L2 lines 128-138; Delta S1 lines 140-144; Copywriting "Created at"/"Suspended at" line 105; empty-state copy lines 95-96)
  </read_first>
  <behavior>
    Props: `{ frames: SuspensionPoint[] }` (each frame has function/fileName/lineNumber/reason).
    - With no frames (empty array): renders the "No source attribution for this coroutine." copy (UI-SPEC line 96).
    - Collapsed default: renders a single compact chip showing the FIRST user-code frame's `{fileName}:{lineNumber}` in font-mono (the creation chip, sketch 002-A).
    - Clicking the chip expands two sections labelled "Created at" and "Suspended at" (sketch 002-B). Clicking again collapses.
    - In the expanded stack, a user frame's `file:line` has role="button", aria-label="Jump to code: {fileName}:{lineNumber}", text-primary, cursor-pointer, hover:underline.
    - A library frame's `file:line` is plain text-default-400 weight-400, no role, no aria-label, not clickable.
    - Clicking a user frame's file:line calls navigator.clipboard.writeText("{fileName}:{lineNumber}") and fires toastSuccess("Copied {fileName}:{lineNumber}").
  </behavior>
  <action>
    Create `frontend/src/components/SourceAttribution.tsx` exporting `SourceAttribution({ frames }: { frames: SuspensionPoint[] })`. Use a local `useState(false)` `expanded` flag. Collapsed: render one HeroUI `Chip` (variant="flat", `font-mono text-xs`) with the first frame where `isUserFrame(frame.function)` (fall back to the first frame if none are user frames) formatted `{fileName}:{lineNumber}`; the chip is a `role="button"` that toggles `expanded`. Expanded: render two sections with `<span className="text-sm font-semibold">Created at</span>` and `Suspended at` headings, each listing frame rows. Split frames into the two sections by `reason` is NOT available for creation — instead render ALL frames under both headings is wrong; use this rule: the FIRST frame is the creation frame ("Created at"), the remaining frames are "Suspended at". If only one frame exists, show it under "Created at" and render the "Suspended at" section empty with no rows.

    Each frame row: render `{frame.function}` on one line (`font-mono text-xs`), and the `{fileName}:{lineNumber}` on the next. For user frames (`isUserFrame(frame.function)`) the file:line is the jump target — `role="button"`, `tabIndex={0}`, `aria-label={`Jump to code: ${fileName}:${lineNumber}`}`, literal classes `text-primary cursor-pointer hover:underline font-semibold`, onClick handler that calls `navigator.clipboard.writeText(`${fileName}:${lineNumber}`)` then `toastSuccess(`Copied ${fileName}:${lineNumber}`)` (import from `@/lib/toast`). For library frames render plain `<span className="text-default-400 font-mono text-xs">` (weight 400, dimmed), no role/aria/onClick.

    Empty frames array => render `<div className="text-xs text-default-400">No source attribution for this coroutine.</div>`. Wrap clipboard write in a guard so a missing `navigator.clipboard` (jsdom) does not throw — the test mocks it. Literal Tailwind only (IN-12). HeroUI `Chip`/`Button` + react-icons/fi only; no new deps.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/SourceAttribution.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Empty frames renders exactly "No source attribution for this coroutine."
    - Default render shows a single compact `font-mono` chip with the first user frame's `file:line`.
    - Clicking the chip reveals headings "Created at" and "Suspended at" (assert both visible via getByText); clicking again hides them.
    - A user frame's file:line has `role="button"` + `aria-label="Jump to code: <fileName>:<lineNumber>"` and classes `text-primary cursor-pointer hover:underline`; a library frame's file:line has neither role nor aria-label.
    - Clicking a user frame's file:line calls a mocked `navigator.clipboard.writeText` with "<fileName>:<lineNumber>" and a mocked `toastSuccess` with "Copied <fileName>:<lineNumber>" (assert via vi.fn mocks).
    - `grep -v '^//' frontend/src/components/SourceAttribution.tsx | grep -c 'bg-\${\|text-\${'` returns 0.
    - `cd frontend && pnpm test --run src/components/SourceAttribution.test.tsx` passes.
  </acceptance_criteria>
  <done>SourceAttribution renders compact chip, expands to Created-at/Suspended-at, jumps user frames via copy+toast, dims library frames; colocated test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Jump-to-code on the two existing file:line sites (S1)</name>
  <files>frontend/src/components/CoroutineNodeEnhanced.tsx, frontend/src/components/CoroutineTimelineView.tsx</files>
  <read_first>
    - frontend/src/components/CoroutineNodeEnhanced.tsx (inert file:line at :75-77 inside the Suspension Points tooltip)
    - frontend/src/components/CoroutineTimelineView.tsx (inert file:line at :131-135 and :255-259)
    - frontend/src/lib/source-frames.ts (isUserFrame — Task 1)
    - frontend/src/lib/toast.ts (toastSuccess)
    - .planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md (Delta S1 lines 140-144: accent text-primary, hover:underline, cursor-pointer, role="button", aria-label, tooltip "Copy file:line", library frames get none)
  </read_first>
  <behavior>
    - In CoroutineNodeEnhanced (suspension points tooltip, :75-77) and CoroutineTimelineView (:131-135 suspension-point card, :255-259 timeline event card), the `{fileName}:{lineNumber}` for a user frame (isUserFrame(point.function)) becomes a clickable jump target.
    - Clicking copies "{fileName}:{lineNumber}" and fires toastSuccess("Copied {fileName}:{lineNumber}").
    - Library frames keep the existing inert dimmed text (no role/aria/onClick).
  </behavior>
  <action>
    Extract a small shared click handler (either inline per-site or a tiny local helper — do NOT add a new exported module beyond what Task 1/2 created) that, given `fileName` and `lineNumber`, writes `${fileName}:${lineNumber}` to `navigator.clipboard` (guarded) and calls `toastSuccess(`Copied ${fileName}:${lineNumber}`)`.

    In `CoroutineNodeEnhanced.tsx` around the suspension-point tooltip rows (currently `{point.fileName}:{point.lineNumber} ({point.reason})` at :75-77): when `isUserFrame(point.function)`, wrap the `{fileName}:{lineNumber}` portion in a `<span role="button" tabIndex={0} aria-label={`Jump to code: ${point.fileName}:${point.lineNumber}`} className="text-primary cursor-pointer hover:underline" onClick={...}>`. Keep the `({point.reason})` suffix outside the clickable span. For library frames leave the existing `text-default-400` text unchanged.

    In `CoroutineTimelineView.tsx` at :131-135 (suspension points card) and :255-259 (timeline event card): apply the same user-frame jump treatment to the `{fileName}{:lineNumber}` render, gating on `isUserFrame(point.function)` / `isUserFrame(event.suspensionPoint.function)`. Wrap each user-frame file:line in HeroUI `Tooltip content="Copy file:line"` per UI-SPEC line 92. Library frames keep their current dimmed inert text.

    Import `isUserFrame` from `@/lib/source-frames` and `toastSuccess` from `@/lib/toast`. Literal Tailwind only.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/CoroutineTimelineView.test.tsx 2>/dev/null; cd /Users/jirihermann/Documents/workspace-vizcore/vizcore/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - In both files, a user-frame file:line renders with `role="button"`, `aria-label="Jump to code: <fileName>:<lineNumber>"`, and classes `text-primary cursor-pointer hover:underline`.
    - A library-frame file:line in both files renders no role/aria-label and keeps `text-default-400`.
    - The CoroutineTimelineView jump targets are wrapped in a Tooltip whose content is "Copy file:line".
    - `cd frontend && pnpm lint` and `cd frontend && pnpm build` pass green (tsc + vite).
    - `grep -RnE 'bg-\$\{|text-\$\{' frontend/src/components/CoroutineNodeEnhanced.tsx frontend/src/components/CoroutineTimelineView.tsx` returns nothing (no dynamic class construction introduced).
  </acceptance_criteria>
  <done>Both existing file:line sites are jump-to-code targets for user frames (copy + toast), library frames untouched; lint + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → clipboard API | `file:line` strings written to `navigator.clipboard` on user click |
| build → rendered Tailwind classes | literal class strings must survive (no runtime construction) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Information Disclosure | clipboard write of `{fileName}:{lineNumber}` | accept | Only a relative file name + line number (no absolute path, no secret, no PII) is copied, and only on explicit user click. No sensitive data crosses the boundary. |
| T-08.1-02 | Tampering | literal Tailwind classes in LivePill/SourceAttribution/edited sites | mitigate | Literal class strings only (IN-12); grep gates in acceptance criteria assert no `bg-${`/`text-${` interpolation; rendered-text tests assert the live classes. |
| T-08.1-SC | Tampering | npm installs (supply-chain) | mitigate | No new dependencies introduced — HeroUI primitives + react-icons + existing `@/lib/toast` only. No package-manager install task exists, so the legitimacy gate is N/A. |
</threat_model>

<verification>
- `cd frontend && pnpm test --run src/lib/source-frames.test.ts src/components/LivePill.test.tsx src/components/SourceAttribution.test.tsx` all pass.
- `cd frontend && pnpm lint` clean.
- `cd frontend && pnpm build` (tsc + vite) succeeds.
- No new entries in `frontend/package.json` dependencies.
- `use-event-stream.ts` and any backend file are untouched (`git diff --name-only` shows only the files in `files_modified`).
</verification>

<success_criteria>
- `source-frames.ts`, `LivePill.tsx`, `SourceAttribution.tsx` exist with passing colocated tests.
- Jump-to-code (copy + "Copied {fileName}:{lineNumber}" toast) works on user frames in `CoroutineNodeEnhanced.tsx` and `CoroutineTimelineView.tsx`; library frames stay inert/dimmed.
- 2-weight contrast holds: user frames font-semibold + text-primary, library frames weight-400 + text-default-400.
- All frontend gates green; no backend or `use-event-stream.ts` change.
</success_criteria>

<output>
Create `.planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-01-SUMMARY.md` when done.
</output>
