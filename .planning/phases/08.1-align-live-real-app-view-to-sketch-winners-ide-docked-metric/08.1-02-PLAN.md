---
phase: "08.1"
plan: "02"
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - frontend/src/components/SessionMetrics.tsx
  - frontend/src/components/SessionMetrics.test.tsx
  - frontend/src/components/SessionDetails.tsx
  - frontend/src/components/SessionDetails.test.tsx
autonomous: true
requirements: [RCO-06, RCO-07]

must_haves:
  truths:
    - "Metric numerals (Active/Peak/Throughput) render font-semibold (600), not font-bold (700) — the live 2-weight type scale (UI-SPEC line 59)"
    - "SessionMetrics tiles reflow from the md:grid-cols-2 card grid into a single horizontal strip (Active · Peak · Throughput /s · Dispatcher util) (Delta L1)"
    - "Metrics + leaks no longer render under the Threads tab; they render in a static docked panel below the canvas with a LIVE/DEMO pill in the header (Delta L1, LOCKED static)"
    - "The Active tile keeps text-primary; leaks stay amber/warning via LeakList; danger is never used for leaks"
  artifacts:
    - path: "frontend/src/components/SessionMetrics.tsx"
      provides: "Horizontal metric-tile strip (reflowed) with font-semibold numerals; optional strip layout mode"
      contains: "font-semibold"
    - path: "frontend/src/components/SessionDetails.tsx"
      provides: "Static docked panel below canvas hosting the tile strip + LivePill; Threads tab no longer mounts SessionMetrics"
      contains: "LivePill"
  key_links:
    - from: "frontend/src/components/SessionDetails.tsx"
      to: "frontend/src/components/LivePill.tsx"
      via: "LivePill streamEnabled={streamEnabled} in the dock header"
      pattern: "LivePill"
    - from: "frontend/src/components/SessionDetails.tsx"
      to: "frontend/src/components/SessionMetrics.tsx"
      via: "SessionMetrics mounted in docked panel (not Threads tab)"
      pattern: "SessionMetrics"
---

<objective>
Reflow the shipped tabbed metrics into the locked sketch-winner layout: a static docked panel below the canvas with a horizontal metric-tile strip and a LIVE/DEMO pill (Delta L1), and remove metrics/leaks from the Threads tab.

Purpose: Phase 8 buried metrics + leaks under the "Threads" tab in a 2-col card grid. Sketch winner 001-C is the opposite — IDE-docked header tiles, always visible. This plan moves them and fixes the numeral weight to the 2-weight type scale, consuming the Wave-1 LivePill.

Output: `SessionMetrics.tsx` reflowed to a horizontal strip with `font-semibold` numerals; `SessionDetails.tsx` with a static docked panel + LivePill and a Threads tab that no longer hosts SessionMetrics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md
@.claude/skills/sketch-findings-vizcore/references/layout-live-view.md
@.planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-01-SUMMARY.md
@frontend/src/components/SessionMetrics.tsx
</context>

<artifacts_this_phase_produces>
- EDIT `SessionMetrics.tsx` — tiles reflow from `md:grid-cols-2` grid to a horizontal strip; numerals `font-bold`→`font-semibold` (lines 64/71/81/85); Dispatcher-util tile joins the strip.
- EDIT `SessionDetails.tsx` — remove `<SessionMetrics>` from the Threads tab (:782-793); mount a static docked panel below the live canvas with the tile strip + `<LivePill streamEnabled={streamEnabled} />` header; `<DispatcherOverview>` (thread lanes) may remain under Threads.
- Test updates: `SessionMetrics.test.tsx` (assert strip + font-semibold) and `SessionDetails.test.tsx` (assert dock placement / Threads-tab removal).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reflow SessionMetrics to a horizontal strip + 2-weight numerals</name>
  <files>frontend/src/components/SessionMetrics.tsx, frontend/src/components/SessionMetrics.test.tsx</files>
  <read_first>
    - frontend/src/components/SessionMetrics.tsx (current md:grid-cols-2 grid at :59; numerals font-bold at :64/:71/:81/:85; Active text-primary at :64; LeakList usage at :126)
    - frontend/src/components/SessionMetrics.test.tsx (existing assertions to keep green)
    - .planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md (Delta L1 lines 113-126 — single horizontal row, wrap allowed; Typography line 59 — font-bold→font-semibold; Color line 84 — leaks warning never danger)
    - .claude/skills/sketch-findings-vizcore/references/layout-live-view.md (header tile strip: Active/Peak/Throughput /s/Dispatcher util)
  </read_first>
  <behavior>
    - The four tiles (Active, Peak, Throughput, Dispatcher utilization) render in a single horizontal flex row (wrap allowed on narrow widths), NOT a `md:grid-cols-2` grid.
    - Active numeral keeps `text-primary`.
    - Active/Peak/Throughput numerals render `font-semibold` (600), not `font-bold` (700).
    - Throughput still shows "—" when zero and "N /s" otherwise.
    - Leaks still render via LeakList (warning/amber), surfaced beside the strip — never danger.
  </behavior>
  <action>
    In `SessionMetrics.tsx`: replace the container `<div className="grid grid-cols-1 md:grid-cols-2 gap-4">` (:59) with a horizontal strip `<div className="flex flex-wrap gap-4">` so the four tiles form one row that wraps on narrow widths (UI-SPEC line 124). Keep the `MetricCard` rendering and the Dispatcher-utilization tile; the Dispatcher tile joins the same strip row.

    Change the metric numerals from `font-bold` to `font-semibold` at the three numeral sites (Active :64, Peak :71, Throughput :81, and the em-dash zero case :85) — this makes the live contract a true 2-weight scale (UI-SPEC line 59). Keep Active's `text-primary`. Do NOT touch the throughput "/s" suffix, the em-dash zero logic, or `formatThroughput`.

    Keep the LeakList block (warning/amber) — leaks remain rendered here, never danger. The `<h3>Session metrics</h3>` heading may stay or be dropped by the dock; keep it for now (the dock header in Task 2 provides its own framing). Literal Tailwind only (IN-12).

    Update `SessionMetrics.test.tsx` to assert (a) the numerals carry `font-semibold` not `font-bold`, and (b) the tile container is a flex strip (no `md:grid-cols-2`). Keep all existing passing assertions (Active text-primary, throughput "—"/"/s", leaks).
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/SessionMetrics.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'font-bold' frontend/src/components/SessionMetrics.tsx` returns 0; `grep -c 'font-semibold' frontend/src/components/SessionMetrics.tsx` is ≥ 3.
    - `grep -c 'md:grid-cols-2' frontend/src/components/SessionMetrics.tsx` returns 0; the tile container uses `flex flex-wrap`.
    - Active numeral still carries `text-primary`; throughput still renders "—" at zero and "/s" suffix when positive (existing tests stay green).
    - LeakList is still rendered (leaks path unchanged, warning/amber).
    - `cd frontend && pnpm test --run src/components/SessionMetrics.test.tsx` passes.
  </acceptance_criteria>
  <done>Tiles reflow to a horizontal strip; numerals are font-semibold; Active stays text-primary; leaks stay amber; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Static docked panel + LivePill in SessionDetails; remove metrics from Threads tab</name>
  <files>frontend/src/components/SessionDetails.tsx, frontend/src/components/SessionDetails.test.tsx</files>
  <read_first>
    - frontend/src/components/SessionDetails.tsx (live canvas card at :734-749; Threads tab SessionMetrics+DispatcherOverview at :767-794; streamEnabled flag at :104; imports at :32-33)
    - frontend/src/components/SessionMetrics.tsx (reflowed strip — Task 1)
    - frontend/src/components/LivePill.tsx (LivePill { streamEnabled } — Plan 01)
    - frontend/src/components/SessionDetails.test.tsx (existing assertions to keep green)
    - .planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-UI-SPEC.md (Delta L1 contract lines 122-126 — static dock LOCKED, no collapse/resize; Component inventory lines 152-159; Color line 71-73 surfaces/border)
    - .claude/skills/sketch-findings-vizcore/references/layout-live-view.md (docked bottom panel below canvas; header tile strip; LIVE pill + ~150ms; leaks inline)
  </read_first>
  <behavior>
    - The Threads tab no longer renders `<SessionMetrics>`. `<DispatcherOverview>` (thread lanes) MAY remain under Threads.
    - A static docked panel renders below the live tree/graph canvas (the "What's running now" region), containing: a header with `<LivePill streamEnabled={streamEnabled} />` and the `<SessionMetrics>` tile strip, with leaks inline.
    - The dock is static — no collapse/resize controls (LOCKED).
    - LivePill shows LIVE when streamEnabled, DEMO otherwise.
  </behavior>
  <action>
    Import `LivePill` from `./LivePill` (add to the import block near :32-33).

    Remove the `<SessionMetrics>` element from the Threads tab block (currently :782-793 inside `<div className="py-2 space-y-4">`). Leave `<DispatcherOverview>` where it is (thread lanes may remain under Threads per UI-SPEC line 125) — only `SessionMetrics` moves.

    Below the live canvas card (after the `</Card>` that closes the tree/graph panel at ~:749, still inside the live "What's running now" Tab region) add a static docked panel: a wrapping `<div>` styled as the dock — literal classes `mt-8 rounded-xl border-t border-default-200 bg-content1` (UI-SPEC: `--surface` body, `--border` top edge; `mt-8` = xl canvas↔dock separation, spacing line 43). Inside it a header row (`<div className="flex items-center justify-between gap-4 p-6">`, lg dock padding) containing `<LivePill streamEnabled={streamEnabled} />` and the reflowed `<SessionMetrics sessionId={sessionId} isLive={streamEnabled} enabled={!readOnly} />` tile strip. Keep `SessionMetrics`'s existing props exactly. The dock is static — do NOT add collapse/resize/drag handles (LOCKED, UI-SPEC line 123).

    Do NOT modify `use-event-stream.ts`, the SSE pipeline, or any data hook. This is layout-only re-parenting of existing components. Literal Tailwind only (IN-12).

    Update `SessionDetails.test.tsx`: assert that within the Threads tab `SessionMetrics` is NOT rendered, and that the docked panel renders `LivePill` + the metric strip in the live view. If the existing test mounts SessionDetails with the full provider tree, mock `useSessionMetrics`/`LivePill` minimally as the existing tests already do for child data hooks; keep all currently-passing assertions green.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/SessionDetails.test.tsx && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `<SessionMetrics>` no longer appears inside the Threads `<Tab key="threads">` block (grep: the `SessionMetrics` element does not occur between the `key="threads"` Tab open and its close).
    - A docked panel exists below the live canvas containing `<LivePill streamEnabled={streamEnabled} />` and `<SessionMetrics ... />`; the dock wrapper uses literal classes `border-t border-default-200 bg-content1` and `mt-8`.
    - No collapse/resize/drag handle is added (grep `resize\|collapse\|drag` introduces nothing new in the dock region).
    - `git diff --name-only` does NOT include `frontend/src/hooks/use-event-stream.ts` or any `backend/` file.
    - `cd frontend && pnpm test --run src/components/SessionDetails.test.tsx`, `pnpm lint`, and `pnpm build` all pass.
  </acceptance_criteria>
  <done>Metrics moved out of the Threads tab into a static docked panel with a LivePill header below the live canvas; all frontend gates green; SSE/backend untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → rendered Tailwind classes | docked-panel + reflowed-strip classes must be literal (no runtime construction) |
| component tree → existing data hooks | re-parenting must not alter the SSE/metrics data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-03 | Tampering | literal Tailwind in reflowed strip + dock panel | mitigate | Literal class strings only (IN-12); grep gates assert no `bg-${`/`text-${` and assert the dock's literal `border-t`/`bg-content1`/`mt-8`. |
| T-08.1-04 | Denial of Service | re-parenting SessionMetrics could double-mount the protected /metrics poll | mitigate | SessionMetrics mounts in exactly ONE place after the move (removed from Threads, added to dock); `enabled={!readOnly}` preserved so read-only shares do not poll/401; assert single mount in test. |
| T-08.1-SC | Tampering | npm installs (supply-chain) | mitigate | No new dependencies — HeroUI primitives + existing components only. No install task; legitimacy gate N/A. |
</threat_model>

<verification>
- `cd frontend && pnpm test --run` (full suite) green, including SessionMetrics + SessionDetails.
- `cd frontend && pnpm lint` clean; `cd frontend && pnpm build` (tsc + vite) succeeds.
- `git diff --name-only` shows only `SessionMetrics.tsx(.test)` and `SessionDetails.tsx(.test)` — no `use-event-stream.ts`, no `backend/`.
- No new `frontend/package.json` dependency.
- Visual: metrics + LIVE pill render in the docked panel below the canvas; Threads tab shows only thread lanes / DispatcherOverview.
</verification>

<success_criteria>
- Metric tiles render as a horizontal strip with `font-semibold` numerals; Active stays `text-primary`.
- Metrics + leaks + LivePill render in a static docked panel below the live canvas (Delta L1); Threads tab no longer hosts SessionMetrics.
- Leaks stay amber/warning (LeakList), never danger.
- All frontend gates green; SSE pipeline and backend untouched.
</success_criteria>

<output>
Create `.planning/phases/08.1-align-live-real-app-view-to-sketch-winners-ide-docked-metric/08.1-02-SUMMARY.md` when done.
</output>
