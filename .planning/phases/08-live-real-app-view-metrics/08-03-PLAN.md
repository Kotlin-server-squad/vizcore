---
phase: 08-live-real-app-view-metrics
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/lib/api-client.ts
  - frontend/src/hooks/use-session-metrics.ts
  - frontend/src/components/SessionMetrics.tsx
  - frontend/src/components/LeakList.tsx
  - frontend/src/components/SessionDetails.tsx
  - frontend/src/hooks/use-session-metrics.test.ts
  - frontend/src/components/SessionMetrics.test.tsx
  - frontend/src/components/LeakList.test.tsx
  - frontend/src/components/SessionDetails.test.tsx
autonomous: false
requirements: [RCO-06, RCO-07]
must_haves:
  truths:
    - "The live view defaults to active-only (\"What's running now\"): completed coroutines collapse into a \"Show completed (N)\" expandable aggregate, and rendered nodes are capped with an \"N more coroutines\" indicator."
    - "The reconstructed hierarchy from 08-01 renders as a real parent/child named tree in CoroutineTreeGraph/CoroutineTree (no longer a flat anonymous wall)."
    - "A \"Session metrics\" panel shows Active / Peak / Throughput (/s) / Dispatcher utilization / Potential leaks, fed by GET /metrics polled while live (useSessionMetrics clones useThreadActivity's poll-while-live semantics)."
    - "A leak list shows flagged long-lived coroutines as warning (amber) Chips — \"{coroutineName} · alive {duration}\" — never danger (danger stays reserved for FAILED)."
    - "use-event-stream.ts is untouched — SSE consumption/dedup/backoff is reused as-is."
  artifacts:
    - path: "frontend/src/hooks/use-session-metrics.ts"
      provides: "useSessionMetrics(sessionId, isLive, enabled) poll-while-live query"
      contains: "session-metrics"
    - path: "frontend/src/components/SessionMetrics.tsx"
      provides: "Metrics Card grid (Active/Peak/Throughput/Dispatcher util/Potential leaks)"
      contains: "Session metrics"
    - path: "frontend/src/components/LeakList.tsx"
      provides: "warning-highlighted leak list (Chip rows)"
      contains: "potential leak"
  key_links:
    - from: "frontend/src/components/SessionDetails.tsx"
      to: "SessionMetrics + active-only filter"
      via: "TERMINAL_STATES filter on panelCoroutines + <SessionMetrics sessionId isLive={streamEnabled} />"
      pattern: "SessionMetrics"
    - from: "frontend/src/hooks/use-session-metrics.ts"
      to: "/api/sessions/{id}/metrics"
      via: "apiClient.getMetrics"
      pattern: "getMetrics"
    - from: "apiClient.getMetrics"
      to: "MetricsResponse"
      via: "fetchJson typed against the 08-02 DTO shape"
      pattern: "MetricsResponse"
---

<objective>
Surface the now-meaningful real-app data in the existing frontend: default the live view to
active-only ("What's running now"), render the reconstructed named hierarchy, and add a "Session
metrics" panel + leak list fed by GET /metrics. Delivers the FE surfaces of RCO-06 (live "what's
running now") and RCO-07 (aggregate metrics + leak detection), per the APPROVED 08-UI-SPEC.

Implements D-08 (active-only default, "Show completed (N)" toggle, "N more" node cap), D-06 FE
display (dispatcher utilization via DispatcherOverview reuse), and D-07 FE (leak badge + filterable
list + warning highlight).

Purpose: Beat the Phase-7 ~1,800-card flat wall. Reuse-first — DispatcherOverview, StateIndicator,
EmptyState, CoroutineTree/Graph, and use-event-stream.ts are all existing; only the metrics panel,
leak list, the metrics hook, and the active-only controls are new.
Output: types + api-client method, useSessionMetrics hook, SessionMetrics + LeakList components, and
the SessionDetails active-only/metrics-mount edits.

Depends on 08-02 (the MetricsResponse API shape) and 08-01 (the event fields that populate the tree).
This plan is NON-AUTONOMOUS: it ends with a human-verify checkpoint against the live demo harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-real-app-view-metrics/08-CONTEXT.md
@.planning/phases/08-live-real-app-view-metrics/08-PATTERNS.md
@.planning/phases/08-live-real-app-view-metrics/08-UI-SPEC.md

# Reuse analogs (read at the verified line ranges before editing)
@frontend/src/hooks/use-thread-activity.ts
@frontend/src/components/DispatcherOverview.tsx
@frontend/src/components/StateIndicator.tsx
@frontend/src/components/EmptyState.tsx
@frontend/src/components/SessionDetails.tsx
@frontend/src/lib/api-client.ts

# The API shape this FE consumes (from 08-02)
@.planning/phases/08-live-real-app-view-metrics/08-02-SUMMARY.md
@backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/RouteDtos.kt
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan:
- `MetricsResponse`, `LeakDto` (new TS interfaces in types/api.ts mirroring the 08-02 backend DTOs)
- `apiClient.getMetrics(sessionId)` (new method)
- `useSessionMetrics` (new hook)
- `SessionMetrics` (new component), `LeakList` (new component)
- active-only constants/derivations in SessionDetails (NODE_CAP, active/completed/shown/moreCount)
- new test files: use-session-metrics.test.ts, SessionMetrics.test.tsx, LeakList.test.tsx, SessionDetails.test.tsx additions
Modified (not new): SessionDetails.tsx (active-only filter + metrics mount). use-event-stream.ts UNCHANGED.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MetricsResponse types + getMetrics + useSessionMetrics poll-while-live hook</name>
  <files>
    frontend/src/types/api.ts,
    frontend/src/lib/api-client.ts,
    frontend/src/hooks/use-session-metrics.ts,
    frontend/src/hooks/use-session-metrics.test.ts
  </files>
  <read_first>
    use-thread-activity.ts lines 27-48 (the poll-while-live useQuery — copy queryKey/enabled/refetchInterval/staleTime semantics EXACTLY);
    api-client.ts around lines 189-201 (getThreadActivity / getHierarchy fetchJson methods to mirror);
    types/api.ts (existing *Response interface convention, e.g. ThreadActivityResponse line 529) to match for MetricsResponse/LeakDto;
    08-PATTERNS.md "use-session-metrics.ts" section
  </read_first>
  <behavior>
    - useSessionMetrics(sessionId, isLive, enabled) returns a useQuery with queryKey ['session-metrics', sessionId], queryFn apiClient.getMetrics(sessionId!), enabled !!sessionId && enabled, refetchInterval enabled ? (isLive ? 5000 : 2000) : false, staleTime 1000 — identical cadence to useThreadActivity.
    - apiClient.getMetrics returns the MetricsResponse shape {active, peak, throughputPerSec, dispatcherUtilization, leaks, leakThresholdMs}.
    - The hook is disabled (no fetch/poll) when sessionId is undefined or enabled=false (read-only view parity).
  </behavior>
  <action>
    Add MetricsResponse and LeakDto interfaces to types/api.ts mirroring the 08-02 backend DTOs exactly (active: number; peak: number; throughputPerSec: number; dispatcherUtilization: Record<string, number>; leaks: LeakDto[]; leakThresholdMs: number) and LeakDto (coroutineId: string; label: string | null; aliveMs: number). Add `async getMetrics(sessionId: string): Promise<MetricsResponse>` to api-client.ts mirroring getThreadActivity (08-PATTERNS.md lines 320-324): `return this.fetchJson<MetricsResponse>(\`/sessions/${encodeURIComponent(sessionId)}/metrics\`)`. Create use-session-metrics.ts cloning useThreadActivity's query (08-PATTERNS.md lines 305-317) with the session-metrics key. Write use-session-metrics.test.ts asserting the query is disabled without sessionId and uses the live (5000) vs non-live (2000) refetch interval (mock apiClient.getMetrics).
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run use-session-metrics</automated>
  </verify>
  <done>use-session-metrics.test.ts passes; MetricsResponse/LeakDto exported; getMetrics present; `pnpm tsc --noEmit` (or build typecheck) clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SessionMetrics panel + LeakList (reuse DispatcherOverview/StateIndicator; literal Tailwind)</name>
  <files>
    frontend/src/components/SessionMetrics.tsx,
    frontend/src/components/LeakList.tsx,
    frontend/src/components/SessionMetrics.test.tsx,
    frontend/src/components/LeakList.test.tsx
  </files>
  <read_first>
    DispatcherOverview.tsx lines 21-55 (loading/empty/Card-grid scaffold, md:grid-cols-2), lines 57-68 (the LITERAL tileClasses Record<color,string> map — MANDATORY, never template-construct bg-${color}/10, IN-12), lines 117-138 (the warning queue-depth tile + singular/plural pattern), line 128 (metric numeral text-lg font-bold — the one sanctioned font-bold exception);
    StateIndicator.tsx lines 9-50 (Chip + getStateConfig — reuse directly for state chips, do NOT re-derive colors);
    EmptyState.tsx (title/description/action props for "No live coroutines yet");
    08-UI-SPEC.md Copywriting Contract (exact copy for every label/badge/tooltip/empty state) + Color (leaks = warning, danger reserved for FAILED) + Typography (4 sizes / text-lg metric numerals);
    08-PATTERNS.md "SessionMetrics.tsx" + "LeakList.tsx" sections
  </read_first>
  <behavior>
    - SessionMetrics consumes useSessionMetrics(sessionId, isLive) and renders a Card grid (md:grid-cols-2): Active, Peak, Throughput (suffix "/s"; "—" em-dash when zero/no events), Dispatcher utilization, Potential leaks. Loading shows the DispatcherOverview-style loading Card; empty/no-data shows a sensible empty state.
    - Metric numerals use text-lg font-bold; labels text-sm; captions text-xs. Title "Session metrics" (text-lg sub-heading).
    - Active count uses the primary accent (per UI-SPEC accent reserved list); other metric values are neutral unless elevated.
    - Dispatcher utilization reuses DispatcherOverview's tile/`tileClasses` LITERAL class map (or feeds DispatcherOverview a derived DispatcherInfo from dispatcherUtilization) — no template-built classes.
    - LeakList renders one warning row per leak: "{coroutineName} · alive {duration}" with FiAlertCircle text-warning, bg-warning/10 border-warning/20 (literal). The badge reads "N potential leak{s}" (singular/plural per count). Tooltip: "Active longer than the leak threshold ({threshold}). May be a stuck or never-completing coroutine." Zero leaks → no badge / nominal state.
  </behavior>
  <action>
    Create SessionMetrics.tsx modelled on DispatcherOverview.tsx (08-PATTERNS.md lines 257-281): same loading/empty/grid structure, consuming useSessionMetrics instead of useThreadLanesByDispatcher. Render the metric Cards with the exact UI-SPEC copy (labels "Active"/"Peak"/"Throughput" + "/s"/"Dispatcher utilization"/"Potential leaks"; throughput "—" when zero). Use the LITERAL tileClasses Record map (DispatcherOverview lines 57-68) for any colored tile — Tailwind JIT requires complete class names (IN-12). Active value uses primary per the UI-SPEC accent reserved list; metric numerals text-lg font-bold (sanctioned exception). For dispatcher utilization, derive a DispatcherInfo[] from dispatcherUtilization and reuse DispatcherOverview's tile rendering or its card — planner's call per CONTEXT (graph grouping vs separate panel); a panel tile is the simplest match.
    Create LeakList.tsx modelled on the DispatcherOverview warning tile (08-PATTERNS.md lines 287-297): warning Chips/rows with the exact "{coroutineName} · alive {duration}" copy, FiAlertCircle text-warning, literal bg-warning/10 strings, the "N potential leak{s}" pluralized badge, and the Tooltip copy referencing {threshold} (from MetricsResponse.leakThresholdMs, formatted to s). Reuse <StateIndicator> directly for any coroutine state chip — never re-derive state colors (LOCKED contract; danger = FAILED only, leaks = warning). Format aliveMs/threshold to human duration (e.g. "42s").
    Write SessionMetrics.test.tsx (renders Active/Peak/Throughput from a mocked useSessionMetrics; "—" when throughput zero; loading state) and LeakList.test.tsx (renders "fetchUser · alive 42s"; "1 potential leak" singular vs "2 potential leaks" plural; warning not danger styling; empty when no leaks). Mock the hook/apiClient.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run SessionMetrics LeakList</automated>
  </verify>
  <done>SessionMetrics.test.tsx + LeakList.test.tsx pass; copy matches the UI-SPEC Copywriting Contract verbatim; only literal Tailwind class strings used; StateIndicator reused for state chips; leaks styled warning (not danger).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SessionDetails active-only view + "N more" cap + "Show completed" + metrics mount</name>
  <files>
    frontend/src/components/SessionDetails.tsx,
    frontend/src/components/SessionDetails.test.tsx
  </files>
  <read_first>
    SessionDetails.tsx lines 51-56 (TERMINAL_STATES — reuse), line 239 (panelCoroutines useMemo — the source to filter), lines 685-687 (the <CoroutineTreeGraph coroutines={panelCoroutines}/> and <CoroutineTree coroutines={panelCoroutines}/> render sites to swap), lines 724-726 (existing <DispatcherOverview isLive={streamEnabled}/> mount — place SessionMetrics adjacent), line 95/465-475 (streamEnabled state + the existing "Enable Live Stream" CTA);
    08-UI-SPEC.md Copywriting ("What's running now" section title, "Show completed (N)", "N more coroutines", empty-state copy);
    08-PATTERNS.md "SessionDetails.tsx" section (active/completed/shown/moreCount derivation, NODE_CAP tuning note A5); 08-RESEARCH.md Code Examples "Active-only FE filter"
  </read_first>
  <behavior>
    - The live view section is titled "What's running now" and defaults to active-only: `active = panelCoroutines.filter(c => !TERMINAL_STATES.has(c.state))`.
    - Rendered nodes are capped at NODE_CAP (~200, tuned against the demo); when active.length > cap, "N more coroutines" (text-xs) is shown.
    - `completedCount = panelCoroutines.length - active.length`; when > 0 a "Show completed (N)" toggle expands the completed aggregate.
    - CoroutineTreeGraph/CoroutineTree receive the capped active set (not panelCoroutines) by default.
    - <SessionMetrics sessionId={sessionId} isLive={streamEnabled} /> is mounted alongside the existing DispatcherOverview.
    - When there are no live coroutines yet, the EmptyState shows "No live coroutines yet" / the VizcoreClient.start(...) copy.
    - use-event-stream.ts is NOT modified.
  </behavior>
  <action>
    In SessionDetails.tsx derive the active-only set from panelCoroutines (line 239) reusing TERMINAL_STATES (lines 51-56) per 08-PATTERNS.md lines 338-348: const NODE_CAP = 200 (tune against the demo, A5); active/completedCount/shown/moreCount. Swap the two render sites (lines 685, 687) `coroutines={panelCoroutines}` → `coroutines={shown}`; add the "N more coroutines" indicator (text-xs) when moreCount > 0 and a "Show completed (N)" toggle (expandable aggregate) when completedCount > 0 — exact UI-SPEC copy. Title the section "What's running now". Mount `<SessionMetrics sessionId={sessionId} isLive={streamEnabled} />` adjacent to the existing DispatcherOverview (lines 724-726). Use EmptyState (no edit) for the "No live coroutines yet" case with the UI-SPEC body copy. Preserve all existing replay/SSE behavior — do NOT touch use-event-stream.ts, and keep the active-only default as a non-destructive view toggle (the live view is read-only observation; no confirmation, UI-SPEC). Keep the readOnly path working (pass enabled=!readOnly to useSessionMetrics like useThreadActivity does, SessionDetails line 121).
    Extend SessionDetails.test.tsx: assert active-only renders only non-terminal coroutines by default; "Show completed (N)" appears with the correct N and reveals completed on toggle; "N more coroutines" appears when active exceeds the cap; SessionMetrics mounts when streaming. Mock apiClient (not the hooks) where feasible to exercise the real pipeline, following the existing SessionDetails.test.tsx convention.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run SessionDetails</automated>
  </verify>
  <done>SessionDetails.test.tsx passes; active-only default + "Show completed (N)" + "N more coroutines" + "What's running now" present with UI-SPEC copy; SessionMetrics mounted; use-event-stream.ts unchanged (git diff shows no edit).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live demo verification — legible named tree + populated metrics</name>
  <action>Human-verify checkpoint — no code change. After Tasks 1-3 land and all automated tests are green, the executor pauses and the user runs the live demo harness (steps in how-to-verify) to confirm SC#1 (legible named hierarchical tree, not a flat wall) and SC#2 (populated metrics panel + leak surfacing) end-to-end before /gsd-verify-work.</action>
  <what-built>
    The full Phase-8 stack: 08-01 reconstructs the hierarchy + names + dispatcher grouping in core,
    08-02 exposes per-session metrics + leaks at GET /metrics, and this plan's FE renders the
    active-only "What's running now" view + the Session metrics panel + leak list. All automated
    tests (backend deterministic + FE Vitest) are green.
  </what-built>
  <how-to-verify>
    1. Start the backend: `docker compose up` (or `cd backend && ./gradlew run`) — listens on :8080.
    2. Start the instrumented demo against it: `cd examples/spring-vizcore-demo && ./gradlew bootRun`
       (streams its coroutines over the Phase-7 WS transport into a backend session).
    3. Open the frontend (`cd frontend && pnpm dev`, http://localhost:3000), open the demo's session,
       and click "Enable Live Stream".
    4. Confirm SC#1 (RCO-06): the live view shows "What's running now" as a HIERARCHICAL, NAMED tree
       (parent/child edges, CoroutineName labels, dispatcher/scope grouping) — NOT a flat anonymous
       ~1,800-card wall. Confirm "Show completed (N)" collapses completed and "N more coroutines"
       appears if the active set exceeds the cap.
    5. Confirm SC#2 (RCO-07): the "Session metrics" panel shows non-zero Active / Peak / Throughput
       (/s) / Dispatcher utilization. If the demo holds a long-lived coroutine past the leak threshold
       (~30s), the leak list shows "N potential leak{s}" in warning/amber with "{name} · alive {dur}".
    6. Confirm leaks are amber (warning), never red (danger), and the throughput shows "—" before any
       events arrive. Optionally hit `GET /api/sessions/{id}/metrics?leakThresholdMs=1000` and confirm
       the threshold override surfaces in the tooltip copy.
  </how-to-verify>
  <resume-signal>Type "approved" if the live view is a legible named tree with populated metrics + leak surfacing, or describe what is flat/empty/miscolored.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FE → GET /metrics | The FE reads metrics for the session it is authorized to view; Bearer attached at the api-client choke point (existing) |
| Read-only shared view → /metrics | The shared shell carries no Bearer; the metrics fetch/poll must be disabled there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | useSessionMetrics polls /metrics in the read-only shared view (no Bearer → 401/noise) | mitigate | Pass enabled=!readOnly to useSessionMetrics (mirrors useThreadActivity SessionDetails line 121); disabled query = no fetch/poll |
| T-08-09 | Information Disclosure | Cross-tenant metrics exposure | mitigate (server-side) | Enforced by 08-02's resolveScopedSession 404; FE attaches Bearer at the existing api-client choke point (no new auth surface here) |
| T-08-10 | Tampering | Template-constructed Tailwind classes (bg-${color}/10) render leak/metric tiles unstyled (silent visual failure) | mitigate | LITERAL tileClasses Record map only (IN-12); SessionMetrics/LeakList tests assert rendered classes |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (UI-SPEC Registry Safety: phase adds no UI dependency; all HeroUI primitives + existing components); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cd frontend && pnpm test` (full Vitest suite) green.
- `cd frontend && pnpm lint` (ESLint flat config) + typecheck clean.
- use-event-stream.ts unchanged (`git diff --stat frontend/src/hooks/use-event-stream.ts` empty).
- Live harness (Task 4 checkpoint): demo streams a legible named tree + populated metrics.
</verification>

<success_criteria>
- Active-only "What's running now" default with "Show completed (N)" + "N more" cap (RCO-06, D-08).
- Reconstructed named hierarchy renders in the tree/graph (RCO-06 FE surface).
- "Session metrics" panel + leak list fed by GET /metrics, poll-while-live (RCO-07 FE surface).
- Leaks styled warning (not danger); UI-SPEC copy verbatim; only literal Tailwind classes.
- use-event-stream.ts untouched; human-verify checkpoint passed against the live demo.
</success_criteria>

<output>
Create `.planning/phases/08-live-real-app-view-metrics/08-03-SUMMARY.md` when done.
</output>
