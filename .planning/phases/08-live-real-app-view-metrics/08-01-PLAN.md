---
phase: 08-live-real-app-view-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/HierarchyReconstructor.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineSnapshot.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineInfoAdapter.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesEventSynthesizer.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesSource.kt
  - backend/coroutine-viz-core/src/test/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/HierarchyReconstructorTest.kt
  - backend/coroutine-viz-core/src/test/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesEventSynthesizerTest.kt
autonomous: true
requirements: [RCO-06]
must_haves:
  truths:
    - "A real-app DebugProbes dump reconstructs a parent/child tree: an observed coroutine whose direct parent is an intermediate (unobserved) scope/async job links to its nearest OBSERVED ancestor."
    - "Synthesized CoroutineCreated events carry a non-null parentCoroutineId using the same dp-<token> id scheme as the parent coroutine's own id, so ProjectionService wires tree edges with zero downstream change."
    - "Synthesized events carry a derived scopeId (dispatcherName-based grouping) instead of the hardcoded \"debugprobes\" constant, so the FE can group by dispatcher/scope."
    - "Coroutine identity keys for parent and child are issued by the SAME adapter cache (jobKeys) — no second key scheme; edges connect."
  artifacts:
    - path: "backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/HierarchyReconstructor.kt"
      provides: "Pure Job.children inversion + nearest-observed-ancestor parent map (D-02, spike-proven)"
      contains: "nearestObservedParent"
      min_lines: 30
    - path: "backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineSnapshot.kt"
      provides: "parentKey field on CoroutineSnapshot"
      contains: "parentKey"
    - path: "backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineInfoAdapter.kt"
      provides: "batch toSnapshots(infos) that owns keying + parent linkage in one pass"
      contains: "fun toSnapshots"
  key_links:
    - from: "DebugProbesEventSynthesizer.contextFor"
      to: "EventContext.parentCoroutineId / scopeId"
      via: "snapshot.parentKey + snapshot.dispatcherName"
      pattern: "parentCoroutineId\\s*=\\s*snapshot\\.parentKey"
    - from: "CoroutineInfoAdapter.toSnapshots"
      to: "HierarchyReconstructor"
      via: "build nearestObservedParent over the whole dump, map parent Job -> same CoroKey"
      pattern: "toSnapshots"
---

<objective>
Reconstruct the coroutine parent/child hierarchy on the DebugProbes path and propagate
parentCoroutineId + a derived scopeId through the synthesizer so the EXISTING ProjectionService /
CoroutineTree / CoroutineTreeGraph populate for real-app sessions. This is the gating work for SC#1
(RCO-06): today the DebugProbes path emits flat, parentless, anonymous coroutines (a ~1,800-card
wall); after this plan the live view becomes a hierarchical, named, grouped tree.

Implements D-01 (full-fidelity tree + names + dispatcher/scope grouping), D-02 (spike-proven
nearest-observed-ancestor reconstruction), D-03 (replace hardcoded scopeId="debugprobes").

Purpose: Make the live data meaningful. The entire display layer already exists and is correct — it
has simply never received a non-null parentCoroutineId / meaningful scopeId from the DebugProbes
path (RESEARCH "Key insight"). The bulk of this is one-field propagation in core plus the batch
reconstruction step.
Output: A pure HierarchyReconstructor, a batch toSnapshots on the adapter, parentKey on the
snapshot, parent+scope propagation in the synthesizer, and the source wired to the batch path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-real-app-view-metrics/08-CONTEXT.md
@.planning/phases/08-live-real-app-view-metrics/08-PATTERNS.md
@.planning/phases/08-live-real-app-view-metrics/08-RESEARCH.md

# Spike proof of the reconstruction algorithm (D-02) — the algorithm to adapt into production
@examples/spring-vizcore-demo/src/main/kotlin/com/jh/vizcore/demo/spike/HierarchySpike.kt

# Edit targets (read at the verified line ranges before editing)
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineInfoAdapter.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineSnapshot.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesEventSynthesizer.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesSource.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/EventContext.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (drift verification excludes these as newly-created):
- `HierarchyReconstructor` (new class/object) + its `nearestObservedParent` / parent-map functions
- `CoroutineInfoAdapter.toSnapshots(infos)` (new batch public method)
- `CoroutineSnapshot.parentKey` (new optional field; optionally `scopeKey`/group token)
- `HierarchyReconstructorTest` (new test class)
Modified (not new): `DebugProbesEventSynthesizer.contextFor` (parentCoroutineId/scopeId now non-null),
`DebugProbesSource.dump` lambda (now calls the batch method), the synthesizer class doc-comment.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure HierarchyReconstructor + parentKey on CoroutineSnapshot</name>
  <files>
    backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/HierarchyReconstructor.kt,
    backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineSnapshot.kt,
    backend/coroutine-viz-core/src/test/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/HierarchyReconstructorTest.kt
  </files>
  <read_first>
    examples/spring-vizcore-demo/src/main/kotlin/com/jh/vizcore/demo/spike/HierarchySpike.kt (the spike-proven algorithm, D-02);
    backend/coroutine-viz-core/.../debugprobes/CoroutineSnapshot.kt lines 46-65 (the all-defaulted-optional field convention; CoroKey value type at lines 24-25);
    08-PATTERNS.md "HierarchyReconstructor.kt" section (purity contract, spike assertions withParent>=3 / named>=4)
  </read_first>
  <behavior>
    - Given a set of Job nodes where observed leaf jobs descend from intermediate UNOBSERVED scope/async jobs, nearestObservedParent climbs child->parent until it lands on an observed Job (or null at the root).
    - Direct-parent-observed is the rare case; the typical case recovers grandchild->child->parent across one or more unobserved intermediates (spike: direct-parent-observed 0/5, nearest-ancestor recovers the chain).
    - The reconstructor operates purely on kotlinx.coroutines.Job (a public type) — unit-testable with fake Job trees, NO real DebugProbes needed.
    - A coroutine with no observed ancestor maps to null parent (becomes a tree root).
  </behavior>
  <action>
    Create HierarchyReconstructor as a pure object/class operating on Job only (keeps it fakeable, mirroring the RawInfo fakeability invariant of CoroutineInfoAdapter). Adapt the spike algorithm verbatim (08-PATTERNS.md lines 110-124): given the observed Job set, invert Job.children across the WHOLE reachable set into a childToParent map (guard re-entry with a walked set to avoid cycles), then expose nearestObservedParent(job): Job? that climbs childToParent skipping non-observed jobs. Do NOT invent a coroutine key scheme here — return Job references; keying is the adapter's job in Task 2 (Open Q4 resolution: fold reconstruction into the adapter batch so keys stay consistent).
    Add `val parentKey: CoroKey? = null` to the CoroutineSnapshot data class following the existing all-defaulted-optional style (so no existing call site breaks). Reuse the existing CoroKey value type for the parent reference. Do NOT add a serialization annotation — CoroutineSnapshot is a pure internal value type, not a wire DTO.
    Write HierarchyReconstructorTest.kt (Wave 0 gap) using fake Job trees: assert nearest-observed-ancestor recovery across an unobserved intermediate, direct-parent case, and null-at-root. Model assertion strength on the spike (withParent>=3, named>=4 are the spike's bar; replicate equivalent structural assertions on the fake tree).
  </action>
  <verify>
    <automated>cd backend && ./gradlew :coroutine-viz-core:test --tests "*HierarchyReconstructor*"</automated>
  </verify>
  <done>HierarchyReconstructorTest passes; CoroutineSnapshot carries parentKey (defaulted null, no existing call site broken — module compiles).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batch toSnapshots on the adapter + wire the source's dump lambda</name>
  <files>
    backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/CoroutineInfoAdapter.kt,
    backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesSource.kt
  </files>
  <read_first>
    CoroutineInfoAdapter.kt lines 69-120 (private keyFor + jobKeys cache + the two toSnapshot overloads);
    DebugProbesSource.kt lines 62-71 (the injectable dump lambda) and lines 122-149 (pollTick — operates on List<CoroutineSnapshot> keyed by it.key, UNCHANGED);
    08-PATTERNS.md "CoroutineInfoAdapter.kt" + "DebugProbesSource.kt" sections (integration landmine: keyFor is private/identityHashCode-based; parent must resolve to the SAME CoroKey via the same jobKeys cache)
  </read_first>
  <behavior>
    - toSnapshots(infos) returns one CoroutineSnapshot per observed info, each carrying parentKey = the CoroKey of its nearest-observed-ancestor Job (or null).
    - Parent and child keys come from the SAME jobKeys cache (computeIfAbsent over System.identityHashCode(job)), so a parent Job resolves to the identical CoroKey the parent coroutine receives as its own key — edges connect.
    - The existing single-info toSnapshot path is preserved (unit tests keep passing); the batch method reuses it for field extraction and adds the parent pass.
  </behavior>
  <action>
    Add a public batch method `fun toSnapshots(infos: List<CoroutineInfo>): List<CoroutineSnapshot>` (and a fakeable RawInfo-based overload if needed for unit tests). One pass: collect the observed Job set from infos, build the HierarchyReconstructor parent map over those Jobs, then for each info produce the snapshot via the existing field-extraction path and set parentKey = nearestObservedParent(info.job)?.let { keyFor(it, ...) }. Because keyFor is computeIfAbsent over jobKeys (line 75), resolving the parent Job's key reuses the same token the parent coroutine's own snapshot gets — do NOT introduce a second key scheme (08-PATTERNS.md landmine). Keep keyFor private; the batch method is the only new public seam. For null-job parents (synthetic-key path), parentKey stays null — DebugProbes gives no reliable parent there.
    In DebugProbesSource.kt, replace the dump lambda body (lines 69-71) `DebugProbes.dumpCoroutinesInfo().map { adapter.toSnapshot(it) }` with `adapter.toSnapshots(DebugProbes.dumpCoroutinesInfo())`. Leave pollTick (lines 122-149) and the rest of the loop unchanged — it already consumes List<CoroutineSnapshot> keyed by it.key.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :coroutine-viz-core:test --tests "*CoroutineInfoAdapter*"</automated>
  </verify>
  <done>Existing CoroutineInfoAdapterTest still passes; toSnapshots present and public; DebugProbesSource compiles against the batch call; a parent-linkage unit case (added in the adapter test or reused from Task 1's fake tree) shows child.parentKey == parent.key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Propagate parentCoroutineId + derived scopeId through the synthesizer</name>
  <files>
    backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesEventSynthesizer.kt,
    backend/coroutine-viz-core/src/test/kotlin/com/jh/proj/coroutineviz/session/source/debugprobes/DebugProbesEventSynthesizerTest.kt
  </files>
  <read_first>
    DebugProbesEventSynthesizer.kt lines 22-58 (the class doc-comment locking parentCoroutineId=null/scopeId="debugprobes" for v1, AND the contextFor builder + coroutineId(snapshot) = "dp-${snapshot.key.token}" at line 43);
    EventContext.kt lines 32-63 (parentCoroutineId/scopeId are constructor fields threaded into coroutineCreated() and every builder);
    ProjectionService.kt lines 76-97 (the is CoroutineCreated branch reads event.parentCoroutineId and wires parent.copy(children=...) — the tree builds for free) and lines 225-235 (getHierarchyTree filters on it.scopeId);
    08-PATTERNS.md "DebugProbesEventSynthesizer.kt" section
  </read_first>
  <behavior>
    - contextFor sets parentCoroutineId = snapshot.parentKey?.let { "dp-${it.token}" } — the SAME dp- prefix as coroutineId(snapshot), so the child's parentCoroutineId equals the parent coroutine's own id.
    - contextFor sets scopeId = snapshot.dispatcherName ?: sourceId (D-03 / Open Q1 recommended default) — no longer a constant, so getHierarchyTree(scopeId) groups by dispatcher.
    - A synthesized Appeared/CoroutineCreated with a non-null parentKey produces a CoroutineCreated whose parentCoroutineId is non-null; with a null parentKey it stays null (root).
  </behavior>
  <action>
    Edit contextFor (lines 47-58): replace `parentCoroutineId = null` with `parentCoroutineId = snapshot.parentKey?.let { "dp-${it.token}" }` (D-01) and `scopeId = sourceId` with `scopeId = snapshot.dispatcherName ?: sourceId` (D-03, Open Q1 default = dispatcherName grouping). Key consistency is mandatory: coroutineId(snapshot) is "dp-${snapshot.key.token}" (line 43), so the parent reference MUST use the identical "dp-${parentKey.token}" form (08-PATTERNS.md key-consistency note). Update the class doc-comment (lines 22-25) which currently states parentCoroutineId=null / scopeId="debugprobes" as v1-locked — it must now describe the Phase-8 reconstructed-parent + dispatcher-grouping behavior.
    Extend DebugProbesEventSynthesizerTest.kt: assert an Appeared delta whose snapshot carries a parentKey yields a CoroutineCreated with parentCoroutineId == "dp-<parentToken>"; assert a snapshot with dispatcherName="Dispatchers.IO" yields scopeId == "Dispatchers.IO"; assert a null-parentKey snapshot yields null parentCoroutineId (root).
  </action>
  <verify>
    <automated>cd backend && ./gradlew :coroutine-viz-core:test --tests "*DebugProbesEventSynthesizer*" --tests "*ProjectionService*"</automated>
  </verify>
  <done>Synthesizer emits non-null parentCoroutineId (dp- prefixed) and dispatcher-derived scopeId; the existing ProjectionService test set still passes (proving CoroutineCreated-with-parent wires tree edges); the v1-locked doc-comment is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's real JVM app → DebugProbes dump | The app's coroutine graph (incl. creation stack traces) is read in-process; dev-only by constraint |
| Reconstructed events → backend session | Events flow over the Phase-7 WS ingest (already auth-scoped, UNCHANGED here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | Creation stack traces expose source file paths via scopeId/label/attribution | accept | Dev-only constraint already documented in DebugProbesSource (T-06-03); this plan adds no new prod surface — reconstruction runs in the same dev-only path |
| T-08-02 | Tampering | Mismatched parent/child keys could fabricate a wrong tree (broken edges) | mitigate | Parent and child keys issued by the SAME jobKeys cache via keyFor (no second scheme); HierarchyReconstructorTest asserts edge correctness on fake trees |
| T-08-03 | Denial of Service | Job.children walk over a huge dump (unbounded recursion/cycle) | mitigate | `walked` HashSet guards re-entry (spike pattern); the dump is the live coroutine set, already bounded by the running app + EventStore cap downstream |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (additive Kotlin edits over existing kotlinx.coroutines.debug, already in BOM); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cd backend && ./gradlew :coroutine-viz-core:test` (deterministic; excludes @Tag("integration")) green.
- `cd examples/spring-vizcore-demo && ./gradlew spike` still runs the reconstruction algorithm without regression (D-02 quick regression check).
- Lint gates: `cd backend && ./gradlew :coroutine-viz-core:ktlintCheck :coroutine-viz-core:detekt` green (JDK 21 — see MEMORY gotcha).
</verification>

<success_criteria>
- HierarchyReconstructor reconstructs nearest-observed-ancestor parent links over a full dump (RCO-06, D-02), unit-proven.
- The synthesizer emits CoroutineCreated with a non-null dp- prefixed parentCoroutineId and a dispatcherName-derived scopeId (D-01/D-03).
- ProjectionService builds tree edges from the propagated parentCoroutineId with ZERO downstream change (existing test set green).
- DebugProbesSource consumes the batch toSnapshots path; pollTick unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-live-real-app-view-metrics/08-01-SUMMARY.md` when done.
</output>
