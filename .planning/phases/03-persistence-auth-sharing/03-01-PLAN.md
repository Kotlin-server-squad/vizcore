---
phase: 03-persistence-auth-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/src/main/resources/application.yaml
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/DatabaseFactory.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/SessionsTable.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/EventsTable.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/SharesTable.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedSessionStore.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedEventStore.kt
  - backend/src/main/resources/db/migration/common/V1__core_schema.sql
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/persistence/ExposedSessionStoreTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/persistence/PersistenceRestartTest.kt
autonomous: true
requirements: [PERS-01, PERS-02]
must_haves:
  truths:
    - "With storage.type=database, creating a session writes a row to the sessions table"
    - "Events appended during a session are stored as JSON payload rows in the events table"
    - "A second store instance opened against the same H2 file/url reads back the prior sessions and events (survives restart)"
    - "Round-tripped DB events are byte-compatible with the SSE and /events wire shape (same PolymorphicSerializer)"
    - "With no storage config (default), the in-memory SessionManager is used and behavior is unchanged (D-04a)"
    - "coroutine-viz-core has NO Exposed/JDBC dependency added (SDK purity preserved)"
  artifacts:
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/DatabaseFactory.kt"
      provides: "HikariCP DataSource + Database.connect + Flyway.migrate"
      contains: "Flyway"
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedSessionStore.kt"
      provides: "SessionStoreInterface impl backed by Exposed"
      contains: "SessionStoreInterface"
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedEventStore.kt"
      provides: "EventStoreInterface impl, JSON payload via PolymorphicSerializer"
      contains: "EventStoreInterface"
    - path: "backend/src/main/resources/db/migration/common/V1__core_schema.sql"
      provides: "sessions, events, shares tables (portable DDL)"
      contains: "create table"
  key_links:
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt"
      to: "persistence/DatabaseFactory + ExposedSessionStore"
      via: "storage.type=database branch wires the Exposed store"
      pattern: "storage.type|DatabaseFactory"
    - from: "ExposedEventStore"
      to: "appJson + PolymorphicSerializer(VizEvent::class)"
      via: "reuse the SSE serializer for payload (de)serialization"
      pattern: "PolymorphicSerializer"
---

<objective>
Add an optional JDBC persistence layer behind the EXISTING `SessionStoreInterface`/`EventStoreInterface` seams, selectable via `storage.type=database`, so sessions and events survive a backend restart. Default (no config) stays in-memory — zero behavior change (D-04a).

Purpose: PERS-01 (config-selected Exposed store) and PERS-02 (durable, Flyway-migrated schema, JSON event payload) are the foundation every other Phase-3 capability builds on (sharing needs the DB; tenancy/retention need the store).
Output: `persistence/` package (DatabaseFactory, table objects, ExposedSessionStore, ExposedEventStore), the V1 Flyway migration (sessions + events + shares), store selection wired in `Application.module()`, and H2-backed integration + restart tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-auth-sharing/03-CONTEXT.md
@.planning/phases/03-persistence-auth-sharing/03-RESEARCH.md
@docs/adr/015-persistence-strategy.md

<read_first>
- backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionStoreInterface.kt — the REAL seam: `createSession(name): VizSession`, `getSession`, `listSessions(): List<SessionInfo>`, `deleteSession`, `clearAll`. NOT the ADR-015 sketch.
- backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/EventStoreInterface.kt — the REAL seam: `record(event)`, `all()`, `since(seq)`, `byCoroutine(id)`, `count(): Int`, `clear()`. Per-session (each VizSession owns a store).
- backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt — the in-memory impl; mirror its construction of VizSession and SessionInfo.
- backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt — VizSession(sessionId, maxEvents, createdAtMs); owns `store: EventStore`. Note: EventStore is constructed inside VizSession today.
- backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt — `toSse()` at the bottom uses `appJson.encodeToString(PolymorphicSerializer(VizEvent::class), event)`. Reuse this EXACT serializer for the DB payload column (Pattern 3) so round-tripped events are wire-identical.
- backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt — `module()`; store selection goes here after `SessionManager.configure(...)`.
- backend/build.gradle.kts — deps go on `:backend` ONLY (lines 26-47 block). coroutine-viz-core/build.gradle.kts MUST stay DB-free (SDK-purity landmine).
- backend/src/main/resources/application.yaml — add the `storage:` block following the existing `auth:`/`cors:`/`session:` env-interpolation convention.
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 tests + Exposed deps + Flyway V1 schema + DatabaseFactory</name>
  <files>backend/build.gradle.kts, backend/src/main/resources/application.yaml, backend/src/main/resources/db/migration/common/V1__core_schema.sql, backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/DatabaseFactory.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/SessionsTable.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/EventsTable.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/tables/SharesTable.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/persistence/ExposedSessionStoreTest.kt</files>
  <read_first>
backend/build.gradle.kts (dependencies block), backend/src/main/resources/application.yaml, RESEARCH.md "Standard Stack" + Pattern 2 (Flyway layout), ADR-015 schema section, ADR-019 shares DDL.
  </read_first>
  <behavior>
    - Test (ExposedSessionStoreTest, H2 in-memory `jdbc:h2:mem:...;DB_CLOSE_DELAY=-1`): DatabaseFactory.init runs Flyway and creates `sessions`, `events`, `shares` tables (assert via information_schema or a trivial insert/select).
    - Test: ExposedSessionStore.createSession persists a row; getSession reads it back; listSessions returns SessionInfo with correct eventCount; deleteSession removes the row; clearAll empties the table.
  </behavior>
  <action>
Add the Exposed 1.3.0 stack to `backend/build.gradle.kts` ONLY (per RESEARCH Standard Stack install block): `org.jetbrains.exposed:exposed-core:1.3.0`, `exposed-jdbc:1.3.0`, `exposed-json:1.3.0`, `exposed-kotlin-datetime:1.3.0`, `com.zaxxer:HikariCP:6.3.0`, `org.flywaydb:flyway-core:11.8.2`, `org.flywaydb:flyway-database-postgresql:11.8.2`, `org.postgresql:postgresql:42.7.7`, `com.h2database:h2:2.3.232`. Add h2 as test runtime too. Do NOT modify coroutine-viz-core/build.gradle.kts (SDK-purity landmine). Use the Exposed 1.x `org.jetbrains.exposed.v1.*` import packages — the ADR-015 snippets are Exposed 0.x and are stale; consult current Exposed 1.x docs via Context7 if imports are unclear.

Add a `storage:` block to application.yaml mirroring the env-interpolation style already used: `storage.type=${STORAGE_TYPE:memory}`, plus `storage.database.url/driver/username/password` (env-interpolated, default H2 file `jdbc:h2:file:./data/coroutineviz;AUTO_SERVER=TRUE`) and `storage.retention.maxAgeDays/maxEventsPerSession/cleanupIntervalMinutes` per ADR-015 (30 / 100000 / 60).

Write `db/migration/common/V1__core_schema.sql` with portable ANSI DDL for THREE tables (Plan 04 reuses `shares`; Plan 03 retention joins it): `sessions(id varchar(128) pk, name varchar(256), created_at timestamp not null, scenario varchar(256), tenant_id varchar(128) null, metadata clob)`; `events(id bigint auto_increment pk, session_id varchar(128) not null references sessions(id) on delete cascade, seq bigint not null, kind varchar(128) not null, ts_nanos bigint not null, payload clob not null)` with index on `(session_id, seq)`; `shares(token varchar(64) pk, session_id varchar(128) not null references sessions(id) on delete cascade, created_by varchar(128) not null, created_at timestamp not null, expires_at timestamp null, permission varchar(16) not null default 'READ_ONLY', access_count integer not null default 0, last_accessed_at timestamp null)` with indexes on `shares(session_id)` and `shares(expires_at)`. Use `JSON`-portable `clob` for payload/metadata (Pattern 2/3: H2 + PG both accept; GIN/JSONB indexing is NOT required by PERS-01..03 — document this tradeoff in the SUMMARY). Declare `tenant_id` now even though Plan 03 populates it, to avoid a second migration.

Create Exposed `Table` objects (`SessionsTable`, `EventsTable`, `SharesTable`) matching the DDL columns; use them in the store impls. Create `DatabaseFactory.init(config)`: build a HikariCP `HikariDataSource` (maximumPoolSize=10, minimumIdle=2, connectionTimeout=30s per ADR-015) from `storage.database.*`, call `Database.connect(dataSource)`, then `Flyway.configure().dataSource(ds).locations("classpath:db/migration/common").baselineOnMigrate(true).load().migrate()`. Return the `Database` handle. Never log the DB password.

Write `ExposedSessionStoreTest` against an H2 in-memory DB (covers PERS-01); use `transaction { }` for assertions. Tests are colocated under `backend/src/test/.../persistence/`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin test --tests "*ExposedSessionStoreTest" -i</automated>
  </verify>
  <done>Deps resolve; Flyway creates all three tables on an H2 store; ExposedSessionStore CRUD passes; coroutine-viz-core POM unchanged (no DB dep).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExposedSessionStore + ExposedEventStore impls, store selection, restart test</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedSessionStore.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/persistence/ExposedEventStore.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/persistence/PersistenceRestartTest.kt</files>
  <read_first>
SessionManager.kt (mirror createSession/listSessions/SessionInfo construction), VizSession.kt (store ownership), EventStoreInterface.kt, SessionRoutes.kt `toSse()` (serializer reuse), RESEARCH Pattern 1 + Pattern 3 + Open Question 3 (per-session ExposedEventStore).
  </read_first>
  <behavior>
    - Test (PersistenceRestartTest, H2 FILE db in a temp dir): create a session, append N VizEvents (use real subclasses), close the store; open a NEW DatabaseFactory+ExposedSessionStore against the SAME url; getSession + events read back identical content (PERS-02).
    - Test: a round-tripped event deserialized from the DB payload equals the original (PolymorphicSerializer wire-compatibility, Pattern 3).
  </behavior>
  <action>
Implement `ExposedSessionStore(db: Database)` satisfying `SessionStoreInterface` against `SessionsTable`. `createSession(name)` inserts a row (id = `${name}-${currentTimeMillis}` matching SessionManager's id scheme; tenant_id null for now — Plan 03 threads tenancy) and returns a `VizSession`. Because `EventStoreInterface` is per-session (Open Question 3 recommendation: keep the 1:1 model), give `ExposedSessionStore` a way to build sessions whose event store is an `ExposedEventStore(db, sessionId)` rather than the in-memory `EventStore` — do this with the smallest VizSession change possible (e.g. an injectable event-store factory / constructor overload on VizSession that defaults to the current in-memory EventStore so the SDK default is untouched). Document the chosen seam in the SUMMARY. `listSessions()` projects rows to `SessionInfo(sessionId, coroutineCount, eventCount)` (eventCount via `EventsTable.select { session_id eq id }.count()`). `getSession`/`deleteSession`/`clearAll` operate on the tables; deleteSession relies on the FK cascade for events/shares.

Implement `ExposedEventStore(db, sessionId)` satisfying `EventStoreInterface`: `record(event)` inserts `(session_id, seq, kind, ts_nanos, payload)` where payload = `appJson.encodeToString(PolymorphicSerializer(VizEvent::class), event)` (REUSE the exact `appJson` + serializer from the SSE path — Pattern 3); `all()`/`since(seq)`/`byCoroutine(id)` query the table ordered by seq and deserialize payloads with the same serializer; `count()` returns the row count; `clear()` deletes the session's events. Keep queries parameterized via the Exposed DSL (never string-concat — SQL-injection mitigation T-03-01).

Wire store selection in `Application.module()` after `SessionManager.configure(...)`: read `storage.type` (default `memory`); when `database`, call `DatabaseFactory.init(environment.config)` and install the Exposed store as the active `SessionStoreInterface` (introduce a single indirection the routes already use — today routes call `SessionManager` directly; introduce a `sessionStore` accessor / `SessionManager.useStore(...)` style indirection, OR keep `SessionManager` as the façade delegating to the configured backing store — choose the lower-blast-radius option and document it). When `memory` (default), behavior is byte-for-byte unchanged (D-04a). Do NOT force auth on when persistence is enabled (D-04b) — auth wiring is Plan 02's concern; this plan only touches storage.

Write `PersistenceRestartTest` (covers PERS-02) using an H2 file DB in a JUnit `@TempDir`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*ExposedSessionStoreTest" --tests "*PersistenceRestartTest" -i</automated>
  </verify>
  <done>storage.type=database selects the Exposed stores; sessions+events survive a simulated restart (new store, same url); event payloads round-trip identically; memory default unchanged; detekt/ktlint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API handler → JDBC store | Session/event ids and event payloads cross into SQL |
| Config → DataSource | DB credentials read from env-interpolated config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ExposedEventStore/ExposedSessionStore queries | mitigate | Exposed DSL parameterizes all queries; never string-concat SQL (sessionId/seq/payload bound as params) |
| T-03-02 | Information disclosure | DatabaseFactory credential handling | mitigate | DB password read from env-interpolated config; never logged; no secret in any `logger` call |
| T-03-03 | Tampering | Flyway migration integrity | mitigate | Versioned `V1__` migration with `baselineOnMigrate`; schema is reproducible, not ad-hoc `CREATE IF NOT EXISTS` |
| T-03-SC | Tampering | Exposed/Hikari/Flyway/PG/H2 installs | mitigate | All packages OK in RESEARCH Package Legitimacy Audit (Maven Central, mature, JetBrains/Redgate/official); no [ASSUMED]/[SUS] — no checkpoint required |
</threat_model>

<verification>
- `cd backend && ./gradlew test` green (full backend suite).
- `cd backend && ./gradlew detekt ktlintCheck` clean.
- Confirm `backend/coroutine-viz-core/build.gradle.kts` has NO Exposed/JDBC/Hikari line (SDK purity — SDK-01 guard).
- Default run (no STORAGE_TYPE) still serves `/api/sessions` from memory (existing AuthTest "no key configured" still passes).
</verification>

<success_criteria>
- PERS-01: `storage.type=database` binds ExposedSessionStore/ExposedEventStore behind the seam; CRUD works on H2.
- PERS-02: a new store instance on the same H2 file reads prior sessions+events; payloads are PolymorphicSerializer-compatible with SSE/`/events`.
- D-04a preserved: in-memory remains the default with unchanged behavior.
- SDK purity: coroutine-viz-core gains no DB dependency.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-auth-sharing/03-01-SUMMARY.md` when done. Record: the VizSession event-store-factory seam chosen, the SessionStore indirection chosen, and the JSON-vs-JSONB portability tradeoff.
</output>

## Artifacts this plan produces
- Package: `com.jh.proj.coroutineviz.persistence`
- Classes: `DatabaseFactory` (`init(config): Database`), `ExposedSessionStore : SessionStoreInterface`, `ExposedEventStore : EventStoreInterface`, `SessionsTable`, `EventsTable`, `SharesTable`
- Migration: `db/migration/common/V1__core_schema.sql` (sessions, events, shares)
- Config keys: `storage.type`, `storage.database.{url,driver,username,password}`, `storage.retention.{maxAgeDays,maxEventsPerSession,cleanupIntervalMinutes}`
- Env vars: `STORAGE_TYPE`, `DB_USER`, `DB_PASSWORD`, `DB_URL`
- Gradle coordinates added (`:backend` only): exposed-core/jdbc/json/kotlin-datetime 1.3.0, HikariCP 6.3.0, flyway-core + flyway-database-postgresql 11.8.2, postgresql 42.7.7, h2 2.3.232
- VizSession change: injectable event-store factory (default = in-memory EventStore)
- Application.module change: `storage.type` store-selection branch
- Tests: `ExposedSessionStoreTest`, `PersistenceRestartTest`
