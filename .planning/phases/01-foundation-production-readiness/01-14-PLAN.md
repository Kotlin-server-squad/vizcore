---
phase: 01-foundation-production-readiness
plan: 14
type: execute
wave: 1
depends_on: ["01-13"]
gap_closure: true
autonomous: true
requirements: [FIX-02]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/lib/thread-lanes.ts
  - frontend/src/lib/thread-lanes.test.ts
  - frontend/src/lib/api-client.ts
  - frontend/src/hooks/use-thread-activity.ts
  - frontend/src/hooks/use-thread-activity.test.ts
  - frontend/src/components/DispatcherOverview.tsx
  - frontend/src/components/SessionDetails.tsx
  - frontend/src/components/SessionDetails.test.tsx
  - frontend/src/mocks/mock-data.ts
  - frontend/src/mocks/handlers.ts
must_haves:
  truths:
    - "Threads tab renders live thread-lane data (thread names, ASSIGNED/RELEASED events) when GET /api/sessions/{id}/threads returns the real Map<threadId, ThreadEvent[]> wire shape"
    - "Dispatcher overview renders dispatcher cards derived from the real Map wire shape (no permanent 'No dispatcher data available')"
    - "No 'as unknown as' double cast remains in SessionDetails.tsx — the client type IS the wire type"
    - "Frontend tests and MSW mocks exercise the real wire shape, not the fictional {threads, dispatcherInfo} shape"
  artifacts:
    - path: "frontend/src/lib/thread-lanes.ts"
      provides: "Pure adapter buildThreadLanes(activity: ThreadActivity): ThreadActivityResponse deriving lanes + dispatcherInfo from the wire shape"
      exports: ["buildThreadLanes"]
      min_lines: 40
    - path: "frontend/src/lib/api-client.ts"
      provides: "getThreadActivity typed to the actual wire shape"
      contains: "Promise<ThreadActivity>"
    - path: "frontend/src/hooks/use-thread-activity.ts"
      provides: "Hooks rewritten against the wire shape via the adapter"
      contains: "buildThreadLanes"
    - path: "frontend/src/lib/thread-lanes.test.ts"
      provides: "Value-asserting unit tests for segment pairing, utilization math, dispatcher grouping"
  key_links:
    - from: "frontend/src/lib/api-client.ts"
      to: "frontend/src/types/api.ts ThreadActivity"
      via: "getThreadActivity return type"
      pattern: "getThreadActivity.*Promise<ThreadActivity>"
    - from: "frontend/src/hooks/use-thread-activity.ts"
      to: "frontend/src/lib/thread-lanes.ts"
      via: "buildThreadLanes import"
      pattern: "import.*buildThreadLanes.*thread-lanes"
    - from: "frontend/src/components/SessionDetails.tsx"
      to: "ThreadTimeline"
      via: "direct prop pass without cast"
      pattern: "ThreadTimeline threadActivity=\\{threadActivity\\}"
---

<objective>
Close UAT gap 1 (01-UAT.md, severity major): "Threads tab renders live thread-lane data during and after a scenario run" — currently failed because `GET /api/sessions/{id}/threads` returns `Map<String, List<ThreadEvent>>` (e.g. `{"57":[…],"80":[…]}`) while the frontend types declare a fictional `ThreadActivityResponse {threads, dispatcherInfo}` that the backend never produces. `data.threads`/`data.dispatcherInfo` are always undefined, three hooks and `DispatcherOverview` are permanently no-op, and `SessionDetails.tsx:408` hides the mismatch with an `as unknown as` double cast (REVIEW.md critical CR-02).

Resolution direction (per CR-02 recommendation and UAT `missing` item): align the FRONTEND to the real wire shape. The backend Map shape is the single source of truth — `ThreadTimeline` already consumes it correctly, and changing the backend would break that working consumer. A pure adapter derives the lane/dispatcher view model from the wire shape so the derived-view consumers (`DispatcherOverview`, `ThreadLanesView`, utilization hooks) become functional instead of silently dead.

Purpose: the Threads tab (and its dispatcher overview) shows real data in the browser, and the type system reflects the actual API contract so this class of mismatch fails compilation/tests instead of shipping green.
Output: retyped client, wire-shape-driven hooks via a new `buildThreadLanes` adapter, double cast removed, MSW mock fixed to the wire shape, value-asserting tests on the live shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-UAT.md
@.planning/phases/01-foundation-production-readiness/01-REVIEW.md

Wire-shape ground truth (backend, read-only — do NOT modify):
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt
@backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/models/ThreadEvent.kt
</context>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `buildThreadLanes(activity: ThreadActivity): ThreadActivityResponse` | exported pure function (new) | `frontend/src/lib/thread-lanes.ts` |
| `ThreadEvent.dispatcherName?: string \| null` | new optional field on existing interface | `frontend/src/types/api.ts` |
| `getThreadActivity(sessionId): Promise<ThreadActivity>` | changed return type (was `Promise<ThreadActivityResponse>`) | `frontend/src/lib/api-client.ts` |
| `useThreadActivity` returning `ThreadActivity` (wire shape) | changed hook return type | `frontend/src/hooks/use-thread-activity.ts` |
| `useThreadLanesByDispatcher`, `useActiveCoroutinesPerThread` rewritten over `buildThreadLanes` | changed hook internals (same external API for lanes-by-dispatcher) | `frontend/src/hooks/use-thread-activity.ts` |
| `generateMockThreadActivityWire(...): ThreadActivity` | new mock generator (wire shape) | `frontend/src/mocks/mock-data.ts` |
| `thread-lanes.test.ts` | new test file | `frontend/src/lib/thread-lanes.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire-shape types + buildThreadLanes adapter with value-asserting unit tests</name>
  <files>frontend/src/types/api.ts, frontend/src/lib/thread-lanes.ts, frontend/src/lib/thread-lanes.test.ts</files>
  <read_first>
    - frontend/src/types/api.ts (lines 450-565: ThreadEvent, ThreadActivity, ThreadActivityResponse, ThreadLaneData, ThreadSegment, DispatcherInfo — the existing type vocabulary this task extends)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/models/ThreadEvent.kt (wire field set: coroutineId, threadId: Long, threadName, timestamp nanos, eventType "ASSIGNED"|"RELEASED", dispatcherName: String? = null)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt (lines 234-238: getThreadActivity returns Map<String, List<ThreadEvent>> sorted by timestamp — key is threadId.toString())
    - frontend/src/lib/utils.ts (existing pure-helper + colocated-test conventions in src/lib)
  </read_first>
  <behavior>
    For fixture `{"57": [ASSIGNED c-1 @1000 dispatcherName "Default", RELEASED c-1 @5000 "Default"], "80": [ASSIGNED c-2 @2000 dispatcherName "IO"]}` (timestamps in nanos, threadName "worker-1"/"worker-2"):
    - Test 1 (segment pairing): thread 57 yields exactly one segment `{coroutineId: 'c-1', startNanos: 1000, endNanos: 5000, state: 'ACTIVE'}`; thread 80 yields one OPEN segment `{coroutineId: 'c-2', startNanos: 2000, endNanos: null, state: 'ACTIVE'}`.
    - Test 2 (utilization math): session span = 5000 − 1000 = 4000; thread 57 busy = 4000 → utilization exactly 1; thread 80 busy = (5000 − 2000) = 3000 → utilization exactly 0.75 (open segments are closed at the global max timestamp for the busy calculation).
    - Test 3 (dispatcher grouping): dispatcherInfo contains exactly two entries — `{id: 'Default', name: 'Default', threadIds: [57], queueDepth: null}` and `{id: 'IO', name: 'IO', threadIds: [80], queueDepth: null}`.
    - Test 4 (lane metadata): lane for key "57" has `threadId: 57` (number), `threadName: 'worker-1'`, `dispatcherName: 'Default'`.
    - Test 5 (degenerate inputs): empty map → `{threads: [], dispatcherInfo: []}`; a thread whose events all lack dispatcherName groups under name 'Unknown'; a single-event map (span 0) → utilization 0, no division-by-zero NaN.
    - Test 6 (unmatched RELEASED): a RELEASED event with no prior ASSIGNED for that coroutineId on that thread produces no segment and does not throw.
    Assertions must compare exact values (toEqual on full objects / toBe on numbers) — NOT presence-of-name checks. A name-presence assertion let a previous defect ship green (see 01-VERIFICATION.md history).
  </behavior>
  <action>
    In frontend/src/types/api.ts: add optional field `dispatcherName?: string | null` to the existing `ThreadEvent` interface (line ~451) to match the backend model (ThreadEvent.kt declares `dispatcherName: String? = null`). Update the comment block above `ThreadActivityResponse` (line ~536) to state it is a DERIVED view model produced by `buildThreadLanes` in `src/lib/thread-lanes.ts`, not a wire shape — the wire shape for GET /sessions/{id}/threads is `ThreadActivity` (the map at line ~459). Do not rename or delete `ThreadActivityResponse`, `ThreadLaneData`, `ThreadSegment`, or `DispatcherInfo` — they remain the derived-view vocabulary.

    Create frontend/src/lib/thread-lanes.ts exporting a single pure function `buildThreadLanes(activity: ThreadActivity): ThreadActivityResponse`. Algorithm (write the failing tests in thread-lanes.test.ts first, then implement):
    - For each map entry `[threadIdStr, events]`: sort events by timestamp ascending (backend already sorts, but do not rely on it). Pair segments per coroutineId: an ASSIGNED event opens a segment `{coroutineId, coroutineName: null, startNanos: event.timestamp, endNanos: null, state: 'ACTIVE'}`; the next RELEASED event with the same coroutineId on the same thread sets that segment's endNanos. RELEASED with no open segment for that coroutineId is ignored. Segments left open keep `endNanos: null`.
    - Compute global span across ALL events in the map: `span = maxTimestamp - minTimestamp`. Per-lane utilization: `busy = sum((endNanos ?? maxTimestamp) - startNanos)` over the lane's segments; `utilization = span > 0 ? Math.min(busy / span, 1) : 0`.
    - Lane fields: `threadId: Number(threadIdStr)`, `threadName: events[0]?.threadName ?? 'Thread ' + threadIdStr`, `dispatcherName` = the dispatcherName of the last event on that thread carrying a non-null dispatcherName, else null; `dispatcherId` = same value as dispatcherName (the wire carries no separate dispatcher id).
    - dispatcherInfo: group lanes by their resolved dispatcherName (null → 'Unknown'); one `DispatcherInfo` per group: `{id: name, name, threadIds: ascending numeric thread ids, queueDepth: null}`.
    No React imports — this module must stay pure (testable without renderHook).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/lib/thread-lanes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/thread-lanes.ts` exists and exports `buildThreadLanes`; `grep -c "from 'react'" frontend/src/lib/thread-lanes.ts` outputs 0 (pure module).
    - `frontend/src/types/api.ts` contains `dispatcherName?: string | null` inside the `ThreadEvent` interface.
    - All six behaviors above pass as exact-value assertions: `pnpm exec vitest run src/lib/thread-lanes.test.ts` exits 0 with >= 6 passing tests.
    - Utilization assertions use exact numbers (`toBe(1)`, `toBe(0.75)`), not range checks.
  </acceptance_criteria>
  <done>buildThreadLanes converts the documented wire fixture into exact expected lanes/dispatcherInfo; degenerate inputs (empty map, span 0, unmatched RELEASED) handled without NaN or throw.</done>
</task>

<task type="auto">
  <name>Task 2: Retype client to the wire shape, rewrite consumers, remove the double cast, fix the MSW mock</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/hooks/use-thread-activity.ts, frontend/src/components/DispatcherOverview.tsx, frontend/src/components/SessionDetails.tsx, frontend/src/mocks/mock-data.ts, frontend/src/mocks/handlers.ts</files>
  <read_first>
    - frontend/src/lib/api-client.ts (lines 1-20 imports; lines 134-136 getThreadActivity)
    - frontend/src/hooks/use-thread-activity.ts (all four hooks — current consumers of the fictional shape)
    - frontend/src/components/DispatcherOverview.tsx (line 11 reads `activity.dispatcherInfo` directly — breaks when useThreadActivity is retyped)
    - frontend/src/components/SessionDetails.tsx (lines 404-424 Threads tab; line 408 double cast; line 59 useThreadActivity call site)
    - frontend/src/components/ThreadLanesView.tsx (consumer of useThreadLanesByDispatcher/useThreadUtilizationStats — its external contract must keep compiling UNCHANGED; do not modify this file)
    - frontend/src/components/ThreadTimeline.tsx (already consumes the wire-shape `ThreadActivity` prop — no change needed; confirms the wire type renders)
    - frontend/src/mocks/mock-data.ts (generateMockThreadActivity at line ~173 returns the fictional shape; scenarioData.threadActivity at line ~305/326)
    - frontend/src/mocks/handlers.ts (line ~190: /threads handler serves scenarioData.threadActivity)
    - frontend/src/lib/thread-lanes.ts (the adapter from Task 1)
  </read_first>
  <action>
    api-client.ts: change `getThreadActivity(sessionId: string): Promise<ThreadActivityResponse>` to return `Promise<ThreadActivity>` and fetch as `this.fetchJson<ThreadActivity>(...)` (same endpoint path `/sessions/${sessionId}/threads`). Replace the `ThreadActivityResponse` import with `ThreadActivity`.

    use-thread-activity.ts rewrite (per REVIEW CR-02 fix guidance):
    - `useThreadActivity(sessionId, isLive)`: unchanged queryKey `['thread-activity', sessionId]`, queryFn, refetchInterval, staleTime — only the inferred data type changes to `ThreadActivity` via the retyped client. Do NOT touch the isLive/refetchInterval logic (locked by plan 01-13's CR-01 fix).
    - `useThreadLanesByDispatcher(sessionId)`: derive `const lanes = useMemo(() => (activity ? buildThreadLanes(activity) : undefined), [activity])`, then group `lanes.threads` by `lanes.dispatcherInfo` exactly as the current code does with the (previously always-undefined) fields. External return contract unchanged: `{ ...query, data: Map<string, ThreadLaneData[]>, dispatcherInfo: DispatcherInfo[] }` — ThreadLanesView must compile without edits.
    - `useThreadUtilizationStats(activity: ThreadActivityResponse | undefined)`: keep the signature and internals as-is (it operates on the derived view model, which ThreadLanesView passes).
    - `useActiveCoroutinesPerThread(sessionId)`: rewrite over `buildThreadLanes`; a coroutine is active on a thread iff its segment has `endNanos == null`. DELETE the `Date.now() * 1_000_000` epoch-nanos comparison — it compares wall-clock epoch nanos against System.nanoTime() monotonic values, which is semantically meaningless (REVIEW CR-02, third bullet).
    - Import `buildThreadLanes` from `@/lib/thread-lanes` and `ThreadActivity` from `@/types/api`.

    DispatcherOverview.tsx: replace `const { data: activity, isLoading } = useThreadActivity(sessionId)` + `activity.dispatcherInfo` reads with `const { dispatcherInfo, isLoading } = useThreadLanesByDispatcher(sessionId)`; the empty-state guard becomes `dispatcherInfo.length === 0`. Rendering of DispatcherCard stays unchanged.

    SessionDetails.tsx line 408: replace `<ThreadTimeline threadActivity={threadActivity as unknown as import('@/types/api').ThreadActivity} />` with `<ThreadTimeline threadActivity={threadActivity} />` — the prop is now correctly typed end-to-end. Keep the surrounding `threadActivity ? ... : <Spinner/>` conditional and everything else in the file untouched (lines 28-157 contain plan 01-12/01-13 debounce logic — do not modify).

    mocks: in mock-data.ts add `generateMockThreadActivityWire(threadCount = 4, eventsPerThread = 4): ThreadActivity` producing the wire shape — keys are stringified thread ids, values are ThreadEvent arrays with paired ASSIGNED/RELEASED entries, monotonic nano timestamps, threadName `worker-{id}`, dispatcherName alternating 'Default'/'IO'. Point `scenarioData.threadActivity` (line ~305/326) at the wire generator and update its declared type; then grep `src/` for remaining consumers of `generateMockThreadActivity` and `scenarioData.threadActivity` and update any found to the wire shape. handlers.ts line ~190 keeps serving `scenarioData.threadActivity` verbatim — after this change the MSW handler serves the SAME shape the real backend serves (this is the regression trap that let CR-02 ship: the mock satisfied the fictional type). Keep the old `generateMockThreadLane` helper only if still referenced; delete it if orphaned.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && ! grep -q "as unknown as" src/components/SessionDetails.tsx && ! grep -q "ThreadActivityResponse" src/lib/api-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0 (strict mode — proves every consumer was migrated, including DispatcherOverview and ThreadLanesView).
    - `grep -q "as unknown as" frontend/src/components/SessionDetails.tsx` exits 1 (cast gone).
    - `grep -q "ThreadActivityResponse" frontend/src/lib/api-client.ts` exits 1 (client speaks the wire type only).
    - `grep -q "Date.now() \* 1_000_000" frontend/src/hooks/use-thread-activity.ts` exits 1 (epoch-vs-nanoTime comparison deleted).
    - `grep -c "buildThreadLanes" frontend/src/hooks/use-thread-activity.ts` outputs >= 2 (import + at least one use).
    - The MSW /threads handler response, when run through `buildThreadLanes`, yields non-empty threads — i.e. mock-data's wire generator produces `Record<string, ThreadEvent[]>` keyed by numeric-string ids.
  </acceptance_criteria>
  <done>Client, hooks, and components compile against the real wire shape; the double cast and the meaningless clock comparison are gone; MSW serves the same shape as the backend.</done>
</task>

<task type="auto">
  <name>Task 3: Value-asserting integration tests on the live wire shape</name>
  <files>frontend/src/hooks/use-thread-activity.test.ts, frontend/src/components/SessionDetails.test.tsx</files>
  <read_first>
    - frontend/src/hooks/use-thread-activity.test.ts (current fixtures use the fictional ThreadActivityResponse shape — must be rewritten, not appended to)
    - frontend/src/components/SessionDetails.test.tsx (existing apiClient mock setup, render helpers, tab interaction patterns — reuse them)
    - frontend/src/components/ThreadTimeline.tsx (lines 9-28: renders thread name, event count chips, eventType chips; empty state 'No thread activity data available yet' — the strings the integration test asserts on)
    - frontend/src/lib/thread-lanes.ts (Task 1 adapter — fixtures should reuse the same wire fixture style)
  </read_first>
  <action>
    Rewrite use-thread-activity.test.ts fixtures to the wire shape: `mockedApiClient.getThreadActivity` resolves `ThreadActivity` maps (e.g. `{'57': [...], '80': [...]}` with ThreadEvent objects exactly as the backend serializes them — fields coroutineId, threadId, threadName, timestamp, eventType, dispatcherName). Update tests:
    - `useThreadActivity` test: asserts `result.current.data` deep-equals the wire fixture (toEqual on the full map) and that getThreadActivity was called with the sessionId.
    - `useThreadLanesByDispatcher` test (new or rewritten): with the wire fixture, asserts the returned Map has exactly the expected dispatcher keys and that a specific lane carries exact derived values (threadId number, segment startNanos/endNanos, utilization as an exact number) — mirror Task 1's fixture math so expected values are computable by hand.
    - `useThreadUtilizationStats` tests: keep, but build their input via `buildThreadLanes(wireFixture)` instead of hand-written ThreadActivityResponse literals, asserting exact avg/max/min numbers.
    - `useActiveCoroutinesPerThread` test (new): a fixture with one open segment (ASSIGNED without RELEASED) and one closed segment asserts the result map contains exactly the open segment's coroutineId under its threadId, and nothing for the closed one.

    SessionDetails.test.tsx: add a test named to the effect of "Threads tab renders thread activity from the real Map wire shape (UAT gap 1)". Mock `apiClient.getThreadActivity` to resolve a two-thread wire map with threadName values 'worker-1'/'worker-2', one ASSIGNED+RELEASED pair and one ASSIGNED-only event. Render SessionDetails with the existing harness, activate the Threads tab (follow the file's existing tab-click pattern), then assert ALL of:
    1. `screen.queryByText('No thread activity data available yet')` is null (the UAT-observed permanent empty state is gone),
    2. thread names 'worker-1' and 'worker-2' are rendered,
    3. at least one 'ASSIGNED' chip text is rendered.
    This is the UAT-mandated "value-asserting integration test on the live shape": the mocked response is byte-for-byte the shape curl showed in UAT round 2, and the assertions check rendered VALUES, not component/test names.
  </action>
  <verify>
    <automated>cd frontend && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` (vitest run, full suite) exits 0 — no regressions in the other 28 test files.
    - `grep -q "ThreadActivityResponse" frontend/src/hooks/use-thread-activity.test.ts` exits 1 OR every remaining occurrence is constructed via `buildThreadLanes(...)` from a wire fixture (no hand-written fictional-shape literals fed into the client mock).
    - `grep -q "No thread activity data available yet" frontend/src/components/SessionDetails.test.tsx` exits 0 (the integration test asserts the empty state is absent).
    - SessionDetails.test.tsx's new test feeds `getThreadActivity` a plain object keyed by numeric-string thread ids (wire shape), not a `{threads, dispatcherInfo}` object.
  </acceptance_criteria>
  <done>The full frontend suite passes with every thread-activity fixture in the real wire shape; a SessionDetails integration test proves the Threads tab renders data (not the empty state) from the exact shape the live backend returns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → frontend renderer | JSON from /threads is parsed and rendered; shape is server-controlled but treated as untrusted input by the adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14-01 | Tampering/DoS | buildThreadLanes (malformed or empty /threads payload) | mitigate | Adapter tolerates empty maps, span-0, unmatched RELEASED without throw/NaN (Task 1 Test 5/6); render guarded by existing `threadActivity ?` conditional |
| T-01-14-02 | Information disclosure | Threads tab rendering | accept | Payload contains only coroutine/thread ids and names already shown elsewhere in the UI; no PII |
| T-01-14-SC | Tampering | npm installs | accept | No new dependencies installed by this plan |
</threat_model>

<verification>
1. `cd frontend && pnpm exec tsc --noEmit` — strict compile proves contract alignment end-to-end.
2. `cd frontend && pnpm test` — full suite (>= 255 existing tests + new ones) green.
3. `! grep -q "as unknown as" frontend/src/components/SessionDetails.tsx` — double cast removed.
4. `! grep -q "ThreadActivityResponse" frontend/src/lib/api-client.ts` — client speaks the wire type.
5. Human follow-up (UAT re-test, not in this plan): run a scenario in the browser; Threads tab shows thread lanes + dispatcher cards during and after the run.
</verification>

<success_criteria>
- UAT gap 1 truth achievable: with the real backend Map response, the Threads tab renders thread activity and DispatcherOverview renders dispatcher cards.
- The fictional shape can no longer ship green: MSW and test fixtures use the wire shape, and the client type matches the backend serializer output.
- ThreadLanesView compiles unchanged against the rewritten hooks (derived view model preserved).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-14-SUMMARY.md` when done.
</output>
