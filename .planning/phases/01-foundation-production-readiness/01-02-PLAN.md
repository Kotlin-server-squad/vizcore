---
phase: 01-foundation-production-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/components/validation/ValidationPanel.tsx
  - frontend/src/components/validation/TimingReportView.tsx
  - frontend/src/components/validation/ValidationResultCard.tsx
  - frontend/src/components/validation/ValidationPassCard.tsx
  - frontend/src/hooks/use-validation.ts
  - frontend/src/components/validation/ValidationPanel.test.tsx
autonomous: true
requirements: [FIX-02]
must_haves:
  truths:
    - "Run Validation renders the {sessionId, results[], timing} backend response without crashing the page (no error boundary)"
    - "Fail results render in a Failures section; Pass results render in a compact Passes list; no Warnings section"
    - "TimingReportView renders the real backend timing shape (coroutineDurations, suspensionDurations, totalDuration)"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "ValidationResponse / ValidationRuleResult / BackendTimingReport types matching the backend"
      contains: "ValidationResponse"
    - path: "frontend/src/components/validation/ValidationPanel.tsx"
      provides: "Adapted panel reading data.results filtered by type"
    - path: "frontend/src/components/validation/TimingReportView.tsx"
      provides: "Timing view rendering backend shape"
  key_links:
    - from: "frontend/src/components/validation/ValidationPanel.tsx"
      to: "data.results"
      via: "filter by r.type === 'Fail' / 'Pass'"
      pattern: "results\\.filter"
    - from: "frontend/src/components/validation/TimingReportView.tsx"
      to: "BackendTimingReport"
      via: "renders timing.totalDuration / coroutineDurations / suspensionDurations"
      pattern: "totalDuration"
---

<objective>
Adapt the frontend validation feature (FIX-02) to consume the REAL backend response shape `{sessionId, results[], timing}` instead of the never-implemented `{valid, errors, warnings}` shape it currently reads. The backend contract is unchanged (locked by D-02); only the frontend moves. This plan runs in parallel with the backend FIX wave (no shared files).

Purpose: Stop the validation page from crashing the whole app via the error boundary when Run Validation is clicked; render the 21-rule output correctly.
Output: Reconciled `api.ts` types, an adapted `ValidationPanel`, a fixed `TimingReportView`, remapped result cards, and an updated component test fixture.
</objective>

<artifacts_produced>
Symbols this phase creates/renames (exclude from drift verification):
- `ValidationResponse` interface (renamed from `ValidationResult`) — `api.ts`
- `ValidationRuleResult` interface (`{ type: 'Pass'|'Fail', ruleName, message, details? }`) — `api.ts`
- `BackendTimingReport` interface (`{ coroutineDurations, suspensionDurations, totalDuration }`) — `api.ts`
- New test anchor `data-testid="timing-coroutine-row"`
- Removed types: `ValidationError`, `ValidationWarning`, `TimingReport`, `LatencyBucket`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-UI-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile api.ts validation types to the backend contract (FIX-02)</name>
  <files>frontend/src/types/api.ts, frontend/src/hooks/use-validation.ts</files>
  <read_first>
    - frontend/src/types/api.ts (lines ~615-654: current `ValidationResult`, `ValidationError`, `ValidationWarning`, `TimingReport`, `LatencyBucket`)
    - frontend/src/hooks/use-validation.ts (return type annotation referencing the validation type)
    - .planning/phases/01-foundation-production-readiness/01-UI-SPEC.md (§Backend Contract: exact ValidationResponse / ValidationRuleResult / BackendTimingReport shapes)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§api.ts change: target TypeScript interfaces)
  </read_first>
  <action>
    In `api.ts`, replace the `ValidationResult` interface with `ValidationResponse { sessionId: string; results: ValidationRuleResult[]; timing: BackendTimingReport }`. Add `ValidationRuleResult { type: 'Pass' | 'Fail'; ruleName: string; message: string; details?: string }` (details present on Fail only). Add `BackendTimingReport { coroutineDurations: Record<string, number>; suspensionDurations: Record<string, number[]>; totalDuration: number }`. Remove the now-unused `ValidationError`, `ValidationWarning`, `TimingReport`, and `LatencyBucket` interfaces. Update `use-validation.ts` so its return type annotation references `ValidationResponse` (runtime logic unchanged — likely just the type import/annotation). Export `ValidationResponse` (the rename from `ValidationResult`) so consumers import the new name.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ValidationResponse' frontend/src/types/api.ts` ≥ 1.
    - `ValidationRuleResult` has a `type: 'Pass' | 'Fail'` union and a `ruleName` field.
    - `BackendTimingReport` has `coroutineDurations`, `suspensionDurations`, `totalDuration`.
    - `grep -c 'LatencyBucket\|ValidationWarning' frontend/src/types/api.ts` returns 0.
    - `pnpm exec tsc --noEmit` reports no errors in `api.ts` / `use-validation.ts` (component errors are fixed in Task 2).
  </acceptance_criteria>
  <done>api.ts exports the backend-matching validation types; the deprecated interfaces are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt ValidationPanel, result cards, and TimingReportView to the new shape (FIX-02)</name>
  <files>frontend/src/components/validation/ValidationPanel.tsx, frontend/src/components/validation/ValidationResultCard.tsx, frontend/src/components/validation/ValidationPassCard.tsx, frontend/src/components/validation/TimingReportView.tsx</files>
  <read_first>
    - frontend/src/components/validation/ValidationPanel.tsx (lines ~82-118: reads data.valid, data.errors, data.warnings; the testid anchors; the framer-motion animation blocks)
    - frontend/src/components/validation/ValidationResultCard.tsx (ValidationErrorCard + ValidationWarningCard; their prop interfaces)
    - frontend/src/components/validation/ValidationPassCard.tsx (current valid/errorCount/warningCount props)
    - frontend/src/components/validation/TimingReportView.tsx (current totalDurationNanos/eventCount/suspendResumeLatencies fields)
    - .planning/phases/01-foundation-production-readiness/01-UI-SPEC.md (§Component Adaptation Contract: summary card logic, Failures section, compact Passes list, removed Warnings section, TimingReportView two-section layout, formatMs helper, preserved/removed/new testid anchors, copywriting contract, color tokens)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (the adapted ValidationPanel results block and field-mapping tables)
  </read_first>
  <action>
    `ValidationPanel.tsx`: compute `failures = data.results.filter(r => r.type === 'Fail')` and `passes = data.results.filter(r => r.type === 'Pass')`. Summary card (`data-testid="validation-summary"`) shows "All Rules Passed" when `failures.length === 0` else "N Rule(s) Failed" with sub-label "N rules checked" / "N of M rules failed". Render a Failures section (heading `text-sm font-semibold text-danger` "Failures (N)") using `ValidationErrorCard` per Fail item. Render a compact Passes list (heading `text-sm font-semibold text-success` "Passes (N)") only when `passes.length > 0` — a `space-y-1` list of `text-xs text-default-500` rows each prefixed by `FiCheckCircle className="text-success"` (size 12), NOT individual cards. Remove the Warnings section entirely and all `ValidationWarningCard` references and all reads of `data.valid` / `data.errors` / `data.warnings`. Pass `data.timing` to `TimingReportView`. Preserve all existing framer-motion animations and the copy strings flagged "do NOT change" in the UI-SPEC. `text-warning` must not appear in the adapted panel.

    `ValidationResultCard.tsx` (`ValidationErrorCard`): change its prop interface from `ValidationError` to the Fail variant of `ValidationRuleResult`; map `ruleName` → danger Chip, `message` → `text-sm text-default-700`, `details` → `text-xs text-default-500` (only when present); drop `code`/`eventSeq`/`coroutineId` renders. Keep the `boxShadow` pulse animation. Leave `ValidationWarningCard` in the file (unused, not deleted) — do not update its interface.

    `ValidationPassCard.tsx`: change props from `valid/errorCount/warningCount` to `failCount: number` (derive `valid = failCount === 0`); drop `warningCount`. Apply the display text from the copywriting contract.

    `TimingReportView.tsx`: replace the four-stat grid with: (1) a Total Duration card (`text-lg font-semibold text-primary` value via `formatMs`, `text-xs text-default-500` "Total Duration"); (2) a "Coroutine Durations" section rendering `Object.entries(timing.coroutineDurations)` sorted descending by duration, one row each tagged `data-testid="timing-coroutine-row"`, reusing the existing `bg-default-100` track + `bg-primary` fill bar pattern; (3) a "Suspension Durations" section rendering per-coroutine `suspensionDurations` count + max. Add `formatMs(ms: number): string` (>= 1000 → "X.XXs", else "Xms") replacing `formatNanos`. Remove all references to `totalDurationNanos`, `eventCount`, `coroutineCount`, `avgEventIntervalNanos`, `maxGapNanos`, `suspendResumeLatencies`, and `LatencyBucket`. Keep the bar fill animation. Preserve `data-testid="timing-report"` on the section root.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && pnpm test -- --run ValidationPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data.valid\|data.errors\|data.warnings' frontend/src/components/validation/ValidationPanel.tsx` returns 0.
    - `grep -c 'results.filter' frontend/src/components/validation/ValidationPanel.tsx` ≥ 1.
    - `grep -c 'text-warning' frontend/src/components/validation/ValidationPanel.tsx` returns 0.
    - `TimingReportView.tsx` references `timing.totalDuration` and `data-testid="timing-coroutine-row"`; `grep -c 'totalDurationNanos\|suspendResumeLatencies' frontend/src/components/validation/TimingReportView.tsx` returns 0.
    - `ValidationPassCard.tsx` prop interface uses `failCount` and no `warningCount`.
    - `pnpm exec tsc --noEmit` is clean and `pnpm test -- --run ValidationPanel` passes.
  </acceptance_criteria>
  <done>The validation UI renders the real backend response without crashing, Failures/Passes/Timing sections match the UI-SPEC, and types compile.</done>
</task>

<task type="auto">
  <name>Task 3: Update ValidationPanel.test.tsx fixture to the real backend response shape (FIX-02)</name>
  <files>frontend/src/components/validation/ValidationPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/validation/ValidationPanel.test.tsx (lines ~1-80 keep: createWrapper, vi.mock('@/lib/api-client'), describe/it/beforeEach; lines ~61-74 old fixture; lines ~159-183 old TimingReportView test block)
    - .planning/phases/01-foundation-production-readiness/01-UI-SPEC.md (§Test contract: new fixture shape, preserved testid anchors, removed anchors validation-warning-card/latency-bucket, new anchor timing-coroutine-row)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (the new mock fixture example)
  </read_first>
  <action>
    Replace the old `{ valid, errors, warnings, timing: {totalDurationNanos,...} }` mock with the real `ValidationResponse` shape: `{ sessionId, results: [{type:'Fail', ruleName, message}, {type:'Pass', ruleName, message}], timing: { coroutineDurations: {...}, suspensionDurations: {...}, totalDuration: 230 } }`. Keep the `createWrapper()` helper, the `vi.mock('@/lib/api-client')` pattern, and the describe/it/beforeEach structure. Update assertions: preserve checks on `validation-panel`, `run-validation-btn`, `validation-empty`, `validation-summary`, `validation-error`, `validation-error-card`, `timing-report`; remove assertions referencing `validation-warning-card` and `latency-bucket`; add an assertion for `timing-coroutine-row` (one per `coroutineDurations` entry). Update the TimingReportView test block to the backend shape.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run ValidationPanel && pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - The fixture uses `results: [...]` with `type: 'Pass'|'Fail'` items and a `BackendTimingReport`-shaped `timing`.
    - `grep -c 'validation-warning-card\|latency-bucket' frontend/src/components/validation/ValidationPanel.test.tsx` returns 0.
    - `grep -c 'timing-coroutine-row' frontend/src/components/validation/ValidationPanel.test.tsx` ≥ 1.
    - `pnpm test -- --run` (full frontend suite) passes.
  </acceptance_criteria>
  <done>The component test feeds the real backend response shape and passes; the full frontend suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → frontend render | Untrusted/variable response shape rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service (client crash) | ValidationPanel render | mitigate | Reading the real `{results, timing}` shape with `filter` guards (no undefined `.length` access) stops the TypeError that triggers the app-wide error boundary; component test feeds the real shape |
| T-01-05 | Tampering | npm installs | mitigate | No new npm packages introduced; UI-SPEC Registry Safety confirms only existing @heroui/react + react-icons are used |
</threat_model>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean.
- `cd frontend && pnpm test -- --run` green.
- No `data.valid` / `data.errors` / `data.warnings` / `text-warning` in ValidationPanel.
</verification>

<success_criteria>
- FIX-02: Run Validation renders results without crashing; Failures/Passes/Timing sections match the UI-SPEC; full frontend suite green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-02-SUMMARY.md` when done.
</output>
