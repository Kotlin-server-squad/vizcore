---
phase: 01-foundation-production-readiness
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - backend/build.gradle.kts
  - backend/Dockerfile
  - docker-compose.prod.yml
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/Monitoring.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ScenarioRunnerRoutes.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/MetricsWiringTest.kt
autonomous: false
user_setup: []
requirements: [PROD-02, PROD-05]
must_haves:
  truths:
    - "The prod logging profile (logback-prod.xml) is selected via a JVM flag in the Docker/compose-prod entrypoint and the logstash encoder dependency is present so the container starts"
    - "All seven ADR-020 metrics are wired via Micrometer and present with non-default values after a scenario run, scraped at /metrics"
  artifacts:
    - path: "backend/build.gradle.kts"
      provides: "logstash-logback-encoder dependency (required by logback-prod.xml)"
      contains: "logstash-logback-encoder"
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt"
      provides: "All 7 ADR-020 metrics wired (2 existing gauges + 5 new)"
    - path: "backend/Dockerfile"
      provides: "Prod logback profile selected via -Dlogback.configurationFile"
      contains: "logback.configurationFile"
  key_links:
    - from: "backend/Dockerfile"
      to: "logback-prod.xml"
      via: "-Dlogback.configurationFile JVM flag in ENTRYPOINT"
      pattern: "logback.configurationFile"
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt"
      to: "VizSession.send"
      via: "onEventEmitted / onEventDropped callbacks increment counters"
      pattern: "events.emitted"
---

<objective>
Make the production logging profile actually selected and complete the ADR-020 metric set. PROD-02: add the missing `logstash-logback-encoder` dependency (the existing `logback-prod.xml` references `LogstashEncoder` but the dep is absent — the container would crash) and wire `-Dlogback.configurationFile` into the Docker/compose-prod entrypoint per D-06 (JVM flag, no janino). PROD-05: rename `/metrics-micrometer` to `/metrics` and wire the five missing ADR-020 metrics into the existing Micrometer setup alongside the Prometheus registry (D-07). Wave 3 — depends on Plan 03 because the metric callbacks attach to core `VizSession`/`SessionManager` after the de-fork.

Purpose: JSON prod logs that actually engage, and the full 7-metric observability surface that pre-positions Phase 4.
Output: build.gradle.kts dep, Dockerfile + compose-prod profile selection, extended MetricsWiring with 5 new metrics + callback plumbing, /metrics endpoint, extended MetricsWiringTest.
</objective>

<artifacts_produced>
Symbols this phase creates (exclude from drift verification):
- `logstash-logback-encoder:8.1` dependency — `build.gradle.kts`
- `-Dlogback.configurationFile` JVM flag + `logback-prod.xml` COPY — `Dockerfile`, `docker-compose.prod.yml`
- Metrics: `events.emitted` (Counter), `events.dropped` (Counter), `events.buffer.size` (Gauge), `scenario.duration` (Timer), `event.processing.duration` (Timer) — `MetricsWiring.kt`
- `onEventEmitted` / `onEventDropped` callbacks on core `VizSession` (and a session-created hook on `SessionManager` if needed) — core session classes
- `/metrics` endpoint (renamed from `/metrics-micrometer`) — `Monitoring.kt`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-RESEARCH.md
@.planning/phases/01-foundation-production-readiness/01-PATTERNS.md
@docs/adr/020-performance-scaling.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification — logstash-logback-encoder [ASSUMED]</name>
  <what-built>
    PROD-02 requires adding the Maven dependency `net.logstash.logback:logstash-logback-encoder:8.1` to `backend/build.gradle.kts` (the existing `logback-prod.xml` references `net.logstash.logback.encoder.LogstashEncoder`, which is otherwise absent and would crash the prod container with ClassNotFoundException). RESEARCH §Package Legitimacy Audit tags this package [ASSUMED] — version confirmed via Maven Central search but not from an authoritative doc reference.
  </what-built>
  <how-to-verify>
    1. Open https://central.sonatype.com/artifact/net.logstash.logback/logstash-logback-encoder (or https://mvnrepository.com/artifact/net.logstash.logback/logstash-logback-encoder).
    2. Confirm the artifact is published by `net.logstash.logback` (source repo github.com/logfellow/logstash-logback-encoder), is widely used (industry-standard JSON logging encoder, used by Spring Boot), and that version `8.1` (or the current stable) exists.
    3. Confirm the group/artifact coordinates exactly match `net.logstash.logback:logstash-logback-encoder` (no typosquat).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or provide a corrected coordinate/version.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add logstash dependency + wire prod logback profile in Docker/compose (PROD-02)</name>
  <files>backend/build.gradle.kts, backend/Dockerfile, docker-compose.prod.yml</files>
  <read_first>
    - backend/build.gradle.kts (dependencies block — where to add the implementation dep)
    - backend/Dockerfile (multi-stage build; the runtime-stage ENTRYPOINT; how resources/jar are assembled)
    - docker-compose.prod.yml (backend service entrypoint/command + healthcheck)
    - backend/src/main/resources/logback-prod.xml (references LogstashEncoder; confirm it is correct and exists)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§PROD-02: dep coordinate, ENTRYPOINT flag, the filesystem-path-vs-classpath copy note — copy logback-prod.xml to /app/logback-prod.xml in the runtime stage; Pitfall 6)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§Dockerfile change: three concrete edits)
  </read_first>
  <action>
    Add `implementation("net.logstash.logback:logstash-logback-encoder:8.1")` (or the version approved in Task 1) to `backend/build.gradle.kts` dependencies. In `backend/Dockerfile` runtime stage: copy the prod logback config to a filesystem path (`COPY --from=build /app/src/main/resources/logback-prod.xml /app/logback-prod.xml` — adjust paths to the actual build layout) and set the ENTRYPOINT to include `-Dlogback.configurationFile=/app/logback-prod.xml` (D-06 JVM flag; `-Dlogback.configurationFile` needs a filesystem path, not a classpath resource). In `docker-compose.prod.yml`, ensure the backend service uses this entrypoint/flag (override entrypoint/command or environment as appropriate). Dev runs are unchanged (default `logback.xml`). No stray `println` introduced.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin && grep -q 'logstash-logback-encoder' build.gradle.kts && grep -q 'logback.configurationFile' Dockerfile && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'logstash-logback-encoder' backend/build.gradle.kts` ≥ 1.
    - `grep -c 'logback.configurationFile' backend/Dockerfile` ≥ 1 and the Dockerfile copies `logback-prod.xml` to a filesystem path.
    - `docker-compose.prod.yml` selects the prod logback profile (flag present in the backend service config).
    - `./gradlew compileKotlin` succeeds (dependency resolves).
  </acceptance_criteria>
  <done>The logstash dependency is present and the prod logback profile is selected via JVM flag in the container entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 5 missing ADR-020 metrics + rename /metrics endpoint (PROD-05)</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/Monitoring.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt, backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt, backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ScenarioRunnerRoutes.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/MetricsWiringTest.kt</files>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt (existing wireMetrics(registry): 2 gauges viz.sessions.active + viz.sse.clients.active; Gauge.builder pattern lines 15-22)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/Monitoring.kt (PrometheusMeterRegistry instantiated ~line 11; /metrics-micrometer route ~lines 19-23; wireMetrics call ~line 17)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt (send() — where events are stored/broadcast; the existing onEvict-style callback pattern on EventStore)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/EventStore.kt (onEvict callback used to increment events.dropped; count() for buffer-size gauge)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt (configure(); any onSessionCreated hook to attach per-session gauges)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ScenarioRunnerRoutes.kt (the scenario run call to wrap with a Timer.Sample for scenario.duration)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/MetricsWiringTest.kt (existing metrics test to extend for the 5 new names)
    - docs/adr/020-performance-scaling.md (the canonical 7-metric names + types)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§PROD-05 metric table + wiring points; Open Question 1: thread counter increments via onEventEmitted/onEventDropped callbacks WITHOUT adding Micrometer to coroutine-viz-core)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§MetricsWiring.kt change: Counter/Timer imports + new metric registrations)
  </read_first>
  <action>
    Rename the scrape route in `Monitoring.kt` from `/metrics-micrometer` to `/metrics` (per ADR-020). In `MetricsWiring.kt`, register the 5 missing ADR-020 metrics on the existing registry following the `Gauge.builder` pattern: `events.emitted` (Counter), `events.dropped` (Counter), `events.buffer.size` (Gauge per session, tagged by sessionId), `scenario.duration` (Timer), `event.processing.duration` (Timer). Import `io.micrometer.core.instrument.Counter` and `Timer`.

    To increment from core WITHOUT adding Micrometer to `coroutine-viz-core` (Open Question 1), add lightweight callback hooks on core `VizSession` — `onEventEmitted: (() -> Unit)?` and `onEventDropped: (() -> Unit)?` (mirroring the existing `EventStore.onEvict` pattern) — and an `event.processing.duration` timing hook around the store+bus+snapshot work in `send()`. Wire these callbacks from `MetricsWiring`/`SessionManager` (e.g. via an `onSessionCreated` hook on `SessionManager`) so emitting increments `events.emitted`, eviction increments `events.dropped`, and `events.buffer.size` reads `session.store.count()`. Record `scenario.duration` in `ScenarioRunnerRoutes.kt` by wrapping the scenario run with `Timer.start()/sample.stop(scenarioDurationTimer)`. Keep core free of any Micrometer import.

    Extend `MetricsWiringTest.kt`: run a scenario via the real wiring, scrape `/metrics`, and assert all seven ADR-020 metric names are present (`viz.sessions.active`, `viz.sse.clients.active`, `events.emitted`, `events.dropped`, `events.buffer.size`, `scenario.duration`, `event.processing.duration`) with non-default values where applicable.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.MetricsWiringTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'events.emitted\|events.dropped\|events.buffer.size\|scenario.duration\|event.processing.duration' backend/src/main/kotlin/com/jh/proj/coroutineviz/MetricsWiring.kt` shows all 5 new metric names registered.
    - `Monitoring.kt` serves the scrape at `/metrics` (renamed from `/metrics-micrometer`).
    - `coroutine-viz-core` has NO Micrometer import (`grep -rc 'io.micrometer' backend/coroutine-viz-core/src/main` returns 0); increments flow via callbacks.
    - `MetricsWiringTest` asserts all 7 ADR-020 metric names present after a scenario run.
    - The test passes via the targeted gradle command and the full backend suite is green.
  </acceptance_criteria>
  <done>All seven ADR-020 metrics are wired and present at /metrics after a scenario run, with core kept Micrometer-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /metrics scrape | Metrics endpoint exposes internal counts to callers |
| build → prod container (dependency + log config) | Dependency supply chain + log-profile selection |
| coroutine labels/messages → prod JSON logs | Structured logs may contain developer-authored data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Maven install (logstash-logback-encoder, [ASSUMED]) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies coordinates on Maven Central before install; coordinates pinned to `net.logstash.logback:logstash-logback-encoder` |
| T-01-12 | Information Disclosure | `/metrics` scrape (session counts, buffer sizes tagged by sessionId) | accept | Exposes internal counts; no secrets/PII; acceptable for local/dev deployment; Phase 3 auth will gate it |
| T-01-13 | Information Disclosure | prod JSON logs | accept | LogstashEncoder structures coroutine labels/messages; these are developer-authored teaching content, no PII; acceptable risk for Phase 1 |
</threat_model>

<verification>
- `cd backend && ./gradlew test` green.
- All 7 ADR-020 metric names present at /metrics after a scenario run.
- `build.gradle.kts` has the logstash dep; Dockerfile selects logback-prod.xml.
- `coroutine-viz-core` has zero Micrometer imports.
</verification>

<success_criteria>
- PROD-02: prod logback profile selected via JVM flag; logstash dep present; container can start.
- PROD-05: all 7 ADR-020 metrics wired and present at /metrics after a scenario run.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-05-SUMMARY.md` when done.
</output>
