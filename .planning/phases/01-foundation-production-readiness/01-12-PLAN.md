---
phase: 01-foundation-production-readiness
plan: 12
type: execute
wave: 2
depends_on: ["01-11"]
gap_closure: true
autonomous: true
requirements: [FIX-02, FIX-03]
files_modified:
  - frontend/src/components/SessionDetails.tsx
  - frontend/src/hooks/use-event-stream.ts
  - frontend/src/hooks/use-thread-activity.ts
  - frontend/src/components/StructuredConcurrencyInfo.tsx
  - frontend/src/components/SessionDetails.test.tsx
  - frontend/src/components/StructuredConcurrencyInfo.test.tsx
must_haves:
  truths:
    - "While SSE is connected, the session page does not poll GET /api/sessions/{id} and /threads at high frequency (no ~88 requests in ~3s)"
    - "After all coroutines reach a terminal state, the Scenario Controls show a runnable or explicit Completed state instead of a permanently disabled 'Scenario Running' button"
    - "The connection badge shows Connected once the EventSource is open on the first session load, without requiring a reload"
    - "The Structured Concurrency Failure Propagation copy states the parent becomes FAILED (matching the rendered tree), not CANCELLED"
  artifacts:
    - path: "frontend/src/components/SessionDetails.tsx"
      provides: "Debounced/SSE-driven refresh, completion-aware scenario button"
      contains: "Scenario"
    - path: "frontend/src/components/StructuredConcurrencyInfo.tsx"
      provides: "Failure Propagation copy aligned to parent-FAILED rendering"
      contains: "Failure Propagation"
  key_links:
    - from: "use-event-stream onopen"
      to: "isConnected badge"
      via: "setIsConnected(true) drives Connected chip on first load"
      pattern: "isConnected"
    - from: "SSE liveEvents arrival"
      to: "react-query invalidation/refetch"
      via: "debounced single refetch instead of per-event + 500ms interval polling"
      pattern: "refetch|invalidateQueries"
---

<objective>
Close the four frontend session-page polish gaps observed in UAT test 2 (all minor/cosmetic):
1. Polling storm: ~88 GET /api/sessions/{id} + /threads requests in ~3s while SSE is active — caused by use-thread-activity refetchInterval: 2000 plus SessionDetails' 500ms auto-refresh interval and per-SSE-event refetch/invalidation without throttling.
2. Stuck "Scenario Running" button: once any coroutine starts (hasStarted = coroutineCount > 0) the Run button is permanently disabled, even after all coroutines terminate; persists across reload.
3. "Connecting…" badge stays lit while events stream on first load; shows Connected only after a reload.
4. StructuredConcurrencyInfo Failure Propagation copy says the parent "gets CANCELLED", but the backend emits CoroutineFailed for the parent and the tree renders FAILED.

Purpose: the live session page behaves correctly — no request storm, an honest run-button state, an accurate connection badge, and copy that matches implemented semantics.
Output: debounced/SSE-driven refresh, completion-aware scenario controls, an onopen-driven badge, corrected copy, plus a SessionDetails test guarding the button-state transition.

Depends on 01-11 because that plan also edits SessionDetails.tsx and use-event-stream.ts (discriminator normalization); this plan builds on the normalized-events baseline. Run after 01-11.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-UAT.md

# Session page: scenario button state, 200ms debounce + 500ms interval auto-refresh, badge
@frontend/src/components/SessionDetails.tsx

# SSE hook: onopen sets isConnected, per-event invalidateQueries (the invalidation storm source)
@frontend/src/hooks/use-event-stream.ts

# Thread activity polling every 2s (one of the request-storm sources)
@frontend/src/hooks/use-thread-activity.ts

# Copy to correct (parent FAILED, not CANCELLED)
@frontend/src/components/StructuredConcurrencyInfo.tsx

# Existing test to extend for the button-state transition
@frontend/src/components/SessionDetails.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stop the polling storm and fix the connection badge</name>
  <files>frontend/src/components/SessionDetails.tsx, frontend/src/hooks/use-event-stream.ts, frontend/src/hooks/use-thread-activity.ts</files>
  <behavior>
    - While SSE is connected, the session does not refetch GET /api/sessions/{id} per SSE event nor on a 500ms interval; refreshes are coalesced (debounced) so a burst of events triggers at most one refetch per debounce window (e.g. one per ~400-500ms), not one per event.
    - use-thread-activity does not poll every 2s while the live stream is driving updates: gate or lengthen the refetchInterval so the Threads view refreshes from SSE-driven invalidation rather than a fixed 2s poll (e.g. disable refetchInterval when live streaming, or raise it substantially). When SSE is NOT active, existing behavior may remain.
    - The connection badge shows Connected as soon as the EventSource fires onopen on the first session load (no reload required). The onopen handler already calls setIsConnected(true); ensure no state reset (e.g. the enable-stream effect or a remount) leaves isConnected false after onopen.
    - Net observable: opening a freshly loaded session with SSE active produces a small, bounded number of REST requests over the first few seconds (no ~88-in-3s storm) and the badge reads Connected.
  </behavior>
  <action>
    In SessionDetails.tsx: remove the 500ms setInterval auto-refresh that fires refetch() while streaming (the autoRefresh interval effect), and replace the per-liveEvents-length 200ms debounce with a single coalesced debounce that triggers at most one refetch per window even under a burst (keep a ref to the latest timer; reset on each event; only the trailing edge fires). Prefer relying on SSE-delivered liveEvents for live data and only refetch the session snapshot occasionally. In use-event-stream.ts: replace the per-event queryClient.invalidateQueries call inside each event listener with a debounced/throttled invalidation (accumulate and invalidate on a trailing-edge timer), so a burst of SSE events does not invalidate (and thus refetch) once per event. In use-thread-activity.ts: make refetchInterval conditional — accept an `enabled`/`isLive` style signal (or disable the interval) so it does not poll every 2s while live streaming is the source of truth; document the chosen approach in the SUMMARY. Confirm the onopen path sets isConnected true on first load and is not reset by the streamEnabled effects. Keep all files ESLint/strict clean and do not regress existing tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- src/components/SessionDetails.test.tsx src/hooks</automated>
  </verify>
  <done>No 500ms polling interval remains; SSE invalidation is debounced; thread-activity does not 2s-poll while live; badge reads Connected on first onopen; existing hook/component tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Completion-aware scenario controls + corrected concurrency copy + button-state test</name>
  <files>frontend/src/components/SessionDetails.tsx, frontend/src/components/StructuredConcurrencyInfo.tsx, frontend/src/components/SessionDetails.test.tsx, frontend/src/components/StructuredConcurrencyInfo.test.tsx</files>
  <behavior>
    - When all coroutines have reached a terminal state (Completed/Cancelled/Failed), the Scenario Controls no longer show a permanently disabled "Scenario Running"; instead they show an explicit Completed state (button labeled e.g. "Scenario Completed" and/or a re-runnable affordance), distinguishable from the in-progress state.
    - The helper text reflects three states: not started ("Ready to run"), in progress ("Scenario is running"), completed ("Scenario completed").
    - Completion is derived from the session/event data already available (e.g. coroutineCount > 0 AND every coroutine in a terminal state, using the normalized events / session.coroutines states), not from a wall-clock guess.
    - StructuredConcurrencyInfo Failure Propagation copy states: a child throwing enters FAILED, the failure propagates to the parent which becomes FAILED (completes exceptionally), and siblings are CANCELLED — matching the rendered tree and Kotlin semantics. The word "CANCELLED" for the parent is replaced with "FAILED".
    - A SessionDetails test asserts the button transitions out of the disabled in-progress state once coroutines are terminal (i.e. given a session whose coroutines are all terminal, the control does not render the disabled "Scenario Running" button).
  </behavior>
  <action>
    In SessionDetails.tsx, replace the binary hasStarted gate on the scenario button with a three-state derivation: notStarted / running / completed. Derive `allTerminal` from session.coroutines (or the normalized events) by checking every coroutine is in a terminal state; treat coroutineCount === 0 as notStarted. Update the button label/isDisabled and the helper paragraph accordingly (notStarted: "Run Scenario" enabled + "Ready to run the scenario"; running: "Scenario Running" disabled + "Scenario is running"; completed: an explicit completed label + "Scenario completed", and make it re-runnable only if the underlying flow supports a fresh run — otherwise show a clear non-misleading Completed state). Use the coroutine state field names already present in the session model (inspect session.coroutines shape before coding). In StructuredConcurrencyInfo.tsx, edit the Failure Propagation block so the parent "gets FAILED" (completes exceptionally) rather than "gets CANCELLED", and keep the sibling-cancelled wording. In SessionDetails.test.tsx, add a test that renders with a session whose coroutines are all terminal and asserts the disabled "Scenario Running" button is not shown (and/or the completed state is shown). Create frontend/src/components/StructuredConcurrencyInfo.test.tsx (Vitest + Testing Library) that renders <StructuredConcurrencyInfo /> and asserts the Failure Propagation section's rendered text says the parent becomes FAILED (e.g. getByText matching /parent/ and the FAILED wording in the same block) and does NOT describe the parent as becoming CANCELLED. Keep ESLint/strict clean; do not break the 225 existing frontend tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- src/components/SessionDetails.test.tsx src/components/StructuredConcurrencyInfo.test.tsx</automated>
  </verify>
  <done>Scenario controls show a distinct completed state when all coroutines are terminal; concurrency copy says the parent becomes FAILED; the button-state transition test passes and no existing tests regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend REST | client-driven request rate; an uncontrolled loop is a self-inflicted DoS on the dev server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12-01 | Denial of Service | session-page polling/invalidation loop | mitigate | Remove 500ms interval, debounce SSE invalidation, gate 2s thread polling — bounded request rate |
| T-01-12-02 | Tampering | npm installs | accept | No new dependencies; UI logic + copy + test only |
</threat_model>

<verification>
- `cd frontend && pnpm test -- src/components/SessionDetails.test.tsx src/hooks` passes.
- Manual/automated: a freshly loaded live session issues a bounded number of REST requests over the first few seconds (no ~88-in-3s storm) — note the observed request behavior in the SUMMARY.
- StructuredConcurrencyInfo.test.tsx asserts the parent-FAILED wording (and not parent-CANCELLED) in the Failure Propagation block.
- `cd frontend && pnpm lint` clean for modified files.
</verification>

<success_criteria>
While SSE is connected the page does not high-frequency poll; the scenario button leaves the disabled in-progress state when coroutines terminate; the badge reads Connected on first onopen; and the Failure Propagation copy states the parent becomes FAILED.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-12-SUMMARY.md` when done.
</output>
