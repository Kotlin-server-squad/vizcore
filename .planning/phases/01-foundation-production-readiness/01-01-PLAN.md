---
phase: 01-foundation-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/VizEventSerializersModule.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/Serialization.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt
  - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/events/flow/FlowEvents.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/wrappers/VizScope.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/scenarios/ScenarioRunner.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/events/VizEventSerializersModuleTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SerializersModuleIntegrationTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SseStreamTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ExceptionScenarioRegressionTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/CancellationScenarioRegressionTest.kt
autonomous: true
requirements: [FIX-01, FIX-03, FIX-04]
must_haves:
  truths:
    - "GET /api/sessions/{id}/events returns 200 with the stored event list after a scenario run (was 500)"
    - "An SSE client receives at least one event end-to-end with zero SerializationException in backend logs"
    - "A throwing coroutine renders FAILED (emits CoroutineFailed) while cancelled victims render CANCELLED"
    - "The Cancellation scenario leaves child-to-be-cancelled CANCELLED and normal-child COMPLETED"
  artifacts:
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/VizEventSerializersModule.kt"
      provides: "Polymorphic SerializersModule registering all 66 VizEvent subclasses + shared appJson"
      contains: "polymorphic(VizEvent::class)"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/events/VizEventSerializersModuleTest.kt"
      provides: "Registration-completeness guard (D-04)"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ExceptionScenarioRegressionTest.kt"
      provides: "FIX-03 regression test"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/CancellationScenarioRegressionTest.kt"
      provides: "FIX-04 regression test"
  key_links:
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/Serialization.kt"
      to: "vizEventSerializersModule"
      via: "install(ContentNegotiation) { json(appJson) }"
      pattern: "json\\(appJson\\)"
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt"
      to: "appJson"
      via: "SSE encodeToString uses module-aware Json"
      pattern: "appJson\\.encodeToString"
---

<objective>
Fix the three backend runtime defects that ship as the FIX wave before any de-fork: VizEvent polymorphic serialization (FIX-01), the unreachable FAILED state (FIX-03), and the broken Cancellation scenario (FIX-04). This is the first wave per the research-locked sequence — the FIX wave MUST complete before FND-01 deletes the fork, because `VizScope.kt` imports `EventContext` extension functions that currently resolve to the fork and will resolve identically to core after deletion.

Purpose: Unblock SSE streaming, the `/events` endpoint, the Events tab, and the conditional Channels/Flow/Sync/Jobs tabs; make coroutine failures render FAILED distinct from CANCELLED; make the Cancellation teaching scenario correct.
Output: A `SerializersModule` with all 66 VizEvent subclasses, module-aware `Json` wired into ContentNegotiation + SSE, a cause-type VizScope completion handler, a targeted-cancel Cancellation scenario, and regression tests for each.
</objective>

<artifacts_produced>
Symbols this phase creates (exclude from drift verification):
- `vizEventSerializersModule` (top-level `val`, `SerializersModule`) — `VizEventSerializersModule.kt`
- `appJson` (top-level `val`, `Json`) — `VizEventSerializersModule.kt`
- Test classes: `VizEventSerializersModuleTest`, `SerializersModuleIntegrationTest`, `ExceptionScenarioRegressionTest`, `CancellationScenarioRegressionTest`
- `@SerialName` annotations added to 7 flow event classes (`FlowBackpressure`, `FlowOperatorApplied`, `FlowValueFiltered`, `FlowValueTransformed`, `SharedFlowEmission`, `SharedFlowSubscription`, `StateFlowValueChanged`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-RESEARCH.md
@.planning/phases/01-foundation-production-readiness/01-PATTERNS.md
@.planning/phases/01-foundation-production-readiness/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VizEventSerializersModule + appJson and wire both Json paths (FIX-01)</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/VizEventSerializersModule.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/Serialization.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt, backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/events/flow/FlowEvents.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/events/VizEventSerializersModuleTest.kt</files>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/Serialization.kt (current ContentNegotiation install — line ~15-24, uses bare `json()`)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt (SSE handler ~line 199 and 215 use bare `Json.encodeToString(event)`)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/events/VizEvent.kt (the non-sealed interface being registered)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/events/flow/FlowEvents.kt (7 classes missing @SerialName: FlowBackpressure, FlowOperatorApplied, FlowValueFiltered, FlowValueTransformed, SharedFlowEmission, SharedFlowSubscription, StateFlowValueChanged — read their `override val kind` values)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/events/EventSerializationTest.kt (exact-match test analog for setup + Json config)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (full 66-subclass SerializersModule listing, the `appJson` declaration, import block, and the registration-completeness test pattern)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§FIX-01: complete 66-class inventory by package, the two Json instances at Serialization.kt:17 and SessionRoutes.kt:199/215)
  </read_first>
  <action>
    Create `backend/src/main/kotlin/com/jh/proj/coroutineviz/VizEventSerializersModule.kt` declaring top-level `val vizEventSerializersModule = SerializersModule { polymorphic(VizEvent::class) { subclass(...) } }` registering all 66 concrete `@Serializable` VizEvent subclasses per the package inventory: coroutine (8), job (4), flow (13), dispatcher (2), deferred (3), channel (9), mutex (6), semaphore (6), actor (7), select (4), deadlock (2), anti-pattern (1), structured-concurrency WaitingForChildren (1). Per D-05 do NOT seal the hierarchy. Exclude non-VizEvent helpers: `SuspensionPoint`, `AntiPatternSeverity`, `AntiPatternType`. Also declare top-level `val appJson = Json { serializersModule = vizEventSerializersModule; encodeDefaults = true; ignoreUnknownKeys = true }` in the same file. Use the import block from 01-PATTERNS.md (events.coroutine.*, events.job.*, events.flow.*, events.dispatcher.*, events.deferred.*, events.channel.*, events.* for the events-package-root classes). The mutex/semaphore/actor/select/deadlock/anti-pattern/WaitingForChildren classes all live in package `com.jh.proj.coroutineviz.events` (verified) — covered by `import com.jh.proj.coroutineviz.events.*`.

    Before registering the 7 flow classes lacking `@SerialName`, add `@SerialName("<value>")` to each in `FlowEvents.kt`, where `<value>` equals each class's `override val kind` property value (FIX-01 / D-03; see Pitfall 5). Confirm the value by reading the class's `kind` override.

    In `Serialization.kt`, change `install(ContentNegotiation) { json() }` to `json(appJson)` (import `appJson`). Leave the existing `/json/kotlinx-serialization` demo route unchanged.

    In `SessionRoutes.kt`, replace both bare `Json.encodeToString(event)` calls (SSE handler, ~lines 199 and 215) with the module-aware instance: `appJson.encodeToString(VizEvent.serializer(), event)`. Import `appJson` and `com.jh.proj.coroutineviz.events.VizEvent`. Do NOT cast `event` to a concrete subtype (that loses the polymorphic discriminator).

    Create the registration-completeness test `VizEventSerializersModuleTest.kt` (D-04): enumerate all 66 subclasses in a `listOf(... ::class ...)` and assert each is resolvable in the polymorphic scope (e.g. via `module.getPolymorphic(VizEvent::class, klass.simpleName!!)` non-null, or by a round-trip encode of a constructed instance), plus one `appJson` round-trip test for a `CoroutineCreated` instance. A newly added unregistered event type must fail this test, not production.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.VizEventSerializersModuleTest"</automated>
  </verify>
  <acceptance_criteria>
    - `VizEventSerializersModule.kt` exists and contains `polymorphic(VizEvent::class)` with exactly 66 `subclass(` registrations.
    - `grep -c 'subclass(' backend/src/main/kotlin/com/jh/proj/coroutineviz/VizEventSerializersModule.kt` returns 66.
    - `appJson` is declared once and used by both `Serialization.kt` (`json(appJson)`) and `SessionRoutes.kt` (`appJson.encodeToString`).
    - `grep -n 'Json.encodeToString(event)' backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt | grep -v '^#'` returns no matches (bare default-Json call removed).
    - The 7 flow classes in `FlowEvents.kt` each have a `@SerialName` annotation.
    - `./gradlew test --tests "*.VizEventSerializersModuleTest"` passes.
  </acceptance_criteria>
  <done>The SerializersModule registers all 66 subclasses, both Json paths use the module-aware instance, and the completeness test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-03 cause-type classification + FIX-04 targeted cancel, with regression tests</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/wrappers/VizScope.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/scenarios/ScenarioRunner.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/ExceptionScenarioRegressionTest.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/CancellationScenarioRegressionTest.kt</files>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/wrappers/VizScope.kt (the completion handler when-branch at ~line 182; the message-contains-label branch that never fires; the else IllegalArgumentException branch; the imports at lines 9-18 that resolve to the fork's EventContext today)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/scenarios/ScenarioRunner.kt (cancellation scenario block ~lines 95-126: commented `//child1.cancel()` at line 117, `delay(1000); job.cancel()` at lines 123-124)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutesTest.kt (exact-match testApplication boilerplate: SessionManager.clearAll() in setUp/tearDown, jsonClient() helper, application { module() })
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (the exact FIX-03 replacement branch and FIX-04 edits; the testApplication regression boilerplate)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§FIX-03 exact 4-line fix, §FIX-04 exact lines; §Validation Architecture for the scenario-run-then-read-snapshot test shape)
  </read_first>
  <action>
    FIX-03 — in `VizScope.kt` replace the `cause !is CancellationException && cause.message?.contains(ctx.label ?: "unknown") == true ->` branch (~line 182) with a plain `cause !is CancellationException ->` branch that emits `ctx.coroutineFailed(cause::class.simpleName, cause.message)` then `ctx.jobStateChanged(isActive = false, isCompleted = false, isCancelled = false, childrenCount = coroutineContext[Job]?.children?.count() ?: 0)`. The terminal classification target is: `cause == null` → Completed; `cause is CancellationException` (or `job.isCancelled`) → Cancelled; any other Throwable → Failed. Remove the now-unreachable `else -> throw IllegalArgumentException(...)` branch. Cancelled victims must still emit `CoroutineCancelled` (do not change the cancellation branch). Note `isCancelled = false` for FAILED coroutines (the old code set it true).

    FIX-04 — in `ScenarioRunner.kt` cancellation scenario: uncomment `child1.cancel()` at line 117 and precede it with a short visible delay (`vizDelay(500)`) so the cancel is mid-flight; remove the `delay(1000)` and `job.cancel()` at lines 123-124 so `normal-child` (the 3-second child) runs to completion under structured concurrency. Keep the `child1` reference for the cancel call; if `child2`/`normal-child` is otherwise unused as a variable, either drop the unused binding or `join()` it explicitly (ktlint must stay clean).

    Create `ExceptionScenarioRegressionTest.kt` (FIX-03): using the testApplication boilerplate, POST to create a session, run the exception scenario over the real route, then read the session snapshot and assert `failing-child` final state == FAILED and `normal-child`/`parent` == CANCELLED.

    Create `CancellationScenarioRegressionTest.kt` (FIX-04): run the cancellation scenario over the real route, read the snapshot, assert `child-to-be-cancelled` == CANCELLED and `normal-child` == COMPLETED. Use the exact coroutine label/id strings from the scenario definitions in `ScenarioRunner.kt`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.ExceptionScenarioRegressionTest" --tests "*.CancellationScenarioRegressionTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'message?.contains' backend/src/main/kotlin/com/jh/proj/coroutineviz/wrappers/VizScope.kt | grep -v '^#'` no longer matches the completion-handler label branch.
    - VizScope completion handler has a `cause !is CancellationException ->` branch emitting `coroutineFailed` with `isCancelled = false`.
    - `grep -n 'job.cancel()' backend/src/main/kotlin/com/jh/proj/coroutineviz/scenarios/ScenarioRunner.kt | grep -v '^#'` no longer matches inside the cancellation scenario block; `child1.cancel()` is active (uncommented).
    - `ExceptionScenarioRegressionTest` asserts `failing-child == FAILED` and `normal-child == CANCELLED`.
    - `CancellationScenarioRegressionTest` asserts `child-to-be-cancelled == CANCELLED` and `normal-child == COMPLETED`.
    - Both new test classes pass via the targeted gradle command.
  </acceptance_criteria>
  <done>VizScope classifies FAILED by cause type, the Cancellation scenario performs a targeted child cancel, and both regression tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end SSE + /events integration test; un-cast existing SseStreamTest (FIX-01 acceptance)</name>
  <files>backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SerializersModuleIntegrationTest.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SseStreamTest.kt</files>
  <read_first>
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SseStreamTest.kt (line ~116 casts `event as CoroutineCreated` to dodge the broken polymorphic path — this workaround must be removed so the test exercises the real fix)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutesTest.kt (testApplication boilerplate + jsonClient helper)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt (the /events route returning List<VizEvent> and the SSE /stream route)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§Pattern 3 testApplication route regression test; §Common Pitfalls Pitfall 1 stale SseStreamTest)
    - .planning/phases/01-foundation-production-readiness/01-VALIDATION.md (FIX-01 row: scenario via REST → /events 200, SSE ≥1 event, 0 SerializationException)
  </read_first>
  <action>
    Create `SerializersModuleIntegrationTest.kt` using the testApplication boilerplate: create a session, run a scenario (e.g. `nested-coroutines`) over the real route, then `GET /api/sessions/{id}/events` and assert HTTP 200 and a non-empty event list (count > 0). Add a second test that connects to the SSE `/stream` route, runs a scenario, and asserts at least one event is received and that no `SerializationException` text appears in the captured response/log path — encode/decode must go through the polymorphic `VizEvent` path (not a concrete cast).

    Update the existing `SseStreamTest.kt`: remove the `Json.encodeToString(event as CoroutineCreated)` cast-to-concrete workaround at ~line 116 and encode as `VizEvent` via `appJson` (the module-aware instance) so the test verifies the real polymorphic path now succeeds.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.SerializersModuleIntegrationTest" --tests "*.SseStreamTest"</automated>
  </verify>
  <acceptance_criteria>
    - `SerializersModuleIntegrationTest` asserts `GET /api/sessions/{id}/events` returns `HttpStatusCode.OK` with a non-empty list after a scenario run.
    - The SSE test asserts ≥1 event received and zero `SerializationException`.
    - `grep -n 'as CoroutineCreated' backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SseStreamTest.kt | grep -v '^#'` returns no matches.
    - Both test classes pass; the full backend suite (`./gradlew test`) is green.
  </acceptance_criteria>
  <done>Integration tests prove /events returns 200 and SSE delivers events end-to-end with no SerializationException, and the stale cast workaround is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SSE/REST event endpoints | Untrusted client reads serialized event stream; serialization must not crash the stream |
| scenario input → VizScope completion handler | Exception cause type drives terminal-state emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | SSE stream / `/events` serialization | mitigate | Polymorphic SerializersModule resolves every subtype; D-04 completeness test fails CI if a new event type is unregistered, preventing a single unregistered type from killing the stream |
| T-01-02 | Information Disclosure | `CoroutineFailed(cause::class.simpleName, cause.message)` | accept | Exception class name + message surfaced to the event stream; coroutine labels/messages are developer-authored teaching content, no PII; acceptable for local/dev deployment (Phase 3 adds auth) |
| T-01-03 | Tampering | npm/Maven installs | mitigate | No new dependency in this plan; RESEARCH Package Legitimacy Audit covers the single Maven dep added in plan 05 (logstash-logback-encoder, [ASSUMED], approved) |
</threat_model>

<verification>
- `cd backend && ./gradlew test` is fully green (existing 221 + new tests).
- `grep -c 'subclass(' .../VizEventSerializersModule.kt` == 66.
- No bare `Json.encodeToString(event)` remains in `SessionRoutes.kt`.
</verification>

<success_criteria>
- FIX-01: `/events` returns 200 with events; SSE delivers ≥1 event; zero SerializationException; registration-completeness test green.
- FIX-03: exception scenario → failing-child FAILED, normal-child CANCELLED.
- FIX-04: cancellation scenario → child-to-be-cancelled CANCELLED, normal-child COMPLETED.
- Backend test suite green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-01-SUMMARY.md` when done.
</output>
