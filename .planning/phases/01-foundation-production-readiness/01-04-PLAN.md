---
phase: 01-foundation-production-readiness
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/Routing.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutesTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/CorsConfigTest.kt
  - backend/src/main/resources/openapi/documentation.yaml
autonomous: true
requirements: [PROD-01, PROD-03, PROD-04]
must_haves:
  truths:
    - "GET /api/health, /api/live, /api/ready, and /health all return 200 with JSON; health responses include component checks, uptime, and a non-empty version"
    - "A test asserts CORS allowed origins come from config (application.yaml/env), not hardcoded literals"
    - "The OpenAPI spec documents the health alias + /api/health|live|ready, the validation response shape, and /events, and passes an OpenAPI validator"
  artifacts:
    - path: "backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt"
      provides: "/api/health, /api/live, /api/ready endpoints + /health alias; HealthStatus with version + components"
      contains: "/api/health"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/CorsConfigTest.kt"
      provides: "PROD-03 CORS config-read regression test"
    - path: "backend/src/main/resources/openapi/documentation.yaml"
      provides: "Updated, validating OpenAPI spec for touched endpoints"
  key_links:
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt"
      to: "SessionManager"
      via: "component check reads SessionManager.listSessions()"
      pattern: "SessionManager\\.listSessions"
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/Routing.kt"
      to: "registerHealthRoutes"
      via: "health routes mounted"
      pattern: "registerHealthRoutes"
---

<objective>
Complete the health/observability surface and lock the existing config-driven behaviors: add `/api/health`, `/api/live`, `/api/ready` with component checks + uptime + version while keeping `/health` as an alias (PROD-01); add a CORS config-read regression test so the already-working config-driven CORS cannot regress (PROD-03); update the hand-maintained OpenAPI spec for the touched endpoints (health alias, the new health paths, the validation response shape, `/events`) and make it pass an OpenAPI validator (PROD-04). Wave 3 — depends on Plan 03 because `HealthRoutes.kt` reads `SessionManager` from core after the de-fork.

Purpose: Kubernetes-compatible probes, regression-proofed CORS, and an accurate spec.
Output: Extended `HealthRoutes.kt`, a `CorsConfigTest`, an updated/validating `documentation.yaml`, and extended `HealthRoutesTest`.
</objective>

<artifacts_produced>
Symbols this phase creates (exclude from drift verification):
- `GET /api/health`, `GET /api/live`, `GET /api/ready` routes — `HealthRoutes.kt`
- `version` and `components` fields added to `HealthStatus` — `HealthRoutes.kt`
- Shared `respondHealth(call)` helper — `HealthRoutes.kt`
- Test classes: `CorsConfigTest`; extended `HealthRoutesTest`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-RESEARCH.md
@.planning/phases/01-foundation-production-readiness/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /api/health, /api/live, /api/ready + /health alias with version + component checks (PROD-01)</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/Routing.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutesTest.kt</files>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt (current single /health route ~lines 1-56; HealthStatus data class ~lines 17-23; MemoryInfo; startTime)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/Routing.kt (where registerHealthRoutes() is mounted)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutesTest.kt (existing test for /health; jsonClient + testApplication boilerplate to extend)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§HealthRoutes.kt change: extended HealthStatus with version+components, the respondHealth() shared helper, the new route block for /api/health|live|ready)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§PROD-01: version field options — Claude's discretion; readiness 503 when memory >= 95%)
  </read_first>
  <action>
    Extend `HealthStatus` with `version: String` and `components: Map<String, String> = emptyMap()`. Extract the existing health-body logic into a shared `private suspend fun ApplicationCall.respondHealth()` that computes memory usage, session count, uptime, status ("UP" / "DEGRADED" when memory >= 90%), `components = mapOf("sessionManager" to "UP", "memory" to ...)`, and a non-empty `version`. Version source is Claude's discretion (per CONTEXT D-list) — simplest acceptable is the `version` from `build.gradle.kts` ("0.0.1") surfaced via a constant or `processResources` token; it MUST be non-empty. In `registerHealthRoutes()`: keep `get("/health")` calling `respondHealth()` (alias preserved); add `route("/api") { get("/health") { call.respondHealth() }; get("/live") { call.respond(OK, mapOf("status" to "UP")) }; get("/ready") { ... 200 status:UP when SessionManager reachable and memory < 95%, else 503 status:DOWN } }`. Confirm `registerHealthRoutes()` is mounted in `Routing.kt`. Extend `HealthRoutesTest` with cases for `/api/health` (asserts 200 + non-empty version + components present), `/api/live` (200 status UP), `/api/ready` (200 status UP), and `/health` (alias still 200).
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.HealthRoutesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `HealthStatus` has `version` and `components` fields.
    - `grep -c '/api/health\|/live\|/ready' backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt` shows all three new paths registered, plus the `/health` alias retained.
    - `HealthRoutesTest` asserts all four paths return 200 and that the health response `version` is non-empty and `components` is present.
    - The test passes via the targeted gradle command.
  </acceptance_criteria>
  <done>All four health paths return 200 with version + component checks; the test is green.</done>
</task>

<task type="auto">
  <name>Task 2: CORS config-read regression test (PROD-03)</name>
  <files>backend/src/test/kotlin/com/jh/proj/coroutineviz/CorsConfigTest.kt</files>
  <read_first>
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/HTTP.kt (lines ~22-65: config-driven CORS reading cors.allowedOrigins/allowedMethods from application.yaml/env — behavior is already correct, do NOT change it)
    - backend/src/main/resources/application.yaml (the cors block defining allowed origins)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutesTest.kt (role-match: jsonClient + testApplication boilerplate for an Origin-header request)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§CorsConfigTest.kt: copy HealthRoutesTest boilerplate; request with Origin header asserts Access-Control-Allow-Origin)
  </read_first>
  <action>
    Create `CorsConfigTest.kt` (PROD-03 — behavior unchanged, add coverage so config-driven CORS cannot silently regress to hardcoded literals). Using the testApplication boilerplate, boot `application { module() }`, issue a request (e.g. an OPTIONS preflight or a GET with an `Origin` header matching a configured allowed origin) and assert the response carries `Access-Control-Allow-Origin` reflecting the configured origin. Assert that an origin NOT in config is not allowed, demonstrating the value comes from config rather than a wildcard literal. Do not modify `HTTP.kt`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.CorsConfigTest"</automated>
  </verify>
  <acceptance_criteria>
    - `CorsConfigTest` asserts a configured allowed origin yields an `Access-Control-Allow-Origin` header and a non-configured origin does not.
    - `HTTP.kt` is unchanged (no diff).
    - The test passes via the targeted gradle command.
  </acceptance_criteria>
  <done>A regression test proves CORS allowed origins come from config, not literals.</done>
</task>

<task type="auto">
  <name>Task 3: Update OpenAPI spec for touched endpoints and pass a validator (PROD-04)</name>
  <files>backend/src/main/resources/openapi/documentation.yaml</files>
  <read_first>
    - backend/src/main/resources/openapi/documentation.yaml (the 2270-line hand-maintained spec; the existing /health path entry; the validate-endpoint schema that does not match served JSON)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/ValidationRoutes.kt (ValidationResponse shape: sessionId, results[] of Pass|Fail, timing TimingReport — the served shape the spec must match)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/HealthRoutes.kt (final HealthStatus shape from Task 1, to document /api/health|live|ready + /health)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutes.kt (the /api/sessions/{id}/events route returning List<VizEvent>, to document /events)
    - .planning/phases/01-foundation-production-readiness/01-SPEC.md (PROD-04 acceptance: validator exits 0; validate-endpoint schema matches a captured live response)
  </read_first>
  <action>
    Update `documentation.yaml` to: (1) add path entries for `/api/health`, `/api/live`, `/api/ready` and keep/adjust `/health` as an alias, with the `HealthStatus` schema including `version` and `components`; (2) correct the `POST /api/validate/session/{id}` response schema to the served shape `{sessionId, results: [{type: Pass|Fail, ruleName, message, details?}], timing: {coroutineDurations, suspensionDurations, totalDuration}}`; (3) document `GET /api/sessions/{id}/events` returning an array of VizEvent. Then validate the spec with an OpenAPI validator and ensure it exits 0. Use an available validator (e.g. `npx --yes @redocly/cli lint` or `swagger-cli validate` if present); the executor selects whichever validator is installable in the environment and records the exact command in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend && npx --yes @redocly/cli@latest lint src/main/resources/openapi/documentation.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `documentation.yaml` contains path entries for `/api/health`, `/api/live`, `/api/ready` and the validate response schema with `results` (Pass/Fail) and the backend `timing` shape.
    - `grep -c '/api/sessions/{id}/events\|/events' backend/src/main/resources/openapi/documentation.yaml` ≥ 1.
    - An OpenAPI validator exits 0 against the spec (command recorded in SUMMARY).
    - The validate-endpoint schema matches the served `ValidationResponse` shape.
  </acceptance_criteria>
  <done>The OpenAPI spec documents the touched endpoints accurately and passes validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/health, /api/ready | Health/readiness endpoints expose runtime state to callers |
| cross-origin browser → API | CORS policy governs which origins may call the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | health responses (session count, memory, version) | accept | Health surfaces session count + memory + version; no secrets/PII; acceptable for local/dev deployment, Phase 3 auth will gate it |
| T-01-10 | Spoofing / Elevation (cross-origin) | CORS allowed origins | mitigate | PROD-03 test asserts origins come from config (not a wildcard literal) and a non-configured origin is rejected, preventing silent regression to permissive CORS |
| T-01-11 | Tampering | npm validator install (`@redocly/cli`) | accept | Dev-time-only OpenAPI lint, well-known Redocly tool, not shipped in runtime; no production dependency added |
</threat_model>

<verification>
- `cd backend && ./gradlew test` green.
- All four health paths return 200 with version + components.
- OpenAPI validator exits 0.
</verification>

<success_criteria>
- PROD-01: /api/health, /api/live, /api/ready, /health all 200 with component checks + uptime + non-empty version.
- PROD-03: CORS config-read test green.
- PROD-04: spec documents touched endpoints and passes validation.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-04-SUMMARY.md` when done.
</output>
