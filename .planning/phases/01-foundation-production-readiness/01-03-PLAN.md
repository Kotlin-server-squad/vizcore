---
phase: 01-foundation-production-readiness
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventApplier.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventBus.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventContext.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventStore.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/FlowEventContext.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/ChannelEventContext.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/JobStatusMonitor.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt
  - backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt
  - backend/src/main/resources/application.yaml
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/BoundedStoreWiringTest.kt
  - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/BoundedStoreRegressionTest.kt
autonomous: true
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "The backend compiles and tests pass against coroutine-viz-core's session classes only — the backend/src/main session fork is gone"
    - "SessionManager.configure(maxEventsPerSession) is called at startup with the application.yaml value (default 10000)"
    - "A high-volume session never grows the event store past maxEvents, proven by a regression test"
  artifacts:
    - path: "backend/src/main/resources/application.yaml"
      provides: "session.maxEvents config key (default 10000, env-overridable)"
      contains: "maxEvents"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt"
      provides: "Static guard that no session classes remain under backend/src/main (FND-01 cannot silently return)"
    - path: "backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/BoundedStoreRegressionTest.kt"
      provides: "FND-03 regression test asserting store.all().size <= maxEvents"
  key_links:
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt"
      to: "SessionManager.configure"
      via: "startup reads session.maxEvents and configures the bounded store"
      pattern: "SessionManager\\.configure"
    - from: "backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt"
      to: "session.maxEvents"
      via: "environment.config.propertyOrNull"
      pattern: "session\\.maxEvents"
---

<objective>
Delete the duplicate `backend/src/main/.../session/` fork so the backend resolves against `coroutine-viz-core`'s authoritative session classes only (FND-01, D-01 big-bang), then wire the bounded EventStore via `SessionManager.configure()` from startup reading a new `application.yaml` key (FND-02), and prove the cap holds with a regression test plus a static fork-deletion guard (FND-03). This is Wave 2 — it runs after the FIX wave (Plan 01) because `VizScope.kt` imports `EventContext` extension functions that resolve to the fork today and must resolve identically to core after deletion.

Purpose: Remove the root cause of the unbounded-store and unwired-perf gaps; the core `EventStore` (ArrayDeque + ReentrantReadWriteLock, maxEvents default 10_000) and `SessionManager.configure()` already exist — this is wiring, not implementation.
Output: Fork subtree deleted, `SessionManager.configure()` wired at startup, `session.maxEvents` yaml key, and three tests (fork-deletion guard, bounded-store wiring smoke, bounded-store eviction regression).
</objective>

<artifacts_produced>
Symbols this phase creates (exclude from drift verification):
- `session.maxEvents` config key (default `${SESSION_MAX_EVENTS:10000}`) — `application.yaml`
- Test classes: `ForkDeletionTest`, `BoundedStoreWiringTest`, `BoundedStoreRegressionTest`
- The 10 deleted fork files are removals (not new symbols); after deletion the same symbols resolve from `coroutine-viz-core` (identical package `com.jh.proj.coroutineviz.session`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-production-readiness/01-SPEC.md
@.planning/phases/01-foundation-production-readiness/01-RESEARCH.md
@.planning/phases/01-foundation-production-readiness/01-PATTERNS.md
@.planning/phases/01-foundation-production-readiness/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Big-bang delete the session fork and verify compilation against core (FND-01)</name>
  <files>backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventApplier.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventBus.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventContext.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/EventStore.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/FlowEventContext.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/ChannelEventContext.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/JobStatusMonitor.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/ProjectionService.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt, backend/src/main/kotlin/com/jh/proj/coroutineviz/session/VizSession.kt</files>
  <read_first>
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§FND-01 De-Fork Blast Radius: the 10 files to delete, the full importing-files list, the EventContext extension-function resolution note, the fork API differences — fork SessionManager lacks configure(), fork EventStore is unbounded CopyOnWriteArrayList, core closeSession() delegates to deleteSession())
    - .planning/codebase/CONCERNS.md (fork inventory + the assessment that core is more complete; reconcile-into-core guidance D-02)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt (core has configure(maxEventsPerSession) ~line 35 and closeSession delegating to deleteSession)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/EventStore.kt (bounded ArrayDeque, maxEvents default 10_000, onEvict callback)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/wrappers/VizScope.kt (imports lines 9-18 of EventContext extension functions resolving to fork today — must resolve to core after deletion, no import change needed)
  </read_first>
  <action>
    Delete all 10 files under `backend/src/main/kotlin/com/jh/proj/coroutineviz/session/` (EventApplier, EventBus, EventContext, EventStore, FlowEventContext, ChannelEventContext, JobStatusMonitor, ProjectionService, SessionManager, VizSession) in one change (D-01 big-bang). Do NOT change import statements in the ~23 consuming files — they import `com.jh.proj.coroutineviz.session.*` / specific symbols by the SAME package name and resolve to `coroutine-viz-core` after the fork is gone (verified: core's `EventContext.kt` declares the identical extension functions). Run `cd backend && ./gradlew compileKotlin` first; resolve any compile failure by reconciling the fork-only behavior INTO core (D-02 — core is the single source of truth), not by reintroducing a fork file. Then run the full test suite to prove parity. Use `git rm` so the deletion is tracked.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin && ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - `find backend/src/main/kotlin/com/jh/proj/coroutineviz/session -name '*.kt' 2>/dev/null | wc -l` returns 0 (directory empty or absent).
    - `cd backend && ./gradlew compileKotlin` succeeds with no unresolved-reference errors.
    - `cd backend && ./gradlew test` is fully green (parity with pre-deletion behavior).
    - No new file was created under `backend/src/main/.../session/` to work around a compile error.
  </acceptance_criteria>
  <done>The fork subtree is deleted, the backend compiles against core, and the full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bounded EventStore via application.yaml + SessionManager.configure() at startup (FND-02)</name>
  <files>backend/src/main/resources/application.yaml, backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/BoundedStoreWiringTest.kt</files>
  <read_first>
    - backend/src/main/resources/application.yaml (existing structure to add the session block)
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt (the module() function order; add configure() before configureRouting())
    - backend/src/main/kotlin/com/jh/proj/coroutineviz/HTTP.kt (lines ~23-29: environment.config.propertyOrNull(...).getString() read pattern to copy)
    - backend/coroutine-viz-core/src/main/kotlin/com/jh/proj/coroutineviz/session/SessionManager.kt (configure(maxEventsPerSession) signature; createSession reads it)
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/MetricsWiringTest.kt (role-match analog for an application { module() } config-level smoke test)
    - .planning/phases/01-foundation-production-readiness/01-PATTERNS.md (§Application.kt change + §application.yaml addition; config-read pattern)
  </read_first>
  <action>
    Add to `application.yaml` a `session:` block with `maxEvents: ${SESSION_MAX_EVENTS:10000}` (env-overridable, default 10000). In `Application.module()`, BEFORE `configureRouting()`, read the value: `val maxEvents = environment.config.propertyOrNull("session.maxEvents")?.getString()?.toIntOrNull() ?: 10_000` and call `SessionManager.configure(maxEventsPerSession = maxEvents)`. Per V5 input validation, `toIntOrNull() ?: 10_000` safely defaults on malformed input. Create `BoundedStoreWiringTest.kt` (analog `MetricsWiringTest.kt`): boot `application { module() }`, create a session, and assert the session's store is the bounded variant (e.g. the configured cap is reflected — assert the store reports its `maxEvents`/capacity == the configured value, or that the wiring path runs without error and a created session uses the bounded core EventStore type).
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.BoundedStoreWiringTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'maxEvents' backend/src/main/resources/application.yaml` ≥ 1 and the key resolves to `${SESSION_MAX_EVENTS:10000}`.
    - `grep -c 'SessionManager.configure' backend/src/main/kotlin/com/jh/proj/coroutineviz/Application.kt` ≥ 1, placed before the `configureRouting()` call.
    - `BoundedStoreWiringTest` boots the module and asserts the configured bounded store is in use.
    - The test passes via the targeted gradle command.
  </acceptance_criteria>
  <done>The yaml key exists, startup configures the bounded store, and the wiring smoke test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded-store eviction regression test + fork-deletion static guard (FND-03)</name>
  <files>backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/BoundedStoreRegressionTest.kt, backend/src/test/kotlin/com/jh/proj/coroutineviz/ForkDeletionTest.kt</files>
  <read_first>
    - backend/src/test/kotlin/com/jh/proj/coroutineviz/routes/SessionRoutesTest.kt (testApplication boilerplate, SessionManager.clearAll(), jsonClient helper)
    - backend/coroutine-viz-core/src/test/kotlin/com/jh/proj/coroutineviz/session/EventStoreTest.kt (eviction test ~lines 50-62 in core — the in-isolation analog; this test proves it through the running wiring)
    - .planning/phases/01-foundation-production-readiness/01-RESEARCH.md (§Validation Architecture: emit > maxEvents → store.all().size <= maxEvents through real wiring)
    - .planning/phases/01-foundation-production-readiness/01-CONTEXT.md (§Specifics: bounded store → store.all().size <= maxEvents)
  </read_first>
  <action>
    Create `BoundedStoreRegressionTest.kt`: configure a small `maxEvents` (or use the default), emit more than `maxEvents` events into a session via the real wiring (create session, run a high-volume scenario or send events through the configured SessionManager/VizSession path), then assert `session.store.all().size <= maxEvents` (FND-03). Use the testApplication boilerplate with `SessionManager.clearAll()` in setUp/tearDown.

    Create `ForkDeletionTest.kt`: a static guard asserting zero session-class source files remain under `backend/src/main/kotlin/com/jh/proj/coroutineviz/session/` — list the directory (relative to the test working dir / project root) and assert no `.kt` files match the deleted fork class names (EventStore, VizSession, SessionManager, EventBus, EventApplier, EventContext, FlowEventContext, ChannelEventContext, JobStatusMonitor, ProjectionService). This makes a silent fork return fail CI.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*.BoundedStoreRegressionTest" --tests "*.ForkDeletionTest"</automated>
  </verify>
  <acceptance_criteria>
    - `BoundedStoreRegressionTest` emits > maxEvents events through real wiring and asserts `store.all().size <= maxEvents`.
    - `ForkDeletionTest` fails if any fork session class reappears under `backend/src/main/.../session/`.
    - Both tests pass; the full backend suite (`./gradlew test`) remains green.
  </acceptance_criteria>
  <done>The cap is proven through the running wiring and the fork cannot silently return.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| high-volume event emission → EventStore | Unbounded growth is a DoS/OOM vector |
| config input (SESSION_MAX_EVENTS) → startup | Untrusted/malformed env value parsed at boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | EventStore growth | mitigate | FND-02 wires the bounded core EventStore (ArrayDeque, maxEvents cap); FND-03 regression test proves `store.all().size <= maxEvents`, eliminating the unbounded `CopyOnWriteArrayList` |
| T-01-07 | Tampering (input validation) | `session.maxEvents` env parse | mitigate | `toIntOrNull() ?: 10_000` safe default on malformed input (ASVS V5) |
| T-01-08 | Tampering | reintroduction of the deleted fork | mitigate | `ForkDeletionTest` static guard fails CI if any fork session class reappears |
</threat_model>

<verification>
- `find backend/src/main/.../session -name '*.kt' | wc -l` == 0.
- `cd backend && ./gradlew test` green.
- `application.yaml` contains the `session.maxEvents` key; `Application.kt` calls `SessionManager.configure`.
</verification>

<success_criteria>
- FND-01: fork deleted, backend builds + tests green against core.
- FND-02: yaml key present (default 10000), bounded store configured at startup.
- FND-03: regression test proves the cap; fork-deletion guard in place.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-production-readiness/01-03-SUMMARY.md` when done.
</output>
